schema: moonweave.ai/ontology-node/v1
id: PolicyCombiningAlgorithm
kind: concept
status: accepted
labels:
  zh: 策略组合算法
  en: policy combining algorithm
  ja: ポリシー結合アルゴリズム
semantics:
  short_definition:
    zh: 把多个适用规则结果确定性归并为一个策略决定的版本化规范。
    en: A versioned specification that deterministically combines multiple applicable rule results into one policy decision.
    ja: 複数の適用規則結果を一つの方針判断へ決定的に結合する版管理仕様です。
  definition:
    zh: 策略组合算法是 PolicySpecification 的组成规范，定义多个规则产生 permit、deny、defer、not-applicable 或 indeterminate 时的排序、优先级、短路、平局和默认结果。一次 PolicyDecision 必须记录实际使用的算法版本，不能仅凭规则排列猜测冲突处理。
    en: A PolicyCombiningAlgorithm is a component specification of a PolicySpecification defining ordering, priority, short-circuit, tie, and default behavior when rules yield permit, deny, defer, not-applicable, or indeterminate. Each PolicyDecision records the exact algorithm version used; conflict resolution cannot be inferred merely from rule order.
    ja: PolicyCombiningAlgorithm は PolicySpecification の構成仕様で、規則が permit・deny・defer・not-applicable・indeterminate を返す場合の順序、優先度、短絡、同順位、既定結果を定めます。各 PolicyDecision は使用したアルゴリズム版を記録し、規則順だけから競合処理を推測しません。
  why_needed:
    zh: 它补齐规则冲突 CQ 的决定依据，使 deny-overrides、first-applicable 或自定义策略能够被复现，而不是写成不可验证的口头约定。
    en: It supplies the missing decision basis for the rule-conflict CQ, making deny-overrides, first-applicable, or a custom strategy reproducible rather than an unverifiable convention.
    ja: 規則競合 CQ の判断根拠を補い、deny-overrides・first-applicable・独自方式を検証不能な慣習ではなく再現可能にします。
  includes:
    - zh: 包含算法标识、版本、策略名称、平局处理、缺省结果和确定性要求。
      en: Includes algorithm identity, version, strategy name, tie behavior, default outcome, and determinism requirement.
      ja: アルゴリズム識別子、版、方式名、同順位処理、既定結果、決定性要件を含みます。
  excludes:
    - zh: 不包含一次算法执行、规则本身、最终决定或执行系统的分支路由。
      en: Excludes one algorithm execution, the rules themselves, the final decision, or business routing by an execution system.
      ja: 一回のアルゴリズム実行、規則自体、最終判断、実行系の業務経路選択を含みません。
  semantic_kind: specification
  aliases: []
structure:
  identity_keys:
    - algorithm_id
    - version
  fields:
    - id: algorithm_id
      labels:
        zh: 算法标识
        en: algorithm ID
        ja: アルゴリズムID
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 稳定识别一种规则结果组合策略。
        en: Stable identity for a rule-result combining strategy.
        ja: 規則結果結合方式を安定識別します。
      allowed_values: []
      pattern: null
      example_value: deny-overrides
      source_claims:
        - claim-policy-combining-algorithm-001
    - id: version
      labels:
        zh: 算法版本
        en: algorithm version
        ja: アルゴリズム版
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 确定本次决定采用的不可变算法语义。
        en: Immutable semantic version used by a decision.
        ja: 判断で使用する不変の意味版です。
      allowed_values: []
      pattern: null
      example_value: "1"
      source_claims:
        - claim-policy-combining-algorithm-001
    - id: strategy
      labels:
        zh: 组合策略
        en: combining strategy
        ja: 結合方式
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 描述规则结果的求值和归并顺序。
        en: Describes evaluation and combination order for rule results.
        ja: 規則結果の評価・結合順序を表します。
      allowed_values: []
      pattern: null
      example_value: deny-overrides
      source_claims:
        - claim-policy-combining-algorithm-001
    - id: tie_behavior
      labels:
        zh: 平局处理
        en: tie behavior
        ja: 同順位処理
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 同等优先级结果冲突时的明确处理。
        en: Explicit handling when equal-priority results conflict.
        ja: 同優先度の結果が競合する場合の明示処理です。
      allowed_values: []
      pattern: null
      example_value: deny
      source_claims:
        - claim-policy-combining-algorithm-001
    - id: default_outcome
      labels:
        zh: 缺省结果
        en: default outcome
        ja: 既定結果
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 没有规则产生允许结果时采用的封闭式默认值。
        en: Closed-world default used when no rule produces a permitting result.
        ja: 許可結果を出す規則がない場合の閉世界既定値です。
      allowed_values: []
      pattern: null
      example_value: deny
      source_claims:
        - claim-policy-combining-algorithm-001
  constraints: []
  required_relation_constraints: []
examples:
  
- id: PolicyCombiningAlgorithm-example-positive-manual-001
    kind: positive
    labels:
      zh: 策略组合算法正例
      en: Positive policy combining algorithm case
      ja: ポリシー結合アルゴリズムの正例
    scenario_id: null
    descriptions: {zh: "Open Policy Agent Rego 评估 input 与 data 文档；除非规则显式 allow，否则 default deny。", en: "Open Policy Agent Rego evaluates input and data documents; default deny applies unless a rule explicitly allows (openpolicyagent.org/docs/policy-language).", ja: "Open Policy Agent Rego は input/data 文書を評価し、明示 allow がなければ default deny。"}
  - id: PolicyCombiningAlgorithm-real-coun-2
    kind: counterexample
    labels:
      zh: "PolicyCombiningAlgorithm counterexample real case"
      en: "PolicyCombiningAlgorithm counterexample real-world case"
      ja: "PolicyCombiningAlgorithm counterexample case"
    scenario_id: null
    descriptions: {zh: "Open Policy Agent Rego 评估 input 与 data 文档；除非规则显式 allow，否则 default deny。", en: "Open Policy Agent Rego evaluates input and data documents; default deny applies unless a rule explicitly allows (openpolicyagent.org/docs/policy-language).", ja: "Open Policy Agent Rego は input/data 文書を評価し、明示 allow がなければ default deny。"}
    field_values:
      algorithm_id: deny-overrides
      default_outcome: deny
      strategy: deny-overrides
      tie_behavior: deny
      version: "1"
    related_node_ids:
      - PolicyCombiningAlgorithm
    related_relation_ids: []
    expected_result: {zh: "Record outcome aligned with cited source.", en: "Record a controlled outcome aligned with the cited source.", ja: "Record outcome aligned with cited source."}
    why_valid_or_invalid: {zh: "OPA 产生 allow/deny 决定；不执行沙箱代码。", en: "OPA produces allow/deny decisions; it does not execute sandboxed code.", ja: "OPA は allow/deny 判断を生成する。サンドボックス code は実行しない。"}
    synthetic: false
    source_claims:
      - c

  - id: PolicyCombiningAlgorithm-example-boundary-manual-001
    kind: boundary
    labels:
      zh: 策略组合算法边界反例
      en: Boundary counterexample for policy combining algorithm
      ja: ポリシー結合アルゴリズムの境界反例
    scenario_id: null
    descriptions: {zh: "Open Policy Agent Rego 评估 input 与 data 文档；除非规则显式 allow，否则 default deny。", en: "Open Policy Agent Rego evaluates input and data documents; default deny applies unless a rule explicitly allows (openpolicyagent.org/docs/policy-language).", ja: "Open Policy Agent Rego は input/data 文書を評価し、明示 allow がなければ default deny。"}
  - id: PolicyCombiningAlgorithm-real-inst-4
    kind: instance
    labels:
      zh: "PolicyCombiningAlgorithm instance real case"
      en: "PolicyCombiningAlgorithm instance real-world case"
      ja: "PolicyCombiningAlgorithm instance case"
    scenario_id: null
    descriptions: {zh: "Open Policy Agent Rego 评估 input 与 data 文档；除非规则显式 allow，否则 default deny。", en: "Open Policy Agent Rego evaluates input and data documents; default deny applies unless a rule explicitly allows (openpolicyagent.org/docs/policy-language).", ja: "Open Policy Agent Rego は input/data 文書を評価し、明示 allow がなければ default deny。"}
    field_values:
      algorithm_id: deny-overrides
      default_outcome: deny
      strategy: deny-overrides
      tie_behavior: deny
      version: "1"
    related_node_ids:
      - PolicyCombiningAlgorithm
    related_relation_ids: []
    expected_result: {zh: "Record outcome aligned with cited source.", en: "Record a controlled outcome aligned with the cited source.", ja: "Record outcome aligned with cited source."}
    why_valid_or_invalid: {zh: "OPA 产生 allow/deny 决定；不执行沙箱代码。", en: "OPA produces allow/deny decisions; it does not execute sandboxed code.", ja: "OPA は allow/deny 判断を生成する。サンドボックス code は実行しない。"}
    synthetic: false
    source_claims:
      - c

  - id: PolicyCombiningAlgorithm-real-counterexample-2
    kind: counterexample
    labels: {zh: "PolicyCombiningAlgorithm counterexample case", en: "PolicyCombiningAlgorithm counterexample case", ja: "PolicyCombiningAlgorithm counterexample case"}
    descriptions: {zh: "Amazon Cedar 评估 principal-action-resource-context 请求并返回 Allow 或 Deny 及诊断信息。", en: "Amazon Cedar evaluates principal-action-resource-context requests and returns Allow or Deny with diagnostics (docs.cedarpolicy.com/auth/authorization.html).", ja: "Amazon Cedar は principal-action-resource-context 要求を評価し Allow/Deny と diagnostics を返す。"}
    field_values:
      algorithm_id: deny-overrides
      default_outcome: deny
      strategy: deny-overrides
      tie_behavior: deny
      version: ""1""
    related_node_ids: [PolicyCombiningAlgorithm]
    related_relation_ids: []
    expected_result: {zh: "Record outcome aligned with cited source.", en: "Record a controlled outcome aligned with the cited source.", ja: "Record outcome aligned with cited source."}
    why_valid_or_invalid: {zh: "Cedar 授权与 HumanApproval 记录的用户界面工具批准不同。", en: "Cedar authorization is distinct from UI tool approval recorded by HumanApproval.", ja: "Cedar 認可は HumanApproval が記録する UI ツール承認とは別である。"}
    synthetic: false
    source_claims:
      - c
  - id: PolicyCombiningAlgorithm-real-boundary-3
    kind: boundary
    labels: {zh: "PolicyCombiningAlgorithm boundary case", en: "PolicyCombiningAlgorithm boundary case", ja: "PolicyCombiningAlgorithm boundary case"}
    descriptions: {zh: "NIST SP 800-207 定义零信任：不因网络位置隐式信任；访问资源前离散认证与授权。", en: "NIST SP 800-207 defines zero trust: no implicit trust by network location; discrete authentication and authorization before resource access.", ja: "NIST SP 800-207 は network location による implicit trust なし、resource access 前の discrete 認証・認可を定義する。"}
    field_values:
      algorithm_id: deny-overrides
      default_outcome: deny
      strategy: deny-overrides
      tie_behavior: deny
      version: ""1""
    related_node_ids: [PolicyCombiningAlgorithm]
    related_relation_ids: []
    expected_result: {zh: "Record outcome aligned with cited source.", en: "Record a controlled outcome aligned with the cited source.", ja: "Record outcome aligned with cited source."}
    why_valid_or_invalid: {zh: "NIST 区分策略引擎（决策）与策略执行点；信任边界映射这一分离。", en: "NIST separates policy engine (decision) from policy enforcement point; trust boundaries map to this split.", ja: "NIST は policy engine と PEP を分離する。trust boundary はこの分離に対応する。"}
    synthetic: false
    source_claims:
      - c
  - id: PolicyCombiningAlgorithm-real-instance-4
    kind: instance
    labels: {zh: "PolicyCombiningAlgorithm instance case", en: "PolicyCombiningAlgorithm instance case", ja: "PolicyCombiningAlgorithm instance case"}
    descriptions: {zh: "Open Policy Agent Rego 评估 input 与 data 文档；除非规则显式 allow，否则 default deny。", en: "Open Policy Agent Rego evaluates input and data documents; default deny applies unless a rule explicitly allows (openpolicyagent.org/docs/policy-language).", ja: "Open Policy Agent Rego は input/data 文書を評価し、明示 allow がなければ default deny。"}
    field_values:
      algorithm_id: deny-overrides
      default_outcome: deny
      strategy: deny-overrides
      tie_behavior: deny
      version: ""1""
    related_node_ids: [PolicyCombiningAlgorithm]
    related_relation_ids: []
    expected_result: {zh: "Record outcome aligned with cited source.", en: "Record a controlled outcome aligned with the cited source.", ja: "Record outcome aligned with cited source."}
    why_valid_or_invalid: {zh: "OPA 产生 allow/deny 决定；不执行沙箱代码。", en: "OPA produces allow/deny decisions; it does not execute sandboxed code.", ja: "OPA は allow/deny 判断を生成する。サンドボックス code は実行しない。"}
    synthetic: false
    source_claims:
      - c
external_mappings: []
applicability:
  - core
sibling_differentiation: []
parent_relation:
  id: PolicySpecification-uses-PolicyCombiningAlgorithm
  predicate: part_of
  direction: target-to-source
  relation_kind: composition
  labels:
    zh: 采用组合算法
    en: uses combining algorithm
    ja: 結合アルゴリズムを使う
  definitions:
    zh: 一个策略规范版本选择一个明确版本的 PolicyCombiningAlgorithm，规定其规则结果的冲突、优先级、平局和缺省处理。
    en: A PolicySpecification version selects a specific PolicyCombiningAlgorithm version governing conflict, priority, ties, and defaults among its rule results.
    ja: 一つの PolicySpecification 版が特定 PolicyCombiningAlgorithm 版を選び、規則結果の競合・優先度・同順位・既定処理を定めます。
  cardinality: null
  cardinality_not_applicable_reason:
    zh: 本关系描述本体事实；实例基数由所引用规范与生命周期约束决定。
    en: This relation states an ontology fact; instance cardinality is governed by the referenced specification and lifecycle constraints.
    ja: この関係は本体事実を表し、実体基数は参照仕様とライフサイクル制約で決まります。
  inverse_reading:
    predicate: part_of
    labels:
      zh: 被策略采用
      en: is used by policy
      ja: 方針に使用される
  conditions: []
  temporal_scope: timeless
  boundary_context: null
  constraints: []
  examples:
    - id: PolicySpecification-uses-PolicyCombiningAlgorithm-example-positive-manual-001
      kind: positive
      labels:
        zh: 采用组合算法正例
        en: Positive uses combining algorithm case
        ja: 結合アルゴリズムを使うの正例
      scenario_id: null
      descriptions:
        zh: tool-authorization@7 采用 deny-overrides@1，并在策略版本元数据中固定该引用。
        en: tool-authorization@7 uses deny-overrides@1 and fixes that reference in policy-version metadata.
        ja: tool-authorization@7 は deny-overrides@1 を使用し、方針版メタデータで参照を固定します。
      field_values: {}
      related_node_ids:
        - PolicySpecification
        - PolicyCombiningAlgorithm
      related_relation_ids:
        - PolicySpecification-uses-PolicyCombiningAlgorithm
      expected_result:
        zh: 任意评估该版本都能解析同一组合语义。
        en: Every evaluation of the version resolves the same combining semantics.
        ja: その版の任意評価が同じ結合意味を解決します。
      why_valid_or_invalid:
        zh: 组合算法是版本化策略组成，不由运行时随机选择。
        en: The combining algorithm is versioned policy content rather than a runtime random choice.
        ja: 結合アルゴリズムは版管理方針内容で実行時ランダム選択ではありません。
      synthetic: true
      verified_version: https://moonweave.ai/ontology/agent-system/2.0.0/
      source_claims:
        - claim-policy-combining-algorithm-001
    - id: PolicySpecification-uses-PolicyCombiningAlgorithm-example-boundary-manual-001
      kind: boundary
      labels:
        zh: 采用组合算法边界反例
        en: Boundary counterexample for uses combining algorithm
        ja: 結合アルゴリズムを使うの境界反例
      scenario_id: null
      descriptions:
        zh: evaluation-42 按性能优化调整规则求值顺序，但不能因此改变策略声明的 deny-overrides@1。
        en: evaluation-42 changes rule evaluation order for performance but cannot thereby change the policy-declared deny-overrides@1 semantics.
        ja: evaluation-42 は性能のため規則評価順を変えても方針宣言 deny-overrides@1 の意味を変えられません。
      field_values: {}
      related_node_ids:
        - PolicySpecification
        - PolicyCombiningAlgorithm
      related_relation_ids:
        - PolicySpecification-uses-PolicyCombiningAlgorithm
      expected_result:
        zh: 实现优化必须产生等价结果，uses 关系仍指向已声明算法。
        en: Implementation optimization must preserve equivalent results and uses still points to the declared algorithm.
        ja: 実装最適化は同等結果を保ち uses は宣言アルゴリズムを指し続けます。
      why_valid_or_invalid:
        zh: 执行顺序与规范组合语义不是同一事实。
        en: Execution order and normative combining semantics are not the same fact.
        ja: 実行順と規範結合意味は同一事実ではありません。
      synthetic: true
      verified_version: https://moonweave.ai/ontology/agent-system/2.0.0/
      source_claims:
        - claim-policy-combining-algorithm-001
  source_claims:
    - claim-policy-combining-algorithm-001
  distinct_fact_rationale: null
  layout_role: primary-backbone
  review_status: accepted
  endpoint_order: parent-to-child
relations: []
review:
  status: accepted
  method: manual-semantic
  reviewer: manual-safety-verification
  reviewed_on: 2026-07-17
  note:
    zh: 已对照 XACML 3.0、NIST SP 800-162、OAuth/MCP 授权与 Cursor 工具批准模式逐项人工语义核验；三值策略决定与授权闭环成立。
    en: Manually verified against XACML 3.0, NIST SP 800-162, OAuth/MCP authorization, and Cursor-style tool approval; three-valued policy decisions and authorization loop hold.
    ja: XACML 3.0、NIST SP 800-162、OAuth/MCP authorization、Cursor-style tool approval に照らし手動意味審査を完了しました。
  sections:
    semantics: accepted
    hierarchy: accepted
    relations: accepted
    structure: accepted
    examples: accepted
    source_claims: accepted
source_claims:
  - id: claim-policy-combining-algorithm-001
    source: eng-policy-xacml-3
    supports: XACML normatively separates a policy and its rules, targets or conditions, Permit or Deny effects, obligations, decision values, and rule or policy combining algorithms.
    locator: "OASIS XACML 3.0 Core, Sections 3, 5 and 7.11-7.18: policy model, rule evaluation, decisions, obligations, and combining algorithms"
    evidence_kind: normative
    confidence: high
    review_status: accepted
