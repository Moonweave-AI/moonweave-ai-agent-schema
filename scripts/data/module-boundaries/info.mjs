import { deepFreeze } from "./deep-freeze.mjs";

const localized = (zh, en, ja) => ({ zh, en, ja });

export const INFO_MODULE_BOUNDARIES = deepFreeze({
  "info-container-command": {
    purpose: localized("把命令或进程执行产生的输出与退出状态转化为可进入上下文的有序观测证据。", "Turn output and exit status from command or process execution into ordered observational evidence that may enter context.", "コマンドまたはプロセス実行の出力と終了状態を、コンテキストへ取り込める順序付き観測証拠へ変換します。"),
    includes: [
      localized("命令输出观测、退出状态观测、标准输出与标准错误流及其有序分块。", "Command-output observations, exit-status observations, stdout and stderr streams, and their ordered chunks.", "コマンド出力観測、終了状態観測、標準出力・標準エラーストリーム、および順序付きチャンク。"),
      localized("执行结果、工作目录和环境绑定的引用，以及观测进入上下文的接纳事件。", "References to execution results, working directories, and environment bindings, plus admission events that bring observations into context.", "実行結果、作業ディレクトリ、環境バインディングへの参照、および観測をコンテキストへ取り込む受入イベント。"),
    ],
    excludes: [
      localized("命令调用、执行尝试、重试与结果身份归调用与执行或执行尝试与结果；这里不声称执行了命令。", "Command invocation, execution attempts, retries, and result identity belong to invocation and execution or execution attempts and outcomes; this scope does not claim to execute commands.", "コマンド呼出し、実行試行、再試行、結果の同一性は呼出しと実行または実行試行と結果に属し、ここではコマンド実行そのものを所有しません。"),
      localized("通用文本、代码或文件内容形式归内容表示；长期日志流与 Sink 归日志与遥测。", "Generic text, code, and file content forms belong to content representation; durable log streams and sinks belong to logging and telemetry.", "一般的なテキスト、コード、ファイル内容形式は内容表現、永続ログストリームと Sink はログとテレメトリに属します。"),
    ],
    owns_when: localized("当对象的身份由某次执行的输出通道、分块顺序、退出状态或进入上下文的观测接纳决定时，在此拥有。", "Own an object when its identity is determined by an execution's output channel, chunk order, exit status, or admission of an observation into context.", "対象の同一性が実行の出力チャネル、チャンク順序、終了状態、または観測のコンテキスト受入で決まる場合に所有します。"),
    references_when: localized("运行追踪、诊断或上下文组装消费执行输出证据而不改变其通道和顺序身份时，引用这里的观测。", "Runtime tracing, diagnostics, or context assembly references these observations when consuming execution-output evidence without changing channel and order identity.", "実行トレース、診断、コンテキスト組み立てがチャネルと順序の同一性を変えず出力証拠を利用するとき、ここで定義した観測を参照します。"),
    competency_questions: [
      {
        semantic_key: "info-container-command.ordered-output",
        related_module_ids: ["runtime-execution-attempts"],
        questions: localized("某次执行的 stdout 与 stderr 分块按各自序号重组后分别是什么，是否存在缺块？", "What do stdout and stderr become when chunks from one execution are reassembled by channel sequence, and are any chunks missing?", "一回の実行の stdout と stderr チャンクを各チャネルの順序で再構成すると何になり、欠落チャンクはありますか。"),
        query: "For one execution reference, partition OutputChunk values by stream kind, order by sequence, and detect sequence gaps or duplicates.",
        expected_assertion: "Each channel yields a deterministic byte/text sequence; missing and duplicate positions are reported rather than silently reordered.",
      },
      {
        semantic_key: "info-container-command.exit-status-evidence",
        related_module_ids: ["feedback-warning-error"],
        questions: localized("退出码、终止信号和完成时间共同支持哪个退出状态观测，是否足以判定成功或失败？", "Which exit-status observation is supported by exit code, termination signal, and completion time, and is it sufficient to determine success or failure?", "終了コード、終了シグナル、完了時刻はどの終了状態観測を裏付け、成功または失敗の判定に十分ですか。"),
        query: "Resolve an ExitStatusObservation to its execution reference and recorded fields, then apply the declared status interpretation policy.",
        expected_assertion: "Exit code remains a field; success or failure is asserted only when the interpretation policy and required fields are present.",
      },
      {
        semantic_key: "info-container-command.context-admission",
        related_module_ids: ["memory-context"],
        questions: localized("哪些执行观测被接纳进某个上下文版本，选择范围、截断和来源引用是什么？", "Which execution observations were admitted to a context version, and what selection range, truncation, and source references were used?", "どの実行観測があるコンテキスト版へ受け入れられ、選択範囲、切詰め、出典参照は何ですか。"),
        query: "Follow ContextIngressEvent to the admitted observation, selected chunk range, truncation metadata, and immutable ContextPackage version.",
        expected_assertion: "Every admitted fragment points to its original observation and range; ContextIngressEvent is not classified as an observation subtype.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "runtime-observability",
        decision: "owns",
        owner_module_id: "info-container-command",
        rationale: localized("执行观测拥有供上下文消费的输出流、分块和退出状态证据；追踪与检查点拥有 Trace、Span、事件和状态恢复记录。", "Execution observations own context-consumable output streams, chunks, and exit-status evidence; tracing and checkpoints own Trace, Span, events, and state-recovery records.", "実行観測はコンテキストが利用する出力ストリーム、チャンク、終了状態証拠を所有し、トレースとチェックポイントは Trace、Span、イベント、状態回復記録を所有します。"),
      },
      {
        other_module_id: "runtime-execution-attempts",
        decision: "owns",
        owner_module_id: "info-container-command",
        rationale: localized("命令输出观察拥有 stdout、stderr、exit-status 的有序观测片段及其来源引用；执行尝试拥有产生这些观察的 RunAttempt、RunResult 和 Outcome。", "Command-output observations own ordered stdout, stderr, and exit-status evidence with source references; execution attempts own the RunAttempt, RunResult, and Outcome that produced it.", "command output observation は stdout、stderr、exit-status の順序付き証拠と source reference を所有し、execution attempt はそれを生成した RunAttempt、RunResult、Outcome を所有します。"),
      },
      {
        other_module_id: "feedback-warning-error",
        decision: "owns",
        owner_module_id: "info-container-command",
        rationale: localized("命令输出观察拥有原始 exit code、signal、stdout/stderr 片段和完成时刻；失败与诊断拥有把这些证据解释为 Warning、ErrorEvent 或 FailureMode 的判断。", "Command-output observations own raw exit code, signal, stdout or stderr chunks, and completion time; failures and diagnostics own judgments interpreting that evidence as Warning, ErrorEvent, or FailureMode.", "command output observation は raw exit code、signal、stdout/stderr chunk、completion time を所有し、failure/diagnostic はそれを Warning、ErrorEvent、FailureMode と解釈する判断を所有します。"),
      },
      {
        other_module_id: "memory-context",
        decision: "owns",
        owner_module_id: "info-container-command",
        rationale: localized("命令输出观察拥有可被选择的执行证据及其通道顺序；上下文组装拥有哪些观察进入 ContextPackage、占据哪个 Slot 以及如何截断的决定。", "Command-output observations own selectable execution evidence and channel order; context assembly owns decisions about which observations enter a ContextPackage, which Slot they occupy, and how they are trimmed.", "command output observation は選択可能な execution evidence と channel order を所有し、context assembly は何を ContextPackage に入れ、どの Slot を占め、どう trim するかを所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "runtime-observability",
        semantic_area: localized("执行输出证据与运行追踪事件", "Execution-output evidence versus runtime trace events", "実行出力証拠と実行トレースイベント"),
        overlap_reason: localized("两者都引用同一次执行，但输出观测按通道和分块组织可见内容，追踪事件按因果与时间组织运行活动。", "Both reference the same execution, but output observations organize visible content by channel and chunk while trace events organize activity by causality and time.", "どちらも同じ実行を参照しますが、出力観測は可視内容をチャネルとチャンクで、トレースイベントは活動を因果と時刻で組織します。"),
        disambiguation_test: localized("若对象必须用 stdout、stderr、chunk sequence 或 exit status 才能识别，则归执行观测；若用 trace/span 上下文和发生时间识别，则归追踪与检查点。", "If identity requires stdout, stderr, chunk sequence, or exit status, it belongs to execution observations; if it requires trace/span context and occurrence time, it belongs to tracing and checkpoints.", "同一性に stdout、stderr、chunk sequence、exit status が必要なら実行観測、trace/span 文脈と発生時刻が必要ならトレースとチェックポイントに属します。"),
        result: "distinct",
        owner_module_id: "info-container-command",
      },
      {
        other_module_id: "runtime-execution-attempts",
        semantic_area: localized("执行发生与命令输出观察", "Execution occurrence versus command-output observation", "execution occurrence と command output observation"),
        overlap_reason: localized("一个 Result 可产生多条分频道观察，观察是可进入上下文的证据载体而不是新的执行或结果。", "One Result can yield many channel-specific observations; an observation is evidence eligible for context, not a new execution or result.", "一 Result は複数 channel observation を生成でき、observation は context 候補の証拠であって新 execution/result ではありません。"),
        disambiguation_test: localized("由 execution/attempt id 和终止结果识别的对象归执行尝试；由 channel、sequence 和观测范围识别的对象归命令输出观察。", "An object identified by execution or attempt ID and terminal result belongs to execution attempts; one identified by channel, sequence, and observed range belongs to command-output observations.", "execution/attempt id と terminal result で識別されれば execution attempt、channel・sequence・observed range で識別されれば command output observation に属します。"),
        result: "distinct",
        owner_module_id: "info-container-command",
      },
      {
        other_module_id: "feedback-warning-error",
        semantic_area: localized("原始进程观察与诊断解释", "Raw process observations versus diagnostic interpretation", "raw process observation と diagnostic 解釈"),
        overlap_reason: localized("非零退出可能被策略接受，零退出也可能包含语义错误；观察本身不决定失败分类。", "A nonzero exit can be accepted by policy and a zero exit can still contain semantic failure, so the observation itself does not determine failure classification.", "nonzero exit が policy で許容され、zero exit に意味失敗が含まれることもあり、observation 自体は failure class を決めません。"),
        disambiguation_test: localized("无需诊断规则即可逐字重放的事实归命令输出观察；需要严重性、失败模式或根因推理的对象归失败与诊断。", "Facts replayable verbatim without a diagnostic rule belong to command-output observations; objects requiring severity, failure-mode, or root-cause reasoning belong to failures and diagnostics.", "diagnostic rule なしで逐語 replay できる事実は command output observation、severity・failure mode・root cause 推論が必要なら failure/diagnostic に属します。"),
        result: "distinct",
        owner_module_id: "info-container-command",
      },
      {
        other_module_id: "memory-context",
        semantic_area: localized("输出观察来源与上下文接纳", "Output-observation source versus context admission", "output observation source と context admission"),
        overlap_reason: localized("同一观察可被多个上下文版本以不同范围引用，ContextIngressEvent 不改变原始输出的身份和顺序。", "The same observation can be referenced by several context versions with different ranges; ContextIngressEvent does not change source-output identity or order.", "同一 observation は複数 context version から異なる range で参照でき、ContextIngressEvent は source output の identity/order を変えません。"),
        disambiguation_test: localized("由 execution、channel 和 sequence 识别的对象归命令输出观察；由 context version、slot、选择或截断决定识别的对象归上下文组装。", "An object identified by execution, channel, and sequence belongs to command-output observations; one identified by context version, slot, selection, or trimming decision belongs to context assembly.", "execution・channel・sequence で識別されれば command output observation、context version・slot・selection/trim decision で識別されれば context assembly に属します。"),
        result: "distinct",
        owner_module_id: "info-container-command",
      },
    ],
  },
  "info-content-block-modality": {
    purpose: localized("用可扩展的内容块分类描述信息采用何种载体形式，并统一每种形式的结构字段与验证约束。", "Describe the carrier form of information through an extensible content-block taxonomy and standardize structural fields and validation constraints for each form.", "拡張可能な内容ブロック分類で情報の媒体形式を記述し、各形式の構造項目と検証制約を統一します。"),
    includes: [
      localized("ContentBlock 及文本、代码、图像、音频、表格、结构化数据和文件附件块的分类骨架。", "The ContentBlock hierarchy for text, code, image, audio, table, structured-data, and file-attachment blocks.", "ContentBlock と、テキスト、コード、画像、音声、表、構造化データ、ファイル添付ブロックの分類骨格。"),
      localized("mime type、encoding、token count、尺寸、语言和内容摘要等块级字段与约束。", "Block-level fields and constraints such as MIME type, encoding, token count, dimensions, language, and content digest.", "MIME type、encoding、token count、寸法、言語、内容ダイジェストなどのブロック単位項目と制約。"),
    ],
    excludes: [
      localized("Conversation、Turn、Message 和消息角色归消息与会话；Message 通过 contains 连接内容块，而不是内容块子类。", "Conversation, Turn, Message, and message roles belong to messages and conversations; Message connects to content blocks through contains and is not a content-block subtype.", "Conversation、Turn、Message、メッセージ役割はメッセージと会話に属し、Message は contains で内容ブロックへ接続し、下位型にはなりません。"),
      localized("提示模板、指令权威和冲突解决归提示与指令；内容是否成为指令不由模态决定。", "Prompt templates, instruction authority, and conflict resolution belong to prompts and instructions; modality does not determine whether content is an instruction.", "プロンプトテンプレート、指示権威、競合解決はプロンプトと指示に属し、内容が指示かどうかはモダリティで決まりません。"),
    ],
    owns_when: localized("当对象可由内容载体类型、编码边界和块级结构独立识别，而不依赖其所在消息或用途时，在此拥有。", "Own an object when it is independently identifiable by carrier type, encoding boundary, and block structure without depending on its containing message or purpose.", "対象が所属メッセージや用途に依存せず、媒体型、符号化境界、ブロック構造で独立識別できる場合に所有します。"),
    references_when: localized("消息、提示、记忆分块或输出披露需要表达具体内容形式时，引用 ContentBlock 或其专门类型。", "Messages, prompts, memory chunks, or output disclosure reference ContentBlock or a specialization when expressing a concrete content form.", "メッセージ、プロンプト、記憶チャンク、出力開示が具体的内容形式を表すとき、ContentBlock またはその特殊型を参照します。"),
    competency_questions: [
      {
        semantic_key: "info-content-block-modality.block-classification",
        related_module_ids: ["info-messages-instructions"],
        questions: localized("给定 MIME 类型、编码和结构字段，一个内容块应归入哪个最具体的已接受类型？", "Given MIME type, encoding, and structural fields, which most specific accepted content-block type applies?", "MIME type、encoding、構造項目が与えられたとき、内容ブロックはどの最も具体的な承認済み型に分類されますか。"),
        query: "Match block discriminators against accepted ContentBlock subtype constraints and choose the unique deepest compatible type.",
        expected_assertion: "Exactly one most-specific type is returned; incompatible or insufficient discriminators produce a validation result, not a fabricated subtype.",
      },
      {
        semantic_key: "info-content-block-modality.message-containment",
        related_module_ids: ["info-messages-instructions"],
        questions: localized("一条消息包含哪些有序内容块，每个块的形式是什么，而消息本身为何不是 ContentBlock？", "Which ordered content blocks does a message contain, what is each block's form, and why is the message itself not a ContentBlock?", "一つのメッセージはどの順序付き内容ブロックを含み、各形式は何で、メッセージ自体が ContentBlock でないのはなぜですか。"),
        query: "Traverse Message contains ContentBlock edges ordered by position and inspect semantic kinds of both endpoints.",
        expected_assertion: "All members are typed ContentBlock specializations; Message remains an envelope/information structure connected by composition, not is_a.",
      },
      {
        semantic_key: "info-content-block-modality.attachment-integrity",
        related_module_ids: ["info-storage-sources"],
        questions: localized("文件附件块的媒体类型、长度和摘要是否与它引用的来源对象一致？", "Do a file-attachment block's media type, length, and digest agree with the source object it references?", "ファイル添付ブロックの媒体型、長さ、ダイジェストは参照先の出典オブジェクトと一致しますか。"),
        query: "Compare FileAttachmentBlock fields with the referenced SourceIdentity media type, byte length, and digest.",
        expected_assertion: "Integrity passes only when all available identity fields agree; the source object itself remains owned by its source domain.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "info-messages-instructions",
        decision: "owns",
        owner_module_id: "info-content-block-modality",
        rationale: localized("内容表示拥有载体形式及其结构约束；消息与会话拥有消息包络、发言角色、轮次和会话顺序。", "Content representation owns carrier forms and their structural constraints; messages and conversations own message envelopes, speaking roles, turns, and conversational order.", "内容表現は媒体形式と構造制約を所有し、メッセージと会話はメッセージ包絡、発話役割、ターン、会話順序を所有します。"),
      },
      {
        other_module_id: "info-storage-sources",
        decision: "owns",
        owner_module_id: "info-content-block-modality",
        rationale: localized("内容表示拥有 TextBlock、CodeBlock、ImageBlock 等载体形式及 MIME、编码和结构约束；存储与来源拥有被引用资源的位置、版本、摘要和出处。", "Content representation owns carrier forms such as TextBlock, CodeBlock, and ImageBlock plus MIME, encoding, and structural constraints; storage and sources own referenced resource location, version, digest, and provenance.", "content representation は TextBlock、CodeBlock、ImageBlock 等の carrier form と MIME・encoding・structure constraint を所有し、storage/source は参照 resource の location・version・digest・provenance を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "info-messages-instructions",
        semantic_area: localized("内容块与消息内容位置", "Content blocks versus positions within messages", "内容ブロックとメッセージ内の位置"),
        overlap_reason: localized("同一文本既有内容形式也位于某条消息中，但形式不随发送者变化，消息身份则依赖会话和轮次。", "The same text has a content form and occupies a message position, but its form does not change with sender while message identity depends on conversation and turn.", "同じテキストには内容形式とメッセージ内位置がありますが、形式は送信者で変わらず、メッセージの同一性は会話とターンに依存します。"),
        disambiguation_test: localized("若脱离 Conversation、Turn 和 sender 后对象仍以编码内容成立，则归内容表示；若身份依赖发言位置或包络，则归消息与会话。", "If the object remains encoded content after removing Conversation, Turn, and sender, it belongs to content representation; if identity depends on speaking position or envelope, it belongs to messages and conversations.", "Conversation、Turn、sender を除いても符号化内容として成立するなら内容表現、同一性が発話位置または包絡に依存するならメッセージと会話に属します。"),
        result: "distinct",
        owner_module_id: "info-content-block-modality",
      },
      {
        other_module_id: "info-storage-sources",
        semantic_area: localized("内容载体形式与来源定位", "Content carrier form versus source location", "content carrier form と source location"),
        overlap_reason: localized("FileAttachmentBlock 可引用 FileResourceReference，但 block 的媒体结构与源文件的持久身份是两个可独立变化的对象。", "A FileAttachmentBlock can reference a FileResourceReference, but block media structure and durable source-file identity can change independently.", "FileAttachmentBlock は FileResourceReference を参照できますが、block media structure と durable source file identity は独立に変化します。"),
        disambiguation_test: localized("由媒体类型、编码和 block 结构识别的对象归内容表示；由 URI、版本、range 或 digest 识别的对象归存储与来源。", "An object identified by media type, encoding, and block structure belongs to content representation; one identified by URI, version, range, or digest belongs to storage and sources.", "media type・encoding・block structure で識別されれば content representation、URI・version・range・digest で識別されれば storage/source に属します。"),
        result: "distinct",
        owner_module_id: "info-content-block-modality",
      },
    ],
  },
  "info-indexing": {
    purpose: localized("以 DiscoveryActivity 为关键概念，在当前交互会话内执行轻量发现，并保留 DiscoveryResult、排名分值观测、索引视图与候选来源。", "Use DiscoveryActivity as the key notion for lightweight discovery within the current interaction session, preserving DiscoveryResult, rank and score observations, index views, and candidate provenance.", "DiscoveryActivity を中核概念として現在の対話 session 内で軽量発見を実行し、DiscoveryResult、rank/score observation、index view、candidate provenance を保持します。"),
    includes: [
      localized("DiscoveryActivity、LightweightRetrieval、DiscoveryResult、CandidateRank、DiscoveryScore、SearchResult 和 SearchScore。", "DiscoveryActivity, LightweightRetrieval, DiscoveryResult, CandidateRank, DiscoveryScore, SearchResult, and SearchScore.", "DiscoveryActivity、LightweightRetrieval、DiscoveryResult、CandidateRank、DiscoveryScore、SearchResult、SearchScore。"),
      localized("DiscoveryIndex、LightIndex、IndexPointer、IndexVersionReference，以及发现活动使用的查询与过滤关系事实。", "DiscoveryIndex, LightIndex, IndexPointer, IndexVersionReference, and relation facts for query and filter conditions used by discovery.", "DiscoveryIndex、LightIndex、IndexPointer、IndexVersionReference、および discovery が使う query/filter relation fact。"),
    ],
    excludes: [
      localized("持久 Index、Embedding、IndexShard、IndexEntry、IndexBuildRun 和 IndexRefreshRun 归表征与索引；LightIndex 只引用其版本。", "Persistent Index, Embedding, IndexShard, IndexEntry, IndexBuildRun, and IndexRefreshRun belong to representations and indexes; LightIndex only references their version.", "persistent Index、Embedding、IndexShard、IndexEntry、IndexBuildRun、IndexRefreshRun は表現と索引に属し、LightIndex はその version を参照するだけです。"),
      localized("跨库检索、融合、重排、TopK 和可重放 RetrievalTrace 归检索与排序；工具候选发现归能力发现与选择。", "Cross-store retrieval, fusion, reranking, TopK, and replayable RetrievalTrace belong to retrieval and ranking; tool-candidate discovery belongs to capability discovery and selection.", "複数ストア検索、融合、再順位付け、TopK、再生可能 RetrievalTrace は検索と順位付け、ツール候補発見は能力発見と選択に属します。"),
    ],
    owns_when: localized("当对象由一个交互会话内的 DiscoveryActivity、DiscoveryResult、CandidateRank 或 DiscoveryScore 识别，且不建立持久索引身份时，在此拥有。", "Own an object when it is identified by a session-local DiscoveryActivity, DiscoveryResult, CandidateRank, or DiscoveryScore without creating persistent index identity.", "対象が session-local DiscoveryActivity、DiscoveryResult、CandidateRank、DiscoveryScore で識別され、persistent index identity を作らない場合に所有します。"),
    references_when: localized("上下文组装消费 DiscoveryResult，或 DiscoveryActivity 通过 LightIndex 读取持久索引时，分别引用发现结果与 IndexVersion。", "Context assembly references DiscoveryResult when consuming discovered material, while DiscoveryActivity references IndexVersion through LightIndex when reading a persistent index.", "context assembly が discovered material を使うときは DiscoveryResult、DiscoveryActivity が LightIndex で persistent index を読むときは IndexVersion を参照します。"),
    competency_questions: [
      {
        semantic_key: "info-indexing.session-candidate-provenance",
        related_module_ids: ["memory-context"],
        questions: localized("某次上下文发现返回了哪些候选，每项来自哪里、匹配了什么查询条件？", "Which candidates did a context-discovery act return, where did each originate, and which query condition did it match?", "あるコンテキスト発見が返した候補は何で、各候補はどこから来て、どのクエリ条件に一致しましたか。"),
        query: "For one DiscoveryActivity, list ordered candidates with source reference, matched predicate, relevance observation, and session identity.",
        expected_assertion: "Every candidate has one source reference and matching explanation within the same session; no persistent retrieval result is invented.",
      },
      {
        semantic_key: "info-indexing.light-index-view",
        related_module_ids: ["memory-embedding-indexes"],
        questions: localized("一个 LightIndex 引用了哪个持久索引版本和范围，它是否错误地声明了独立分片或构建生命周期？", "Which persistent index version and scope does a LightIndex reference, and does it incorrectly claim independent shards or a build lifecycle?", "LightIndex はどの永続索引版と範囲を参照し、独自 shard または構築ライフサイクルを誤って主張していませんか。"),
        query: "Resolve LightIndex to one IndexVersion reference and inspect for locally owned shard, entry, build, or refresh identities.",
        expected_assertion: "A valid LightIndex has one persistent version reference and session scope, with no independently owned storage or build objects.",
      },
      {
        semantic_key: "info-indexing.discovery-versus-retrieval",
        related_module_ids: ["memory-retrieval-ranking"],
        questions: localized("一个候选结果属于会话内发现，还是属于带索引版本、融合和重排证据的持久检索？", "Is a candidate result session-local discovery or persistent retrieval with index-version, fusion, and reranking evidence?", "候補結果はセッション内発見ですか、それとも索引版、融合、再順位付け証拠を持つ永続検索ですか。"),
        query: "Classify the result by identity: interaction session and lightweight query versus RetrievalRequest, index version, scoring pipeline, and replay trace.",
        expected_assertion: "Session-only candidates belong to context discovery; results requiring a replayable retrieval pipeline belong to retrieval and ranking.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "memory-retrieval-ranking",
        decision: "owns",
        owner_module_id: "info-indexing",
        rationale: localized("上下文发现拥有会话内轻量候选和相关性观测；检索与排序拥有跨持久存储的请求、评分、融合、重排和最终结果。", "Context discovery owns session-local lightweight candidates and relevance observations; retrieval and ranking own requests, scoring, fusion, reranking, and final results across persistent stores.", "コンテキスト発見はセッション内の軽量候補と関連性観測を所有し、検索と順位付けは永続ストア横断の要求、採点、融合、再順位付け、最終結果を所有します。"),
      },
      {
        other_module_id: "memory-context",
        decision: "owns",
        owner_module_id: "info-indexing",
        rationale: localized("上下文发现拥有一次 DiscoveryActivity、查询条件、轻量索引视图和候选结果；上下文组装拥有从候选中选择、排序、裁剪并形成 ContextPackage/Window 的过程。", "Context discovery owns one DiscoveryActivity, query conditions, lightweight index view, and candidate results; context assembly owns selecting, ordering, trimming, and packaging candidates into ContextPackage or ContextWindow.", "context discovery は一 DiscoveryActivity、query condition、lightweight index view、candidate result を所有し、context assembly は候補を選択・整列・trim して ContextPackage/Window にする過程を所有します。"),
      },
      {
        other_module_id: "memory-embedding-indexes",
        decision: "owns",
        owner_module_id: "info-indexing",
        rationale: localized("上下文发现拥有 session 级 LightIndex、IndexPointer 和发现结果；表征与索引拥有持久 Index、IndexVersion、Shard、Entry 以及构建和刷新活动。", "Context discovery owns session-level LightIndex, IndexPointer, and discovery results; representations and indexes own persistent Index, IndexVersion, Shard, Entry, and build or refresh activities.", "context discovery は session-level LightIndex、IndexPointer、discovery result を所有し、representation/index は persistent Index、IndexVersion、Shard、Entry と build/refresh activity を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "memory-retrieval-ranking",
        semantic_area: localized("轻量发现候选与持久检索候选", "Lightweight discovery candidates versus persistent retrieval candidates", "軽量発見候補と永続検索候補"),
        overlap_reason: localized("两者都产生候选和分数，但发现面向当前会话的快速提示，检索要求可重放的数据源、索引版本和排序管线。", "Both produce candidates and scores, but discovery gives fast hints for the current session while retrieval requires replayable sources, index versions, and ranking pipelines.", "どちらも候補と得点を生成しますが、発見は現在セッション向けの高速手掛かり、検索は再生可能なデータ源、索引版、順位付け管線を必要とします。"),
        disambiguation_test: localized("若只有会话、临时查询和相关性观测即可识别，则归上下文发现；若必须记录持久索引版本与评分阶段才能重放，则归检索与排序。", "If session, temporary query, and relevance observation suffice for identity, it belongs to context discovery; if replay requires persistent index version and scoring stages, it belongs to retrieval and ranking.", "セッション、一時クエリ、関連性観測だけで識別できればコンテキスト発見、再生に永続索引版と採点段階が必要なら検索と順位付けに属します。"),
        result: "distinct",
        owner_module_id: "info-indexing",
      },
      {
        other_module_id: "memory-context",
        semantic_area: localized("候选发现与上下文组装", "Candidate discovery versus context assembly", "candidate discovery と context assembly"),
        overlap_reason: localized("DiscoveryResult 说明可用候选及相关性，ContextSelectionActivity 决定其中哪些在预算和策略下实际进入上下文。", "DiscoveryResult states available candidates and relevance; ContextSelectionActivity decides which actually enter context under budget and policy.", "DiscoveryResult は利用可能 candidate と relevance を示し、ContextSelectionActivity は budget/policy 下で実際に context へ入るものを決めます。"),
        disambiguation_test: localized("若对象可在尚未创建 ContextPackage 时存在，且由 discovery run 识别，则归上下文发现；若依赖 package version、slot 或预算取舍，则归上下文组装。", "If an object exists before any ContextPackage and is identified by a discovery run, it belongs to context discovery; if it depends on package version, slot, or budget tradeoff, it belongs to context assembly.", "ContextPackage 作成前に存在し discovery run で識別されれば context discovery、package version・slot・budget tradeoff に依存すれば context assembly に属します。"),
        result: "distinct",
        owner_module_id: "info-indexing",
      },
      {
        other_module_id: "memory-embedding-indexes",
        semantic_area: localized("轻量发现视图与持久索引结构", "Lightweight discovery view versus persistent index structure", "lightweight discovery view と persistent index structure"),
        overlap_reason: localized("LightIndex 只能引用已有 IndexVersion，不拥有独立 shard、entry 或构建生命周期；持久索引可跨会话复用。", "LightIndex only references an existing IndexVersion and owns no independent shard, entry, or build lifecycle; a persistent index is reusable across sessions.", "LightIndex は既存 IndexVersion を参照するだけで、独自 shard・entry・build lifecycle を所有せず、persistent index は session 横断で再利用できます。"),
        disambiguation_test: localized("由 session 与发现查询识别的索引视图归上下文发现；由 index id、version、build run 和持久条目识别的对象归表征与索引。", "An index view identified by session and discovery query belongs to context discovery; an object identified by index ID, version, build run, and persistent entries belongs to representations and indexes.", "session と discovery query で識別される index view は context discovery、index id・version・build run・persistent entry で識別されれば representation/index に属します。"),
        result: "distinct",
        owner_module_id: "info-indexing",
      },
    ],
  },
  "info-messages-instructions": {
    purpose: localized("表达会话、轮次、消息和消息包络的顺序结构，使不同发送者和协议消息可以在同一对话链中定位。", "Represent the ordered structure of conversations, turns, messages, and message envelopes so messages from different senders and protocols can be located in one dialogue lineage.", "会話、ターン、メッセージ、メッセージ包絡の順序構造を表し、異なる送信者・プロトコルのメッセージを一つの対話系譜で位置付けます。"),
    includes: [
      localized("Conversation、ConversationTurn、有序 MessageHistory，以及 turn 与 message 的组成和顺序关系。", "Conversation, ConversationTurn, ordered MessageHistory, and composition and ordering relations among turns and messages.", "Conversation、ConversationTurn、順序付き MessageHistory、および turn と message の構成・順序関係。"),
      localized("系统、开发者、用户、助手、工具结果、工具观测和外部智能体消息，以及协议或会话包络。", "System, developer, user, assistant, tool-result, tool-observation, and external-agent messages, plus protocol and conversation envelopes.", "system、developer、user、assistant、tool-result、tool-observation、external-agent メッセージ、およびプロトコル・会話包絡。"),
    ],
    excludes: [
      localized("PromptTemplate、PromptTemplateInstance、Instruction 及其权威和解决活动归提示与指令；Message 只承载它们。", "PromptTemplate, PromptTemplateInstance, Instruction, authority, and resolution activities belong to prompts and instructions; Message only carries them.", "PromptTemplate、PromptTemplateInstance、Instruction、その authority と resolution activity は prompt/instruction に属し、Message は運ぶだけです。"),
      localized("ContextPackage 和可见上下文版本归上下文组装；会话消息仅通过引用使用它们。", "ContextPackage and visible context versions belong to context assembly; conversation messages use them only by reference.", "ContextPackage と可視コンテキスト版はコンテキスト組み立てに属し、会話メッセージは参照で利用するだけです。"),
    ],
    owns_when: localized("当对象的身份由会话、轮次位置、发送主体、消息角色或包络消息 id 决定时，在此拥有。", "Own an object when its identity is determined by conversation, turn position, sender, message role, or envelope message ID.", "対象の同一性が会話、ターン位置、送信主体、メッセージ役割、包絡 message id で決まる場合に所有します。"),
    references_when: localized("提示处理、工具交互或运行追踪需要指出某段信息在哪次发言中出现时，引用消息或轮次身份。", "Prompt processing, tool interactions, or runtime tracing references message or turn identity when locating where information appeared in dialogue.", "プロンプト処理、ツール相互作用、実行トレースが情報の発話位置を示すとき、メッセージまたはターン同一性を参照します。"),
    competency_questions: [
      {
        semantic_key: "info-messages-instructions.conversation-order",
        related_module_ids: ["runtime-system"],
        questions: localized("一个会话的轮次和消息按什么顺序排列，是否存在重复位置或断裂前驱？", "In what order are a conversation's turns and messages arranged, and are there duplicate positions or broken predecessors?", "会話のターンとメッセージはどの順序で並び、位置重複または前駆関係の断裂がありますか。"),
        query: "Traverse Conversation has_turn and Turn contains_message composition, validating position values and predecessor continuity.",
        expected_assertion: "Each turn and message has one unambiguous position; conversation, turn, and message remain composition levels rather than an is_a chain.",
      },
      {
        semantic_key: "info-messages-instructions.message-role-envelope",
        related_module_ids: ["tool-mcp-transport"],
        questions: localized("某条消息由谁发送、采用什么角色，并位于哪个协议或会话包络中？", "Who sent a message, which role does it carry, and in which protocol or conversation envelope is it located?", "あるメッセージは誰が送信し、どの役割を持ち、どのプロトコルまたは会話包絡に位置しますか。"),
        query: "Resolve Message sender, role specialization, containing turn, and envelope identifiers without deriving role from content modality.",
        expected_assertion: "The message has one sender and accepted role plus an explicit envelope; content type does not determine sender role.",
      },
      {
        semantic_key: "info-messages-instructions.context-reference",
        related_module_ids: ["memory-context"],
        questions: localized("一条消息引用了哪个不可变 ContextPackage，引用发生在发送前的哪个上下文版本？", "Which immutable ContextPackage does a message reference, and which pre-send context version does that reference identify?", "メッセージはどの不変 ContextPackage を参照し、その参照は送信前のどのコンテキスト版を示しますか。"),
        query: "Follow Message references_context to one immutable context version and compare its production time with message send order.",
        expected_assertion: "The referenced context is owned by context assembly, immutable, and produced no later than the message that cites it.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "info-prompts-instructions",
        decision: "owns",
        owner_module_id: "info-messages-instructions",
        rationale: localized("消息与会话拥有发言包络、角色和顺序；提示与指令拥有用于规范行为的模板、实例、指令权威和解决过程。", "Messages and conversations own utterance envelopes, roles, and order; prompts and instructions own behavior-shaping templates, instances, instruction authority, and resolution processes.", "メッセージと会話は発話包絡、役割、順序を所有し、プロンプトと指示は行動を規定するテンプレート、インスタンス、指示権威、解決過程を所有します。"),
      },
      {
        other_module_id: "runtime-system",
        decision: "owns",
        owner_module_id: "info-messages-instructions",
        rationale: localized("消息与对话拥有 Conversation、Turn、Message 的内容角色与对话顺序；运行系统拥有 AgentSystem、RuntimeSession、Environment 及其开始暂停结束。", "Messages and conversations own Conversation, Turn, and Message roles and dialogue order; runtime system owns AgentSystem, RuntimeSession, Environment, and start, pause, or end lifecycle.", "message/conversation は Conversation、Turn、Message の role と dialogue order を所有し、runtime system は AgentSystem、RuntimeSession、Environment と start/pause/end lifecycle を所有します。"),
      },
      {
        other_module_id: "tool-mcp-transport",
        decision: "owns",
        owner_module_id: "info-messages-instructions",
        rationale: localized("消息与对话拥有协议无关的 Message、Conversation 和内容顺序；MCP 交互拥有实际 Participant、Session、Capability 协商与传输交换。", "Messages and conversations own protocol-neutral Message, Conversation, and content order; MCP interactions own actual Participant, Session, capability negotiation, and transport exchanges.", "message/conversation は protocol-neutral Message、Conversation、content order を所有し、MCP interaction は実 Participant、Session、capability negotiation、transport exchange を所有します。"),
      },
      {
        other_module_id: "memory-context",
        decision: "owns",
        owner_module_id: "info-messages-instructions",
        rationale: localized("消息与对话拥有 Message 及其在 Conversation 中的顺序，并记录对某 ContextPackage 的引用；上下文组装拥有该 Package/Window 的选择、排序、slot 和版本。", "Messages and conversations own Message and its order in Conversation and record a reference to ContextPackage; context assembly owns that Package or Window's selection, ordering, slots, and version.", "message/conversation は Message と Conversation 内順序、および ContextPackage 参照を所有し、context assembly は Package/Window の selection、ordering、slot、version を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "info-prompts-instructions",
        semantic_area: localized("承载指令的消息与指令本身", "Messages carrying instructions versus instructions themselves", "指示を運ぶメッセージと指示自体"),
        overlap_reason: localized("同一文本可以既位于用户消息又表达用户指令，但消息身份来自会话位置，指令身份来自规范作用和权威元数据。", "The same text may occupy a user message and express a user instruction, but message identity comes from dialogue position while instruction identity comes from normative effect and authority metadata.", "同じテキストが user message に位置し user instruction を表せますが、メッセージ同一性は会話位置、指示同一性は規範作用と権威メタデータに由来します。"),
        disambiguation_test: localized("若删除指令优先级和作用域后对象仍可由会话、轮次和发送者识别，则归消息与会话；若身份依赖规范约束，则归提示与指令。", "If removing instruction priority and scope leaves identity by conversation, turn, and sender, it belongs to messages and conversations; if identity depends on normative constraint, it belongs to prompts and instructions.", "指示優先度と作用域を除いても会話、ターン、送信者で識別できればメッセージと会話、同一性が規範制約に依存すればプロンプトと指示に属します。"),
        result: "distinct",
        owner_module_id: "info-messages-instructions",
      },
      {
        other_module_id: "runtime-system",
        semantic_area: localized("对话序列与运行会话生命周期", "Dialogue sequence versus runtime-session lifecycle", "dialogue sequence と runtime session lifecycle"),
        overlap_reason: localized("一个 RuntimeSession 可承载多个对话，一个对话也可在恢复后的会话继续；消息位置不应成为 session 状态。", "One RuntimeSession can carry several conversations and one conversation can continue after session recovery; message position must not become session state.", "一 RuntimeSession は複数 conversation を運べ、一 conversation は session recovery 後も続けられるため、message position は session state ではありません。"),
        disambiguation_test: localized("由 conversation id、turn 和 message position 识别的对象归消息与对话；由 runtime session id、environment 和生命周期事件识别的对象归运行系统。", "An object identified by conversation ID, turn, and message position belongs to messages and conversations; one identified by runtime-session ID, environment, and lifecycle event belongs to runtime system.", "conversation id・turn・message position で識別されれば message/conversation、runtime session id・environment・lifecycle event で識別されれば runtime system に属します。"),
        result: "distinct",
        owner_module_id: "info-messages-instructions",
      },
      {
        other_module_id: "tool-mcp-transport",
        semantic_area: localized("通用消息语义与 MCP 会话交互", "Generic message semantics versus MCP session interaction", "generic message 意味と MCP session interaction"),
        overlap_reason: localized("MCP 载荷可被表示为 Message，但 MCP session 的参与者、协商能力和传输身份不是对话角色或消息内容。", "An MCP payload can be represented as a Message, but MCP-session participants, negotiated capabilities, and transport identity are neither dialogue roles nor message content.", "MCP payload は Message として表現できますが、MCP-session participant・negotiated capability・transport identity は dialogue role や message content ではありません。"),
        disambiguation_test: localized("若事实可脱离 MCP 仍由 conversation/turn/message 识别，归消息与对话；若必须依赖 MCP session、participant 或 transport，归 MCP 交互。", "If a fact remains identifiable by conversation, turn, and message without MCP, it belongs to messages and conversations; if it requires MCP session, participant, or transport, it belongs to MCP interactions.", "MCP なしでも conversation/turn/message で識別されれば message/conversation、MCP session・participant・transport が必要なら MCP interaction に属します。"),
        result: "distinct",
        owner_module_id: "info-messages-instructions",
      },
      {
        other_module_id: "memory-context",
        semantic_area: localized("消息的上下文引用与被引用上下文", "A message's context reference versus the referenced context", "message の context reference と参照 context"),
        overlap_reason: localized("同一 Message 可引用一个不可变上下文版本，但不能因引用而拥有 ContextSlot 或把上下文项目变成会话消息。", "A Message can reference one immutable context version, but the reference neither gives it ownership of ContextSlot nor turns context items into conversation messages.", "Message は immutable context version を参照できますが、その参照により ContextSlot を所有したり context item を conversation message に変えたりしません。"),
        disambiguation_test: localized("由 conversation position 和 sender role 识别的对象归消息与对话；由 context version、budget、slot 和 assembly activity 识别的对象归上下文组装。", "An object identified by conversation position and sender role belongs to messages and conversations; one identified by context version, budget, slot, and assembly activity belongs to context assembly.", "conversation position と sender role で識別されれば message/conversation、context version・budget・slot・assembly activity で識別されれば context assembly に属します。"),
        result: "distinct",
        owner_module_id: "info-messages-instructions",
      },
    ],
  },
  "info-output-disclosure": {
    purpose: localized("以 DisclosureActivity 为关键概念，记录运行结果如何被选择、截断、抑制和发布，以及最终向特定受众显示了哪些片段。", "Use DisclosureActivity as the key notion to record how runtime results are selected, truncated, suppressed, and published and which fragments are ultimately shown to a particular audience.", "DisclosureActivity を中核概念として、runtime result の selection、truncation、suppression、publication と、特定 audience に最終表示された fragment を記録します。"),
    includes: [
      localized("DisclosureActivity 及其 DisclosureSelectionActivity、DisclosureTruncationActivity、DisclosureSuppressionActivity 和 DisclosurePublicationActivity 阶段。", "DisclosureActivity and its DisclosureSelectionActivity, DisclosureTruncationActivity, DisclosureSuppressionActivity, and DisclosurePublicationActivity phases.", "DisclosureActivity と、その DisclosureSelectionActivity、DisclosureTruncationActivity、DisclosureSuppressionActivity、DisclosurePublicationActivity phase。"),
      localized("DisclosureRecord、DisclosureArtifact、OutputWindow、HeadTailWindow、DisclosedOutputSegment、SuppressedContext 和 TruncatedContextSpan。", "DisclosureRecord, DisclosureArtifact, OutputWindow, HeadTailWindow, DisclosedOutputSegment, SuppressedContext, and TruncatedContextSpan.", "DisclosureRecord、DisclosureArtifact、OutputWindow、HeadTailWindow、DisclosedOutputSegment、SuppressedContext、TruncatedContextSpan。"),
    ],
    excludes: [
      localized("敏感信息识别、DisclosureRule、Redaction 与安全抑制归披露与脱敏；这里应用其决定并记录实际显示结果。", "Sensitive-data identification, DisclosureRule, Redaction, and security suppression belong to disclosure and redaction; this scope applies their decisions and records what was actually shown.", "機微情報識別、DisclosureRule、Redaction、安全抑制は開示と墨消しに属し、ここでは判断を適用し実際の表示結果を記録します。"),
      localized("ContextWindow、上下文预算和 ContextPackage 归上下文组装；输出窗口只限制外显片段。", "ContextWindow, context budgets, and ContextPackage belong to context assembly; an output window only limits externally visible fragments.", "ContextWindow、コンテキスト予算、ContextPackage はコンテキスト組み立てに属し、出力窓は外部表示断片だけを制限します。"),
    ],
    owns_when: localized("当对象的身份由一次 DisclosureActivity、其选择/截断/抑制/发布阶段、面向受众的 DisclosureRecord 或实际显示范围决定时，在此拥有。", "Own an object when its identity is determined by one DisclosureActivity, its selection, truncation, suppression, or publication phase, an audience-facing DisclosureRecord, or an actual displayed range.", "対象が一 DisclosureActivity、その selection/truncation/suppression/publication phase、audience-facing DisclosureRecord、actual displayed range で識別される場合に所有します。"),
    references_when: localized("安全策略、运行结果或上下文组装需要核对最终显示范围而不重新定义披露活动时，引用这里的决定和记录。", "Safety policy, runtime results, or context assembly references decisions and records here when checking the final displayed range without redefining disclosure activity.", "安全方針、実行結果、コンテキスト組み立てが開示活動を再定義せず最終表示範囲を確認するとき、ここで定義した判断と記録を参照します。"),
    competency_questions: [
      {
        semantic_key: "info-output-disclosure.visible-fragments",
        related_module_ids: ["runtime-artifacts"],
        questions: localized("某个运行结果向指定受众实际显示了哪些片段，哪些被截断或抑制？", "Which fragments of a runtime result were actually shown to a specified audience, and which were truncated or suppressed?", "ある実行結果のどの断片が指定受け手へ実際に表示され、どれが切詰めまたは抑制されましたか。"),
        query: "For one DisclosureRecord and audience, return source fragment ranges with display, truncate, or suppress outcomes in source order.",
        expected_assertion: "Displayed and withheld ranges form a non-overlapping accounting of the considered output, each linked to its decision.",
      },
      {
        semantic_key: "info-output-disclosure.window-application",
        related_module_ids: ["memory-context"],
        questions: localized("首尾窗口或长度窗口如何应用到内容片段，实际边界与不可变上下文版本有何关系？", "How is a head/tail or length window applied to content fragments, and how do actual boundaries relate to the immutable context version?", "先頭・末尾窓または長さ窓は内容断片へどう適用され、実際の境界は不変コンテキスト版とどう関係しますか。"),
        query: "Apply OutputWindow to referenced source ranges and compare the result with DisclosureRecord boundaries and cited ContextPackage version.",
        expected_assertion: "OutputWindow determines visible source ranges only; it neither owns nor mutates ContextWindow or ContextPackage.",
      },
      {
        semantic_key: "info-output-disclosure.policy-to-display",
        related_module_ids: ["safety-disclosure-redaction"],
        questions: localized("一项安全披露决定如何改变最终显示，安全变换和实际披露是否都有独立证据？", "How does a safety-disclosure decision change final display, and are the security transformation and actual disclosure independently evidenced?", "安全開示判断は最終表示をどう変え、安全変換と実際の開示はそれぞれ独立した証拠を持ちますか。"),
        query: "Join the applicable PolicyDecision to redaction or suppression evidence, then to the audience-facing DisclosureRecord and displayed ranges.",
        expected_assertion: "The safety scope owns policy and transformation; this scope owns the resulting audience-facing record, with an explicit link between them.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "safety-disclosure-redaction",
        decision: "owns",
        owner_module_id: "info-output-disclosure",
        rationale: localized("输出披露拥有实际面向受众的选择、窗口和显示记录；披露与脱敏拥有敏感识别、允许规则、遮蔽变换和安全决定。", "Output disclosure owns audience-facing selection, windows, and display records; disclosure and redaction own sensitivity detection, allow rules, masking transformations, and security decisions.", "出力開示は受け手向け選択、窓、表示記録を所有し、開示と墨消しは機微識別、許可規則、遮蔽変換、安全判断を所有します。"),
      },
      {
        other_module_id: "runtime-artifacts",
        decision: "owns",
        owner_module_id: "info-output-disclosure",
        rationale: localized("输出披露拥有 DisclosureActivity、可见窗口、披露/抑制/截断决定和 DisclosureRecord；运行产物拥有可独立版本化、存储和评审的报告、图或导出物。", "Output disclosure owns DisclosureActivity, visible window, disclosure, suppression, or truncation decisions, and DisclosureRecord; runtime artifacts own reports, graphs, or exports independently versioned, stored, and reviewed.", "output disclosure は DisclosureActivity、visible window、disclosure/suppression/truncation decision、DisclosureRecord を所有し、runtime artifact は独立 versioning・storage・review される report、graph、export を所有します。"),
      },
      {
        other_module_id: "memory-context",
        decision: "owns",
        owner_module_id: "info-output-disclosure",
        rationale: localized("输出披露拥有面向受众的输出选择、截断、抑制、发布和审计记录；上下文组装拥有模型调用前的 ContextPackage、Window、Slot 和输入预算。", "Output disclosure owns audience-facing output selection, truncation, suppression, publication, and audit records; context assembly owns pre-invocation ContextPackage, Window, Slot, and input budget.", "output disclosure は audience-facing output selection、truncation、suppression、publication、audit record を所有し、context assembly は invocation 前 ContextPackage、Window、Slot、input budget を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "safety-disclosure-redaction",
        semantic_area: localized("实际显示抑制与安全披露抑制", "Display suppression versus security disclosure suppression", "実際の表示抑制と安全開示抑制"),
        overlap_reason: localized("两者都可能产生 suppressed 结果，但显示面回答最终看到了什么，安全面回答依据何种敏感规则做了什么变换。", "Both may yield a suppressed outcome, but the display side answers what was ultimately seen while the security side answers which sensitive rule caused which transformation.", "どちらも suppressed 結果を生み得ますが、表示側は最終的に何が見えたか、安全側はどの機微規則でどの変換を行ったかを答えます。"),
        disambiguation_test: localized("若对象即使没有安全原因也可由受众、范围和显示结果识别，则归输出披露；若身份依赖敏感分类、规则或脱敏算法，则归披露与脱敏。", "If the object is identifiable by audience, range, and display outcome even without a security reason, it belongs to output disclosure; if identity depends on sensitivity class, rule, or redaction algorithm, it belongs to disclosure and redaction.", "安全理由がなくても受け手、範囲、表示結果で識別できれば出力開示、同一性が機微分類、規則、墨消しアルゴリズムに依存すれば開示と墨消しに属します。"),
        result: "distinct",
        owner_module_id: "info-output-disclosure",
      },
      {
        other_module_id: "runtime-artifacts",
        semantic_area: localized("披露视图记录与持久运行产物", "Disclosure-view records versus persistent runtime artifacts", "disclosure view record と persistent runtime artifact"),
        overlap_reason: localized("Artifact 可成为披露输入或输出内容，但针对受众的可见片段与抑制理由是一次披露活动的事实，不创建新的 Artifact 子类。", "An Artifact can be disclosure input or output content, but audience-specific visible segments and suppression reasons are facts of a disclosure activity, not new Artifact subtypes.", "Artifact は disclosure input/output content になれますが、audience-specific visible segment と suppression reason は disclosure activity の事実で、新 Artifact subtype ではありません。"),
        disambiguation_test: localized("由 audience、window、selection 和 disclosure outcome 识别的对象归输出披露；由 artifact id、digest、version 和 storage lineage 识别的对象归运行产物。", "An object identified by audience, window, selection, and disclosure outcome belongs to output disclosure; one identified by artifact ID, digest, version, and storage lineage belongs to runtime artifacts.", "audience・window・selection・disclosure outcome で識別されれば output disclosure、artifact id・digest・version・storage lineage で識別されれば runtime artifact に属します。"),
        result: "distinct",
        owner_module_id: "info-output-disclosure",
      },
      {
        other_module_id: "memory-context",
        semantic_area: localized("输入上下文选择与输出披露选择", "Input-context selection versus output-disclosure selection", "input context selection と output disclosure selection"),
        overlap_reason: localized("两者都可能裁剪内容，但前者控制提供给运行的输入，后者控制允许向某受众显示的输出，时间点与政策依据不同。", "Both may trim content, but the former controls input supplied to execution while the latter controls output visible to an audience; timing and policy basis differ.", "どちらも content を trim しますが、前者は execution input、後者は audience-visible output を制御し、時点と policy basis が異なります。"),
        disambiguation_test: localized("若决定发生在模型或工具执行前并形成输入 package，归上下文组装；若决定发生在结果产生后并绑定 audience，归输出披露。", "If the decision occurs before model or tool execution and forms an input package, it belongs to context assembly; if it occurs after result production and binds an audience, it belongs to output disclosure.", "model/tool execution 前に input package を作る decision は context assembly、result 生成後に audience を束縛する decision は output disclosure に属します。"),
        result: "distinct",
        owner_module_id: "info-output-disclosure",
      },
    ],
  },
  "info-prompts-instructions": {
    purpose: localized("区分提示规范、实例化提示和具有权威顺序的指令，并记录解析、冲突检测与解决结果。", "Distinguish prompt specifications, instantiated prompts, and authority-ordered instructions, recording parsing, conflict detection, and resolution results.", "プロンプト仕様、インスタンス化プロンプト、権威順序を持つ指示を区別し、解析、競合検出、解決結果を記録します。"),
    includes: [
      localized("PromptTemplate、PromptTemplateInstance、FewShotExample 及其 instantiates 与组成关系。", "PromptTemplate, PromptTemplateInstance, FewShotExample, and their instantiates and composition relations.", "PromptTemplate、PromptTemplateInstance、FewShotExample、および instantiates/composition relation。"),
      localized("系统、开发者、用户和覆盖指令，指令权威、优先级、作用域、来源，以及解析和解决活动。", "System, developer, user, and override instructions; instruction authority, priority, scope, source; and parsing and resolution activities.", "system、developer、user、override 指示、指示権威、優先度、作用域、出典、解析・解決活動。"),
    ],
    excludes: [
      localized("Conversation、Turn、Message 与发送者角色归消息与会话；消息通过 carries 关系承载指令。", "Conversation, Turn, Message, and sender roles belong to messages and conversations; messages carry instructions through an explicit relation.", "Conversation、Turn、Message、送信者役割はメッセージと会話に属し、メッセージは明示的関係で指示を運びます。"),
      localized("InstructionConflict 的威胁含义和安全处置唯一归注入与内容投毒防御；这里引用冲突并记录解析结果。", "The threat meaning and security handling of InstructionConflict belong exclusively to injection and content-poisoning defense; this scope references conflicts and records resolution results.", "InstructionConflict の脅威意味と安全処置は注入とコンテンツ汚染防御だけが所有し、ここでは競合を参照して解析結果を記録します。"),
    ],
    owns_when: localized("当对象的身份由提示模板或实例化关系，或由指令的规范作用、权威、优先级与作用域决定时，在此拥有。", "Own an object when its identity is determined by a prompt template or instantiation relation, or by an instruction's normative effect, authority, priority, and scope.", "対象の同一性がプロンプトテンプレートやインスタンス化関係、または指示の規範作用、権威、優先度、作用域で決まる場合に所有します。"),
    references_when: localized("消息、运行或安全分析需要说明采用了哪个提示实例或哪条已解析指令时，引用这里的规范对象和解决结果。", "Messages, runtime, or security analysis references specification objects and resolution results here when identifying a prompt instance or resolved instruction in use.", "メッセージ、実行、安全分析が使用したプロンプトインスタンスまたは解決済み指示を示すとき、ここで定義した仕様対象と解決結果を参照します。"),
    competency_questions: [
      {
        semantic_key: "info-prompts-instructions.template-instantiation",
        related_module_ids: ["info-content-block-modality"],
        questions: localized("某个 PromptTemplateInstance 实例化了哪个模板，变量绑定和内容块是否满足模板约束？", "Which template does a PromptTemplateInstance instantiate, and do its variable bindings and content blocks satisfy template constraints?", "PromptTemplateInstance はどの template を instantiate し、variable binding と content block は template constraint を満たしますか。"),
        query: "Resolve PromptTemplateInstance instantiates PromptTemplate, then validate required bindings, declared types, and ordered content-block composition.",
        expected_assertion: "The instance points to exactly one compatible template and satisfies every required binding; template and instance retain different semantic kinds.",
      },
      {
        semantic_key: "info-prompts-instructions.authority-resolution",
        related_module_ids: ["safety-injection-defense"],
        questions: localized("相互冲突的系统、开发者、用户和覆盖指令按何种权威、作用域与优先级得到最终解析？", "How are conflicting system, developer, user, and override instructions resolved by authority, scope, and priority?", "競合する system、developer、user、override 指示は、権威、作用域、優先度によりどのように最終解決されますか。"),
        query: "Order applicable Instruction values by authority policy, scope containment, and priority; reference any safety-owned InstructionConflict.",
        expected_assertion: "The resolution cites all applicable instructions, returns one governed outcome or explicit unresolved state, and does not redefine the security conflict.",
      },
      {
        semantic_key: "info-prompts-instructions.message-carriage",
        related_module_ids: ["info-messages-instructions"],
        questions: localized("一条消息承载了哪些指令或提示内容，哪些普通内容块不产生规范约束？", "Which instructions or prompt content does a message carry, and which ordinary content blocks create no normative constraint?", "メッセージはどの指示またはプロンプト内容を運び、どの通常内容ブロックは規範制約を生みませんか。"),
        query: "From one Message, traverse carries_instruction and contains content separately, then test instruction identity conditions.",
        expected_assertion: "Only explicitly identified normative content is returned as Instruction; ordinary message content remains ContentBlock even when text is imperative.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "info-messages-instructions",
        decision: "owns",
        owner_module_id: "info-prompts-instructions",
        rationale: localized("提示与指令拥有模板、实例化、规范作用与权威解决；消息与会话拥有承载这些内容的发言位置和包络。", "Prompts and instructions own templates, instantiation, normative effect, and authority resolution; messages and conversations own the utterance positions and envelopes carrying that content.", "プロンプトと指示はテンプレート、インスタンス化、規範作用、権威解決を所有し、メッセージと会話はそれらを運ぶ発話位置と包絡を所有します。"),
      },
      {
        other_module_id: "info-content-block-modality",
        decision: "owns",
        owner_module_id: "info-prompts-instructions",
        rationale: localized("提示与指令拥有 PromptTemplate、实例化、Instruction、authority、解析和冲突解决；内容表示拥有承载这些语义的 TextBlock、CodeBlock 等形式。", "Prompts and instructions own PromptTemplate, instantiation, Instruction, authority, parsing, and conflict resolution; content representation owns carrier forms such as TextBlock and CodeBlock.", "prompt/instruction は PromptTemplate、instantiation、Instruction、authority、parsing、conflict resolution を所有し、content representation は TextBlock、CodeBlock 等の carrier form を所有します。"),
      },
      {
        other_module_id: "safety-injection-defense",
        decision: "owns",
        owner_module_id: "info-prompts-instructions",
        rationale: localized("提示与指令拥有正常 Instruction 的解析、authority 比较和非恶意冲突解决；注入与污染防御拥有 ThreatSignal、Taint、恶意冲突判断和缓解。", "Prompts and instructions own parsing, authority comparison, and nonmalicious conflict resolution for normal Instruction; injection and poisoning defense owns ThreatSignal, Taint, malicious-conflict judgment, and mitigation.", "prompt/instruction は正常 Instruction の parsing、authority comparison、非悪意 conflict resolution を所有し、injection/poisoning defense は ThreatSignal、Taint、malicious conflict 判断、mitigation を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "info-messages-instructions",
        semantic_area: localized("提示实例与消息文本", "Prompt instances versus message text", "プロンプトインスタンスとメッセージテキスト"),
        overlap_reason: localized("同一文本可能从模板实例化后放入消息，但实例化身份依赖模板和绑定，消息身份依赖会话和发送者。", "The same text may be instantiated from a template and placed in a message, but instantiation identity depends on template and bindings while message identity depends on conversation and sender.", "同じテキストがテンプレートからインスタンス化されメッセージへ置かれ得ますが、インスタンス化の同一性はテンプレートとバインディング、メッセージの同一性は会話と送信者に依存します。"),
        disambiguation_test: localized("若替换会话位置仍保持同一模板绑定和规范作用，则归提示与指令；若替换发送者或轮次即改变身份，则归消息与会话。", "If changing dialogue position preserves template bindings and normative effect, it belongs to prompts and instructions; if changing sender or turn changes identity, it belongs to messages and conversations.", "会話位置を変えてもテンプレートバインディングと規範作用が保たれるならプロンプトと指示、送信者またはターンの変更で同一性が変わるならメッセージと会話に属します。"),
        result: "distinct",
        owner_module_id: "info-prompts-instructions",
      },
      {
        other_module_id: "info-content-block-modality",
        semantic_area: localized("指令语义与内容载体", "Instruction semantics versus content carrier", "instruction 意味と content carrier"),
        overlap_reason: localized("同一文本块可在不同场景成为普通内容或 Instruction，authority 和 applicability 不由 MIME 或 block 类型决定。", "The same text block can be ordinary content or an Instruction in different contexts; authority and applicability are not determined by MIME or block type.", "同じ text block は文脈により普通 content または Instruction となり、authority/applicability は MIME や block type では決まりません。"),
        disambiguation_test: localized("由媒体结构和编码识别的对象归内容表示；由 template identity、instruction source、authority 和 scope 识别的对象归提示与指令。", "An object identified by media structure and encoding belongs to content representation; one identified by template identity, instruction source, authority, and scope belongs to prompts and instructions.", "media structure・encoding で識別されれば content representation、template identity・instruction source・authority・scope で識別されれば prompt/instruction に属します。"),
        result: "distinct",
        owner_module_id: "info-prompts-instructions",
      },
      {
        other_module_id: "safety-injection-defense",
        semantic_area: localized("普通指令冲突与安全注入威胁", "Ordinary instruction conflict versus injection threat", "通常 instruction conflict と injection threat"),
        overlap_reason: localized("权限不同的合法指令可能冲突而没有攻击；只有带不可信来源、越权意图或污染证据的冲突进入安全防御。", "Legitimate instructions of different authority can conflict without attack; only conflicts with untrusted source, privilege-escalation intent, or poisoning evidence enter security defense.", "authority の異なる正当 instruction は攻撃なしでも conflict し得て、untrusted source・privilege escalation intent・poisoning evidence がある場合だけ security defense に入ります。"),
        disambiguation_test: localized("仅靠 authority、priority 和 applicability 可解决的对象归提示与指令；需要 threat classification、taint flow 或 mitigation 的对象归注入与污染防御。", "An object resolvable by authority, priority, and applicability alone belongs to prompts and instructions; one requiring threat classification, taint flow, or mitigation belongs to injection and poisoning defense.", "authority・priority・applicability だけで解決できれば prompt/instruction、threat classification・taint flow・mitigation が必要なら injection/poisoning defense に属します。"),
        result: "distinct",
        owner_module_id: "info-prompts-instructions",
      },
    ],
  },
  "info-storage-sources": {
    purpose: localized("用稳定引用、精确位置和版本身份定位信息来源，使任何引用都能回到原对象和原范围。", "Locate information sources through stable references, precise positions, and version identity so every citation can return to its original object and range.", "安定参照、精密位置、版同一性で情報源を位置付け、すべての引用を元の対象と範囲へ戻せるようにします。"),
    includes: [
      localized("文件、数据库行、图节点、网络资源和文本文档引用，以及引用锚和内容锚。", "File, database-row, graph-node, web-resource, and text-document references, plus citation and content anchors.", "ファイル、データベース行、グラフノード、Web リソース、テキスト文書への参照、および引用アンカー・内容アンカー。"),
      localized("行、页、单元格、字节偏移和文本跨度位置，以及版本、摘要、校验和与来源身份。", "Line, page, cell, byte-offset, and text-span locations, plus versions, digests, checksums, and source identity.", "行、ページ、セル、バイトオフセット、テキスト範囲の位置、および版、ダイジェスト、チェックサム、出典同一性。"),
    ],
    excludes: [
      localized("被引用资源实体本身归其业务 owner；SourceReference 不是 referent 的替代副本。", "The referenced resource itself belongs to its business owner; SourceReference is not a replacement copy of the referent.", "参照先リソース自体は業務上の所有者に属し、SourceReference は参照対象の代替コピーではありません。"),
      localized("文档实体的接入、去重和持久化归记忆接入；这里拥有 TextDocumentReference 而非摄入后的文档对象。", "Document ingestion, deduplication, and persistence belong to memory ingestion; this scope owns TextDocumentReference rather than the ingested document object.", "文書実体の取込、重複排除、永続化は記憶取込に属し、ここでは取込後の文書ではなく TextDocumentReference を所有します。"),
    ],
    owns_when: localized("当对象的身份是指向其他对象的稳定引用、该引用中的位置范围或用于核对版本的来源身份时，在此拥有。", "Own an object when its identity is a stable reference to another object, a location range within that reference, or source identity used to verify a version.", "対象の同一性が他対象への安定参照、その参照内の位置範囲、または版照合に用いる出典同一性である場合に所有します。"),
    references_when: localized("接入、分块、检索、消息或产物需要说明来源而不取得 referent 所有权时，引用这里的 SourceReference 和 Location。", "Ingestion, chunking, retrieval, messages, or artifacts reference SourceReference and Location here when stating provenance without taking ownership of the referent.", "取込、チャンク化、検索、メッセージ、成果物が参照対象の所有権を得ず出典を示すとき、ここで定義した SourceReference と Location を参照します。"),
    competency_questions: [
      {
        semantic_key: "info-storage-sources.precise-resolution",
        related_module_ids: ["memory-ingestion"],
        questions: localized("一个来源引用能否解析到唯一对象版本和精确行、页、单元格或文本跨度？", "Can a source reference resolve to one object version and an exact line, page, cell, or text span?", "出典参照は一意の対象版と正確な行、ページ、セル、テキスト範囲へ解決できますか。"),
        query: "Resolve SourceReference identity and SourceLocation selector against the declared source version, reporting zero, one, or multiple matches.",
        expected_assertion: "A valid precise reference resolves to exactly one versioned object range; ambiguity and stale selectors are explicit failures.",
      },
      {
        semantic_key: "info-storage-sources.identity-verification",
        related_module_ids: ["runtime-artifacts"],
        questions: localized("引用记录的版本、摘要和校验和是否与当前取回对象一致，若不一致是哪一项漂移？", "Do the referenced version, digest, and checksum match the retrieved object, and which identity dimension drifted if not?", "参照記録の版、ダイジェスト、チェックサムは取得対象と一致し、不一致ならどの同一性次元が変化しましたか。"),
        query: "Compare declared SourceIdentity fields with the retrieved referent using the stated digest and checksum algorithms.",
        expected_assertion: "Verification reports each identity dimension independently and never updates the stored reference silently.",
      },
      {
        semantic_key: "info-storage-sources.reference-versus-referent",
        related_module_ids: ["memory-ingestion"],
        questions: localized("查询结果中的对象是来源引用、位置值，还是被引用的实际资源，canonical owner 分别是谁？", "Is a query result a source reference, a location value, or the actual referenced resource, and who is the canonical owner of each?", "照会結果は出典参照、位置値、実際の参照資源のどれで、それぞれの canonical owner は誰ですか。"),
        query: "Inspect semantic kind and outgoing refers_to or has_location relations, then resolve owner without copying referent fields into the reference.",
        expected_assertion: "References and locations are owned here; the referent resolves to its business owner through an explicit relation.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "memory-ingestion",
        decision: "owns",
        owner_module_id: "info-storage-sources",
        rationale: localized("来源与定位拥有稳定引用、位置和版本核验身份；记忆接入拥有被载入的文档或记录、接入运行、去重与规范化。", "Sources and locations own stable references, positions, and version-verification identity; memory ingestion owns loaded documents or records, ingestion runs, deduplication, and normalization.", "出典と位置は安定参照、位置、版検証同一性を所有し、記憶取込は読み込まれた文書・記録、取込実行、重複排除、正規化を所有します。"),
      },
      {
        other_module_id: "runtime-artifacts",
        decision: "owns",
        owner_module_id: "info-storage-sources",
        rationale: localized("存储与来源拥有 SourceIdentity、Location、Version、Span、Digest 和可访问路径；运行产物拥有由执行生成的 Artifact 本体及其产物类型。", "Storage and sources own SourceIdentity, Location, Version, Span, Digest, and access path; runtime artifacts own the Artifact produced by execution and its artifact type.", "storage/source は SourceIdentity、Location、Version、Span、Digest、access path を所有し、runtime artifact は execution が生成した Artifact 本体と artifact type を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "memory-ingestion",
        semantic_area: localized("文档引用与已接入文档", "Document references versus ingested documents", "文書参照と取込済み文書"),
        overlap_reason: localized("两者都可带 URI、摘要和版本，但引用用于定位外部对象，接入文档是具有摄入生命周期和持久身份的内容实体。", "Both may carry URI, digest, and version, but a reference locates an external object while an ingested document is a content entity with ingestion lifecycle and persistent identity.", "どちらも URI、ダイジェスト、版を持ち得ますが、参照は外部対象を位置付け、取込文書は取込ライフサイクルと永続同一性を持つ内容実体です。"),
        disambiguation_test: localized("若不读取或保存内容也能完整表达对象，则归来源引用；若对象参与 Loader、去重、规范化或持久化，则归记忆接入。", "If the object is complete without reading or storing content, it belongs to source references; if it participates in loading, deduplication, normalization, or persistence, it belongs to memory ingestion.", "内容を読込・保存せず完全に表せるなら出典参照、Loader、重複排除、正規化、永続化に参加するなら記憶取込に属します。"),
        result: "distinct",
        owner_module_id: "info-storage-sources",
      },
      {
        other_module_id: "runtime-artifacts",
        semantic_area: localized("资源定位与生成产物身份", "Resource location versus generated-artifact identity", "resource location と generated artifact identity"),
        overlap_reason: localized("Artifact 可通过 SourceReference 定位并具有 digest，但引用描述在哪里找到内容，不决定它为何是 Patch、Report 或 Graph。", "An Artifact can be located through SourceReference and have a digest, but a reference says where content is found, not why it is a Patch, Report, or Graph.", "Artifact は SourceReference で locate され digest を持てますが、reference は content の所在を示すだけで Patch/Report/Graph の種別理由を決めません。"),
        disambiguation_test: localized("由 URI、range、version 或 checksum 识别的对象归存储与来源；由 producing execution、artifact id 和产物语义识别的对象归运行产物。", "An object identified by URI, range, version, or checksum belongs to storage and sources; one identified by producing execution, artifact ID, and artifact semantics belongs to runtime artifacts.", "URI・range・version・checksum で識別されれば storage/source、producing execution・artifact id・artifact semantics で識別されれば runtime artifact に属します。"),
        result: "distinct",
        owner_module_id: "info-storage-sources",
      },
    ],
  },
});
