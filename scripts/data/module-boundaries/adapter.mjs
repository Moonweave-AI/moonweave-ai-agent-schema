import { deepFreeze } from "./deep-freeze.mjs";

const localized = (zh, en, ja) => ({ zh, en, ja });

export const ADAPTER_MODULE_BOUNDARIES = deepFreeze({
  "adapter-benchmarks": {
    purpose: localized("把基准任务、环境、轨迹、指标和结果对应到核心本体，使跨基准比较使用同一语义口径。", "Map benchmark tasks, environments, trajectories, metrics, and outcomes to the core ontology so comparisons across benchmarks use one semantic basis.", "ベンチマークのタスク、環境、軌跡、指標、結果をコア本体へ対応付け、異なるベンチマークを同じ意味基準で比較できるようにします。"),
    includes: [
      localized("基准套件、任务和环境构造到核心 Goal、RuntimeEnvironment 与 Execution 的版本化映射。", "Versioned mappings from benchmark suites, tasks, and environments to core Goal, RuntimeEnvironment, and Execution concepts.", "ベンチマークスイート、タスク、環境構造からコア Goal、RuntimeEnvironment、Execution への版付き写像。"),
      localized("基准轨迹、得分字段和结果标签到 Trace、Metric、Measurement 与 Outcome 的对应及损失说明。", "Correspondences from benchmark trajectories, score fields, and result labels to Trace, Metric, Measurement, and Outcome, including loss notes.", "ベンチマーク軌跡、得点項目、結果ラベルから Trace、Metric、Measurement、Outcome への対応と損失説明。"),
    ],
    excludes: [
      localized("指标定义、测量活动和评估运行的核心语义归评估与度量；这里不重复定义 Metric。", "Core semantics for metric definitions, measurement activities, and evaluation runs belong to evaluation and measurement; Metric is not redefined here.", "指標定義、測定活動、評価実行のコア意味は評価と測定に属し、Metric を再定義しません。"),
      localized("真实运行轨迹和执行结果归运行域；这里只保留基准字段如何对应它们。", "Actual runtime traces and execution outcomes belong to runtime scopes; only the benchmark-field correspondences are retained here.", "実際の実行トレースと結果は実行領域に属し、ここではベンチマーク項目との対応だけを保持します。"),
    ],
    owns_when: localized("当对象是某个命名基准及版本中的构造，且其意义来自该基准的任务或评分规范时，在此拥有。", "Own an object when it is a construct in a named benchmark version and derives its meaning from that benchmark's task or scoring specification.", "対象が名前付きベンチマーク版の構造で、その意味がタスクまたは採点仕様に由来する場合に所有します。"),
    references_when: localized("评估运行载入基准任务或把基准得分解释为核心测量时，引用对应关系，评估事实仍归评估与度量。", "An evaluation run references these correspondences when loading a benchmark task or interpreting a benchmark score as a core measurement; the evaluation fact remains owned by evaluation and measurement.", "評価実行がベンチマークタスクを読み込む、または得点をコア測定として解釈するときに参照し、評価事実は評価と測定が所有します。"),
    competency_questions: [
      {
        semantic_key: "adapter-benchmarks.task-environment-mapping",
        related_module_ids: ["runtime-system"],
        questions: localized("指定基准版本的任务和环境分别对应哪些核心目标、约束与运行环境？", "Which core goals, constraints, and runtime environments correspond to the task and environment of a specified benchmark version?", "指定ベンチマーク版のタスクと環境は、それぞれどのコア目標、制約、実行環境に対応しますか。"),
        query: "Resolve the benchmark version, then return task-to-goal, task-to-constraint, and environment-to-runtime mappings with coverage status.",
        expected_assertion: "The response distinguishes task semantics from environment semantics and marks every required source construct as mapped or missing.",
      },
      {
        semantic_key: "adapter-benchmarks.score-interpretation",
        related_module_ids: ["feedback-metrics-evaluation"],
        questions: localized("一个基准得分应解释为哪项 Metric 的哪次 Measurement，量纲和方向是否兼容？", "As which Metric and Measurement should a benchmark score be interpreted, and are its scale and direction compatible?", "ベンチマーク得点はどの Metric のどの Measurement として解釈され、尺度と方向は互換ですか。"),
        query: "Map the benchmark score field to a Metric definition, then compare unit, range, aggregation, and better-direction constraints.",
        expected_assertion: "A compatible mapping identifies one metric and measurement semantics; incompatible scale or direction produces an explicit conversion warning.",
      },
      {
        semantic_key: "adapter-benchmarks.trajectory-provenance",
        related_module_ids: ["runtime-observability"],
        questions: localized("基准轨迹映射为 Trace 后，能否保留原始样本、步骤顺序和结果的来源链？", "After a benchmark trajectory maps to Trace, are the original sample, step order, and outcome provenance preserved?", "ベンチマーク軌跡を Trace に写像した後、元サンプル、手順順序、結果の来歴を保持できますか。"),
        query: "Follow the trajectory mapping from benchmark sample through ordered steps to Trace and Outcome provenance references.",
        expected_assertion: "Every mapped trace retains one source sample reference, an order-preserving step mapping, and a provenance link to the benchmark outcome.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "feedback-metrics-evaluation",
        decision: "owns",
        owner_module_id: "adapter-benchmarks",
        rationale: localized("基准映射拥有外部评分字段到核心度量的对应；评估与度量拥有 Metric、Measurement 和 EvaluationRun 本身。", "Benchmark mappings own correspondences from external scoring fields to core measures; evaluation and measurement own Metric, Measurement, and EvaluationRun themselves.", "ベンチマーク写像は外部採点項目からコア測定への対応を所有し、評価と測定は Metric、Measurement、EvaluationRun 自体を所有します。"),
      },
      {
        other_module_id: "runtime-system",
        decision: "owns",
        owner_module_id: "adapter-benchmarks",
        rationale: localized("基准映射拥有 benchmark task/environment 字段到核心 Goal、Environment 和 Execution 的对应；运行系统拥有实际会话所处环境与生命周期。", "Benchmark mappings own correspondences from benchmark task and environment fields to core Goal, Environment, and Execution; runtime system owns the actual session environment and lifecycle.", "benchmark 写像は task/environment field からコア Goal、Environment、Execution への対応を所有し、runtime system は実 session の環境と lifecycle を所有します。"),
      },
      {
        other_module_id: "runtime-observability",
        decision: "owns",
        owner_module_id: "adapter-benchmarks",
        rationale: localized("基准映射拥有 trajectory/sample 字段到 Trace 语义的对应与保真报告；运行可观测性拥有实际 Trace、Span、Event 的因果和时间事实。", "Benchmark mappings own correspondences and fidelity reports from trajectory or sample fields to Trace semantics; runtime observability owns actual Trace, Span, and Event causal and temporal facts.", "benchmark 写像は trajectory/sample field から Trace 意味への対応と忠実度報告を所有し、runtime observability は実 Trace、Span、Event の因果・時間事実を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "feedback-metrics-evaluation",
        semantic_area: localized("基准得分字段与核心测量", "Benchmark score fields versus core measurements", "ベンチマーク得点項目とコア測定"),
        overlap_reason: localized("两者都出现名称、数值和量纲，但基准字段依赖套件版本，核心测量依赖评估运行和指标定义。", "Both carry names, values, and scales, but a benchmark field depends on a suite version while a core measurement depends on an evaluation run and metric definition.", "どちらも名称、値、尺度を持ちますが、ベンチマーク項目はスイート版に依存し、コア測定は評価実行と指標定義に依存します。"),
        disambiguation_test: localized("若离开基准规范后对象仍可由 metric、run、value、unit 唯一识别，则归核心测量；若字段含义依赖基准版本，则归基准映射。", "If the object remains identifiable by metric, run, value, and unit outside the benchmark specification, it is a core measurement; if meaning depends on benchmark version, it is a benchmark mapping.", "ベンチマーク仕様を離れても metric、run、value、unit で識別できればコア測定、意味が版に依存すればベンチマーク写像に属します。"),
        result: "distinct",
        owner_module_id: "adapter-benchmarks",
      },
      {
        other_module_id: "runtime-system",
        semantic_area: localized("基准环境描述与运行环境实例", "Benchmark environment description versus runtime environment instance", "benchmark 環境記述と runtime 環境 instance"),
        overlap_reason: localized("同一 benchmark 环境可执行多次，而每次 RuntimeSession 有自己的进程、工作目录和时间边界。", "One benchmark environment can be executed many times, while each RuntimeSession has its own process, working directory, and temporal boundary.", "同じ benchmark 環境は複数回実行でき、各 RuntimeSession は固有 process、working directory、時間境界を持ちます。"),
        disambiguation_test: localized("由 benchmark suite 与版本识别的环境语义归基准映射；由 session id 和实际绑定识别的环境归运行系统。", "Environment semantics identified by benchmark suite and version belong to benchmark mappings; an environment identified by session ID and actual binding belongs to runtime system.", "benchmark suite と版で識別される環境意味は benchmark 写像、session id と実 binding で識別される環境は runtime system に属します。"),
        result: "distinct",
        owner_module_id: "adapter-benchmarks",
      },
      {
        other_module_id: "runtime-observability",
        semantic_area: localized("基准轨迹映射与原生运行追踪", "Benchmark-trajectory mapping versus native runtime tracing", "benchmark trajectory 写像と native runtime trace"),
        overlap_reason: localized("导入后的轨迹可生成可观测记录，但字段对应和原始样本位置仍属于基准版本的映射证据。", "An imported trajectory can yield observability records, but field correspondence and original sample position remain mapping evidence tied to the benchmark version.", "import 済 trajectory は observability record を生成できますが、field 対応と原 sample 位置は benchmark 版に結び付く写像証拠です。"),
        disambiguation_test: localized("若对象回答某 benchmark 字段如何解释，归基准映射；若对象回答一次执行中何时发生、因何相连，归运行可观测性。", "If the object answers how a benchmark field is interpreted, it belongs to benchmark mappings; if it answers when an occurrence happened and what caused it in one execution, it belongs to runtime observability.", "benchmark field の解釈を答えるなら benchmark 写像、一実行でいつ発生し何が原因かを答えるなら runtime observability に属します。"),
        result: "distinct",
        owner_module_id: "adapter-benchmarks",
      },
    ],
  },
  "adapter-frameworks": {
    purpose: localized("把智能体框架公开的角色、节点、任务和运行构造映射到核心语义，同时保留框架版本与差异。", "Map public roles, nodes, tasks, and runtime constructs from agent frameworks to core semantics while retaining framework versions and differences.", "エージェントフレームワークが公開する役割、ノード、タスク、実行構造を、版と差異を保持したままコア意味へ写像します。"),
    includes: [
      localized("CrewAI、LangGraph、OpenAI Agents 等框架构造的版本化适配器和概念对应。", "Versioned adapters and concept correspondences for constructs in CrewAI, LangGraph, OpenAI Agents, and similar frameworks.", "CrewAI、LangGraph、OpenAI Agents などの構造に対する版付きアダプターと概念対応。"),
      localized("框架特有字段、生命周期和组合语义与核心 Role、Task、Run、Trace 之间的差异记录。", "Difference records between framework-specific fields, lifecycles, and composition semantics and core Role, Task, Run, and Trace concepts.", "フレームワーク固有の項目、ライフサイクル、構成意味と、コア Role、Task、Run、Trace の差異記録。"),
    ],
    excludes: [
      localized("框架的完整内部类树、私有实现细节和运行实例不在此重建。", "Complete internal class trees, private implementation details, and runtime instances of a framework are not reconstructed here.", "フレームワークの完全な内部クラス木、非公開実装詳細、実行インスタンスは再構築しません。"),
      localized("不依赖框架名称即可复用的映射规则与验证方法归映射规则与验证。", "Mapping rules and validation methods reusable without a framework name belong to mapping rules and validation.", "フレームワーク名に依存せず再利用できる写像規則と検証方法は写像規則と検証に属します。"),
    ],
    owns_when: localized("当映射的源端点由特定框架及版本定义，并且移除该上下文会改变构造含义时，在此拥有。", "Own a mapping when its source endpoint is defined by a particular framework and version and removing that context changes the construct's meaning.", "写像元端点が特定フレームワークと版で定義され、その文脈を除くと構造の意味が変わる場合に所有します。"),
    references_when: localized("编排、运行或追踪数据需要说明其原始框架构造时，引用框架适配器，而核心事实仍由相应业务域拥有。", "Orchestration, runtime, or tracing data references a framework adapter to state its originating construct, while the core fact remains owned by its business domain.", "編成、実行、トレースのデータが由来となるフレームワーク構造を示すときに参照し、コア事実は対応する業務領域が所有します。"),
    competency_questions: [
      {
        semantic_key: "adapter-frameworks.construct-equivalence",
        related_module_ids: ["orchestration-composition"],
        questions: localized("给定框架版本中的节点或团队构造，它对应哪个核心组合、角色或任务概念？", "For a node or team construct in a given framework version, which core composition, role, or task concept does it correspond to?", "特定フレームワーク版のノードまたはチーム構造は、どのコア構成、役割、タスク概念に対応しますか。"),
        query: "Select a framework adapter by name and version, then resolve the exact source construct to one or more typed core endpoints.",
        expected_assertion: "The result names the framework construct, version, typed core endpoint, and any non-equivalent residual semantics.",
      },
      {
        semantic_key: "adapter-frameworks.lifecycle-difference",
        related_module_ids: ["runtime-system"],
        questions: localized("框架运行对象的生命周期与核心 RuntimeSession 有哪些状态对应和不可映射差异？", "Which states of a framework run object correspond to core RuntimeSession states, and which differences cannot be mapped?", "フレームワーク実行オブジェクトのライフサイクル状態はコア RuntimeSession のどれに対応し、写像不能な差異は何ですか。"),
        query: "Compare the framework lifecycle state set and transitions with RuntimeSession state and event mappings for the selected version.",
        expected_assertion: "Each source state is mapped or explicitly marked unmapped, and transition direction is preserved.",
      },
      {
        semantic_key: "adapter-frameworks.no-shadow-taxonomy",
        related_module_ids: ["adapter-mapping-infrastructure"],
        questions: localized("框架映射是否只保留必要端点，而没有复制一套与核心概念竞争的影子分类树？", "Does a framework mapping retain only necessary endpoints without copying a shadow taxonomy that competes with core concepts?", "フレームワーク写像は必要な端点だけを保持し、コア概念と競合する影の分類木を複製していませんか。"),
        query: "List framework-owned concepts and flag any accepted concept lacking either an external construct identity or an explicit mapping role.",
        expected_assertion: "Every framework-owned concept identifies an external construct or adapter role; no duplicate core taxonomy appears.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "orchestration-composition",
        decision: "owns",
        owner_module_id: "adapter-frameworks",
        rationale: localized("框架映射拥有外部构造到核心组合语义的对应；协作组合拥有 Chain、Parallel、Voting 等框架无关模式。", "Framework mappings own correspondences from external constructs to core composition semantics; collaboration composition owns framework-neutral Chain, Parallel, Voting, and related patterns.", "フレームワーク写像は外部構造からコア構成意味への対応を所有し、協働構成は Chain、Parallel、Voting などフレームワーク非依存のパターンを所有します。"),
      },
      {
        other_module_id: "runtime-system",
        decision: "owns",
        owner_module_id: "adapter-frameworks",
        rationale: localized("框架映射拥有框架 run、node 或 team 构造到核心运行语义的版本化对应；运行系统拥有真实 RuntimeSession、Environment 和生命周期事件。", "Framework mappings own versioned correspondences from framework run, node, or team constructs to core runtime semantics; runtime system owns actual RuntimeSession, Environment, and lifecycle events.", "framework 写像は run、node、team 構造からコア実行意味への版付き対応を所有し、runtime system は実際の RuntimeSession、Environment、lifecycle event を所有します。"),
      },
      {
        other_module_id: "adapter-mapping-infrastructure",
        decision: "owns",
        owner_module_id: "adapter-frameworks",
        rationale: localized("框架映射拥有 CrewAI、LangGraph、OpenAI Agents 等框架构造的专用对应与差异；映射基础设施拥有可跨框架复用的规则、测试和覆盖算法。", "Framework mappings own framework-specific correspondences and differences for CrewAI, LangGraph, OpenAI Agents, and peers; mapping infrastructure owns rules, tests, and coverage algorithms reusable across frameworks.", "framework 写像は CrewAI、LangGraph、OpenAI Agents 等の固有対応・差異を所有し、写像基盤は framework 横断で再利用する規則・試験・被覆 algorithm を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "orchestration-composition",
        semantic_area: localized("框架节点拓扑与核心协作拓扑", "Framework node topology versus core collaboration topology", "フレームワークのノードトポロジーとコア協働トポロジー"),
        overlap_reason: localized("两者都描述节点和边，但框架映射保留外部 API 身份，协作组合表达可跨实现复用的控制结构。", "Both describe nodes and edges, but framework mappings retain external API identity while collaboration composition expresses implementation-independent control structures.", "どちらもノードと辺を記述しますが、フレームワーク写像は外部 API の同一性を保持し、協働構成は実装非依存の制御構造を表します。"),
        disambiguation_test: localized("若替换框架后该模式的身份和约束仍不变，则归协作组合；若必须保留框架类型或版本才能解释，则归框架映射。", "If replacing the framework leaves the pattern's identity and constraints unchanged, it belongs to collaboration composition; if a framework type or version is required, it belongs to framework mappings.", "フレームワークを置き換えてもパターンの同一性と制約が変わらなければ協働構成、型または版が説明に必須ならフレームワーク写像に属します。"),
        result: "distinct",
        owner_module_id: "adapter-frameworks",
      },
      {
        other_module_id: "runtime-system",
        semantic_area: localized("框架运行构造与真实运行会话", "Framework run constructs versus actual runtime sessions", "framework 実行構造と実 runtime session"),
        overlap_reason: localized("框架对象的状态枚举需要映射，但一次会话的开始、暂停和结束是具有时间与环境身份的运行事实。", "Framework-object state enums require mapping, whereas a session's start, pause, and end are runtime facts with temporal and environment identity.", "framework object の状態 enum は写像対象ですが、session の開始・停止・終了は時刻と環境 identity を持つ実行事実です。"),
        disambiguation_test: localized("若更换实际 session 后对象仍表示同一框架构造，则归框架映射；若对象随一次运行开始或结束而产生，则归运行系统。", "If the object still denotes the same framework construct after changing the actual session, it belongs to framework mappings; if it arises when one run starts or ends, it belongs to runtime system.", "実 session を替えても同じ framework 構造なら framework 写像、一回の run の開始・終了で生じるなら runtime system に属します。"),
        result: "distinct",
        owner_module_id: "adapter-frameworks",
      },
      {
        other_module_id: "adapter-mapping-infrastructure",
        semantic_area: localized("框架专用适配与通用映射基础设施", "Framework-specific adaptation versus generic mapping infrastructure", "framework 固有適配と汎用写像基盤"),
        overlap_reason: localized("框架适配会调用 MappingRule 和覆盖检查，但框架版本、公开构造与生命周期差异不能提升为通用规则。", "Framework adaptation invokes MappingRule and coverage checks, but framework versions, public constructs, and lifecycle differences must not be promoted into generic rules.", "framework 適配は MappingRule と被覆検査を使いますが、framework 版・公開構造・lifecycle 差異を汎用規則へ昇格させません。"),
        disambiguation_test: localized("去掉框架名称后仍有完整定义的对象归映射基础设施；其身份包含框架及版本的对象归框架映射。", "An object fully defined after removing the framework name belongs to mapping infrastructure; one whose identity includes framework and version belongs to framework mappings.", "framework 名を除いても完全に定義できれば写像基盤、identity に framework と版を含めば framework 写像に属します。"),
        result: "distinct",
        owner_module_id: "adapter-frameworks",
      },
    ],
  },
  "adapter-mapping-infrastructure": {
    purpose: localized("为异构模型之间的语义对应提供可复用的映射规则，并用测试、覆盖率与转换告警证明映射是否可靠。", "Provide reusable semantic correspondences between heterogeneous models and show their reliability through tests, coverage, and conversion warnings.", "異種モデル間の意味対応を再利用可能な写像規則として定義し、テスト、網羅率、変換警告によって信頼性を示します。"),
    includes: [
      localized("适配器上位类型、映射规范、映射规则及规则采用的匹配条件。", "The Adapter supertype, mapping specifications, mapping rules, and the matching conditions used by those rules.", "Adapter 上位型、写像仕様、写像規則、および規則が用いる照合条件。"),
      localized("映射测试与执行记录、覆盖率结果、未映射项和有损转换告警。", "Mapping tests and runs, coverage results, unmapped items, and lossy-conversion warnings.", "写像テストと実行記録、網羅率結果、未写像項目、有損変換警告。"),
    ],
    excludes: [
      localized("具体协议、框架、基准、状态图和导出格式的词汇由各自适配范围拥有；这里不复制外部类树。", "Protocol-, framework-, benchmark-, statechart-, and export-format vocabularies belong to their respective adapter scopes; external class trees are not copied here.", "プロトコル、フレームワーク、ベンチマーク、ステートチャート、出力形式固有の語彙は各適配範囲が所有し、外部クラス木はここに複製しません。"),
      localized("一次转换产生的文件或运行产物归模式导出或运行产物；它们不是映射规则的子类。", "Files or runtime artifacts produced by one conversion belong to schema export or runtime artifacts; they are not subclasses of MappingRule.", "一回の変換で生じるファイルや実行成果物はスキーマ出力または実行成果物に属し、MappingRule の下位型ではありません。"),
    ],
    owns_when: localized("当对象的身份由跨模型的对应规则、规则适用条件或对该对应关系的验证证据决定时，在此登记。", "Own an object when its identity is determined by a cross-model correspondence, its applicability conditions, or evidence validating that correspondence.", "対象の同一性がモデル間対応、適用条件、またはその対応を検証する証拠で決まる場合に所有します。"),
    references_when: localized("具体适配范围应用通用规则或报告映射执行结果时，引用这里的 Adapter、MappingRule 和验证概念。", "A concrete adapter scope references Adapter, MappingRule, and validation concepts here when applying a shared rule or reporting a mapping run.", "具体的な適配範囲が共通規則を適用したり写像実行結果を報告したりするとき、ここで定義した Adapter、MappingRule、検証概念を参照します。"),
    competency_questions: [
      {
        semantic_key: "adapter-mapping-infrastructure.rule-applicability",
        related_module_ids: ["adapter-frameworks"],
        questions: localized("给定源概念、目标概念和版本约束，哪些已接受映射规则实际适用？", "Given source and target concepts plus version constraints, which accepted mapping rules actually apply?", "変換元概念、変換先概念、版制約が与えられたとき、どの承認済み写像規則が実際に適用されますか。"),
        query: "Select accepted MappingRule values whose source pattern, target pattern, and version constraints all match the requested mapping context.",
        expected_assertion: "Every returned rule matches both endpoints and every declared version constraint; no merely related rule is returned.",
      },
      {
        semantic_key: "adapter-mapping-infrastructure.coverage-gap",
        related_module_ids: ["adapter-schema-export"],
        questions: localized("一次映射验证后，哪些必需语义没有目标对应，哪些对应被判定为有损？", "After validating a mapping, which required semantics lack a target correspondence and which correspondences are lossy?", "写像を検証した後、必須意味のうち対応先がないものと、有損と判定された対応はどれですか。"),
        query: "For one mapping validation, derive required source semantics minus covered semantics, then list ConversionWarning values classified as lossy.",
        expected_assertion: "The uncovered set equals the required-but-unmapped set, and each lossy item is backed by a warning tied to the tested rule.",
      },
      {
        semantic_key: "adapter-mapping-infrastructure.adapter-specialization",
        related_module_ids: ["adapter-protocols"],
        questions: localized("每个具体适配器能否沿分类骨架回到 Adapter，而不会把规则、执行记录或报告误当成适配器？", "Can every concrete adapter reach Adapter through specialization without classifying rules, runs, or reports as adapters?", "各具体アダプターは、規則、実行記録、報告をアダプターと誤分類せず、特殊化経路で Adapter に到達できますか。"),
        query: "Traverse primary is_a edges from every accepted concrete adapter and separately inspect MappingRule, MappingTestResult, and CoverageReport kinds.",
        expected_assertion: "Each concrete adapter reaches Adapter exactly once; rules, test results, and coverage reports do not appear in that specialization set.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "adapter-frameworks",
        decision: "owns",
        owner_module_id: "adapter-mapping-infrastructure",
        rationale: localized("通用映射规则的身份不依赖某一框架；框架映射只选择并约束这些规则来解释特定框架构造。", "A generic mapping rule is identifiable without any one framework; framework mappings select and constrain such rules for framework-specific constructs.", "共通写像規則の同一性は特定フレームワークに依存せず、フレームワーク写像は固有構造を説明するために規則を選択して制約します。"),
      },
      {
        other_module_id: "adapter-schema-export",
        decision: "owns",
        owner_module_id: "adapter-mapping-infrastructure",
        rationale: localized("映射基础设施拥有与目标格式无关的规则适用性、覆盖缺口和有损判断；模式导出拥有把这些判断投影到特定格式的活动与配置。", "Mapping infrastructure owns format-independent rule applicability, coverage gaps, and loss judgments; schema export owns the activity and profile that project them into a target format.", "写像基盤は形式非依存の規則適用性、被覆欠落、損失判断を所有し、スキーマ出力はそれらを対象形式へ投影する活動と profile を所有します。"),
      },
      {
        other_module_id: "adapter-protocols",
        decision: "owns",
        owner_module_id: "adapter-mapping-infrastructure",
        rationale: localized("映射基础设施拥有 Adapter、MappingRule、测试结果和覆盖报告的通用机制；协议映射拥有某一协议版本构造到核心语义的规范对应。", "Mapping infrastructure owns the generic Adapter, MappingRule, test-result, and coverage machinery; protocol mappings own normative correspondences from versioned protocol constructs to core semantics.", "写像基盤は汎用 Adapter、MappingRule、試験結果、被覆報告を所有し、プロトコル写像は版付きプロトコル構造からコア意味への規範対応を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "adapter-frameworks",
        semantic_area: localized("通用映射机制与框架专用对应", "Generic mapping machinery versus framework-specific correspondences", "共通写像機構とフレームワーク固有対応"),
        overlap_reason: localized("两者都会出现 Adapter 和 MappingRule，但只有框架映射需要 CrewAI、LangGraph 等外部构造作为端点。", "Both use Adapter and MappingRule, but only framework mappings require constructs from CrewAI, LangGraph, or another framework as endpoints.", "どちらも Adapter と MappingRule を用いますが、CrewAI や LangGraph などの外部構造を端点に必要とするのはフレームワーク写像だけです。"),
        disambiguation_test: localized("移除框架名称和版本后规则仍能完整说明源、目标与验证条件，则归通用映射；否则归框架映射。", "If removing the framework name and version leaves a complete source, target, and validation condition, the rule is generic; otherwise it belongs to framework mappings.", "フレームワーク名と版を除いても変換元、変換先、検証条件が完全なら共通写像、そうでなければフレームワーク写像に属します。"),
        result: "distinct",
        owner_module_id: "adapter-mapping-infrastructure",
      },
      {
        other_module_id: "adapter-schema-export",
        semantic_area: localized("通用语义映射与格式投影", "Generic semantic mapping versus format projection", "汎用意味写像と形式投影"),
        overlap_reason: localized("两者都会报告无法表达的语义，但前者判断源与目标概念是否对应，后者判断某种序列化语言能否承载该对应。", "Both can report inexpressible semantics, but the former judges correspondence between source and target concepts while the latter judges whether a serialization language can carry it.", "どちらも表現不能な意味を報告しますが、前者は概念間対応、後者は特定直列化言語でその対応を表せるかを判断します。"),
        disambiguation_test: localized("若更换 JSON Schema、OWL 或 SHACL 后规则结论不变，则归映射基础设施；若结论取决于目标格式能力，则归模式导出。", "If the conclusion survives switching JSON Schema, OWL, or SHACL, it belongs to mapping infrastructure; if it depends on target-format expressiveness, it belongs to schema export.", "JSON Schema、OWL、SHACL を替えても結論が不変なら写像基盤、対象形式の表現力に依存するならスキーマ出力に属します。"),
        result: "distinct",
        owner_module_id: "adapter-mapping-infrastructure",
      },
      {
        other_module_id: "adapter-protocols",
        semantic_area: localized("映射机制与协议专用对应", "Mapping machinery versus protocol-specific correspondence", "写像機構とプロトコル固有対応"),
        overlap_reason: localized("协议适配使用通用规则和测试，但协议参与者、消息行为和版本兼容区间只在协议语境中有意义。", "Protocol adapters use generic rules and tests, but protocol participants, message behavior, and version compatibility ranges have meaning only in a protocol context.", "プロトコル adapter は汎用規則と試験を使いますが、参加者、message 動作、版互換範囲はプロトコル文脈でのみ意味を持ちます。"),
        disambiguation_test: localized("不指定协议名称仍可复用的规则、运行或报告归映射基础设施；必须指定 MCP、A2A 等协议版本才能识别的对应归协议映射。", "Rules, runs, or reports reusable without naming a protocol belong to mapping infrastructure; correspondences identifiable only with an MCP, A2A, or other protocol version belong to protocol mappings.", "プロトコル名なしで再利用できる規則・実行・報告は写像基盤、MCP や A2A などの版が必要な対応はプロトコル写像に属します。"),
        result: "distinct",
        owner_module_id: "adapter-mapping-infrastructure",
      },
    ],
  },
  "adapter-protocols": {
    purpose: localized("把外部智能体协议的参与者、消息行为和版本语义对应到核心本体，使跨协议数据可以被一致解释。", "Map participants, message behaviors, and version semantics from external agent protocols to the core ontology so cross-protocol data can be interpreted consistently.", "外部エージェントプロトコルの参加者、メッセージ動作、版意味をコア本体へ対応付け、プロトコル横断データを一貫して解釈できるようにします。"),
    includes: [
      localized("A2A、MCP、FIPA、KQML 等协议构造到核心 Actor、Message、Task 与Capability 的版本化对应。", "Versioned correspondences from A2A, MCP, FIPA, KQML, and similar constructs to core Actor, Message, Task, and Capability semantics.", "A2A、MCP、FIPA、KQML などの構造からコアの Actor、Message、Task、Capability 意味への版付き対応。"),
      localized("协议字段差异、消息行为差异、兼容范围和无法无损转换的语义缺口。", "Protocol field differences, message-behavior differences, compatibility ranges, and semantic gaps that cannot be converted losslessly.", "プロトコルのフィールド差、メッセージ動作差、互換範囲、無損変換できない意味的欠落。"),
    ],
    excludes: [
      localized("正在运行的 MCP 会话、请求、响应和订阅归 MCP 交互；这里描述协议语义的对应，而非一次线上的交换。", "Live MCP sessions, requests, responses, and subscriptions belong to MCP interactions; this scope describes protocol-semantic correspondences, not one wire exchange.", "実行中の MCP セッション、要求、応答、購読は MCP インタラクションに属し、ここでは一回の通信ではなくプロトコル意味の対応を扱います。"),
      localized("通用映射规则、覆盖率计算和验证执行由映射规则与验证拥有。", "Generic mapping rules, coverage computation, and validation runs belong to mapping rules and validation.", "共通写像規則、網羅率計算、検証実行は写像規則と検証が所有します。"),
    ],
    owns_when: localized("当事实必须借助协议名称、协议版本及该协议构造的规范含义才能识别时，在此定义。", "Own a fact when identifying it requires a protocol name, protocol version, and the normative meaning of a protocol construct.", "事実の識別にプロトコル名、版、構造の規範的意味が必要な場合に所有します。"),
    references_when: localized("MCP 运行交互或消息解析需要解释协议字段的核心含义时，引用对应的协议适配器与映射规则。", "MCP runtime interactions or message parsers reference the protocol adapter and mapping rules when they need the core meaning of protocol fields.", "MCP 実行時インタラクションやメッセージ解析がプロトコル項目のコア意味を必要とするとき、対応するプロトコルアダプターと写像規則を参照します。"),
    competency_questions: [
      {
        semantic_key: "adapter-protocols.versioned-construct-mapping",
        related_module_ids: ["tool-mcp-transport"],
        questions: localized("某协议版本中的一个构造对应哪些核心概念和关系，兼容范围到哪一版？", "Which core concepts and relations correspond to a construct in a given protocol version, and through which versions is that mapping valid?", "あるプロトコル版の構造はどのコア概念・関係に対応し、その写像はどの版まで有効ですか。"),
        query: "Resolve the protocol adapter by protocol and version, then select mappings whose source construct and compatibility interval contain the request.",
        expected_assertion: "The result identifies explicit core endpoints and a compatibility interval containing the requested version.",
      },
      {
        semantic_key: "adapter-protocols.message-behavior-gap",
        related_module_ids: ["info-messages-instructions"],
        questions: localized("协议消息行为映射到核心 Message 时，哪些必需行为被保留，哪些只能记录为差异？", "When protocol message behavior maps to core Message, which required behaviors are preserved and which remain explicit gaps?", "プロトコルのメッセージ動作をコア Message に写像するとき、どの必須動作が保持され、どれが明示的差異として残りますか。"),
        query: "Compare normative source behaviors with mapped core predicates and collect unmapped or cardinality-changing behaviors as protocol gaps.",
        expected_assertion: "Every normative behavior is either mapped to a core predicate with compatible cardinality or listed as an explicit gap.",
      },
      {
        semantic_key: "adapter-protocols.participant-role-separation",
        related_module_ids: ["runtime-actors"],
        questions: localized("协议参与者如何对应运行主体，而不会把协议角色直接定义成永久 Actor 类型？", "How does a protocol participant correspond to a runtime actor without turning a protocol role into a permanent Actor subtype?", "プロトコル参加者を、プロトコル役割を恒久的な Actor 下位型にせず実行主体へどう対応付けますか。"),
        query: "Return participant-to-actor mappings and inspect whether role semantics are represented by bindings rather than cross-kind is_a edges.",
        expected_assertion: "Every participant mapping identifies a runtime actor or reference plus a role binding; no protocol role is asserted as an Actor subtype.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "tool-mcp-transport",
        decision: "owns",
        owner_module_id: "adapter-protocols",
        rationale: localized("协议映射拥有版本化规范对应；MCP 交互拥有一次会话中实际发生的请求、响应、通知和订阅。", "Protocol mappings own versioned normative correspondences; MCP interactions own requests, responses, notifications, and subscriptions that occur in a live session.", "プロトコル写像は版付き規範対応を所有し、MCP インタラクションは実セッションで生じる要求、応答、通知、購読を所有します。"),
      },
      {
        other_module_id: "info-messages-instructions",
        decision: "owns",
        owner_module_id: "adapter-protocols",
        rationale: localized("协议映射拥有外部消息类型与行为到核心 Message 语义的版本化对应；消息与对话拥有实际 Conversation、Turn、Message 及其顺序。", "Protocol mappings own versioned correspondences from external message types and behavior to core Message semantics; messages and conversations own actual Conversation, Turn, Message, and ordering facts.", "プロトコル写像は外部 message 型・動作からコア Message 意味への版付き対応を所有し、message と会話は実際の Conversation、Turn、Message と順序を所有します。"),
      },
      {
        other_module_id: "runtime-actors",
        decision: "owns",
        owner_module_id: "adapter-protocols",
        rationale: localized("协议映射拥有协议 participant/role 到核心 Actor 语义的对应；运行参与者拥有在某次系统运行中实际绑定的 Actor、Role 和 Capability。", "Protocol mappings own correspondences from protocol participants or roles to core Actor semantics; runtime actors own Actor, Role, and Capability bindings that actually exist in a system run.", "プロトコル写像は participant/role からコア Actor 意味への対応を所有し、実行参加者は system run で実在する Actor、Role、Capability binding を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "tool-mcp-transport",
        semantic_area: localized("MCP 规范语义与 MCP 运行交换", "MCP specification semantics versus MCP runtime exchanges", "MCP 仕様意味と MCP 実行時交換"),
        overlap_reason: localized("两者都使用 MCP 请求和能力名称，但一个是版本化映射声明，另一个是带时间与会话身份的运行事实。", "Both mention MCP requests and capabilities, but one is a versioned mapping declaration and the other is a time- and session-identified runtime fact.", "どちらも MCP 要求と能力を扱いますが、一方は版付き写像宣言、他方は時刻とセッション同一性を持つ実行事実です。"),
        disambiguation_test: localized("若删除 session id、消息 id 和发生时间后对象仍是完整的规范对应，则归协议映射；否则归 MCP 交互。", "If removing session ID, message ID, and occurrence time leaves a complete normative correspondence, it belongs to protocol mappings; otherwise it belongs to MCP interactions.", "session id、message id、発生時刻を除いても完全な規範対応ならプロトコル写像、そうでなければ MCP インタラクションに属します。"),
        result: "distinct",
        owner_module_id: "adapter-protocols",
      },
      {
        other_module_id: "info-messages-instructions",
        semantic_area: localized("协议消息对应与真实对话消息", "Protocol-message correspondence versus actual dialogue messages", "プロトコル message 対応と実会話 message"),
        overlap_reason: localized("两者都提到发送者和消息，但映射描述类型层面的保真与缺口，消息范围描述带会话身份和位置的发生记录。", "Both mention senders and messages, but mappings describe type-level fidelity and gaps while the message scope describes occurrences with conversation identity and position.", "どちらも送信者と message を扱いますが、写像は型レベルの保持・欠落、message 範囲は会話 identity と位置を持つ発生を記述します。"),
        disambiguation_test: localized("若没有具体 conversation id 仍成立，则归协议映射；若对象由会话、轮次、位置或发送者实例识别，则归消息与对话。", "If the fact remains true without a concrete conversation ID, it belongs to protocol mappings; if identity depends on session, turn, position, or sender instance, it belongs to messages and conversations.", "具体 conversation id なしでも成立すればプロトコル写像、session、turn、位置、送信者 instance により識別されれば message と会話に属します。"),
        result: "distinct",
        owner_module_id: "adapter-protocols",
      },
      {
        other_module_id: "runtime-actors",
        semantic_area: localized("协议角色映射与运行参与者身份", "Protocol-role mapping versus runtime participant identity", "プロトコル role 写像と実行参加者 identity"),
        overlap_reason: localized("协议角色可由许多运行主体扮演，运行主体也可同时承担多个协议或业务角色，二者不能互作永久子类。", "Many runtime actors can play a protocol role and one runtime actor can carry several protocol or business roles, so neither is a permanent subtype of the other.", "一つの protocol role を複数 actor が担え、一 actor が複数 role を持てるため、永久的 subtype 関係にはできません。"),
        disambiguation_test: localized("由协议规范名称和版本识别的是映射；由 actor id、有效期和运行绑定识别的是运行参与者。", "An object identified by protocol construct name and version is a mapping; one identified by actor ID, validity interval, and runtime binding is a runtime actor fact.", "protocol 構造名と版で識別されるものは写像、actor id・有効期間・runtime binding で識別されるものは実行参加者事実です。"),
        result: "distinct",
        owner_module_id: "adapter-protocols",
      },
    ],
  },
  "adapter-schema-export": {
    purpose: localized("把 canonical 本体投影为 JSON Schema、OWL、SHACL、ShEx、Zod 或 Pydantic，并明确往返转换的语义损失。", "Project the canonical ontology into JSON Schema, OWL, SHACL, ShEx, Zod, or Pydantic while making round-trip semantic loss explicit.", "canonical 本体を JSON Schema、OWL、SHACL、ShEx、Zod、Pydantic へ投影し、往復変換の意味損失を明示します。"),
    includes: [
      localized("目标格式配置、命名与类型映射、约束投影、导出活动及其可识别输出。", "Target-format profiles, naming and type mappings, constraint projections, export activities, and their identifiable outputs.", "対象形式プロファイル、命名・型写像、制約投影、出力活動、および識別可能な出力。"),
      localized("round-trip 验证、不可表达约束、有损投影报告和格式版本兼容性。", "Round-trip validation, inexpressible constraints, lossy-projection reports, and format-version compatibility.", "round-trip 検証、表現不能な制約、有損投影報告、形式版の互換性。"),
    ],
    excludes: [
      localized("canonical 概念和关系的业务含义由各自 owner 维护，导出不得生成第二套权威语义。", "Business meanings of canonical concepts and relations remain with their owners; an export must not create a second authoritative semantics.", "canonical 概念・関係の業務意味は各所有者が維持し、出力によって第二の権威的意味を作りません。"),
      localized("通用映射规则的验证算法归映射规则与验证；这里拥有的是格式投影和产物。", "Generic mapping-rule validation belongs to mapping rules and validation; this scope owns format projection and its artifacts.", "共通写像規則の検証アルゴリズムは写像規則と検証に属し、ここでは形式投影と成果物を所有します。"),
    ],
    owns_when: localized("当对象的身份由目标 schema 语言、目标版本、投影配置或一次导出运行决定时，在此拥有。", "Own an object when its identity is determined by a target schema language, target version, projection profile, or one export run.", "対象の同一性が対象スキーマ言語、版、投影プロファイル、または一回の出力実行で決まる場合に所有します。"),
    references_when: localized("其他范围发布或验证其 canonical 定义的机器可读投影时，引用对应导出配置和产物标识。", "Other scopes reference the relevant export profile and artifact identity when publishing or validating a machine-readable projection of their canonical definitions.", "他の範囲が canonical 定義の機械可読投影を公開・検証するとき、対応する出力プロファイルと成果物識別子を参照します。"),
    competency_questions: [
      {
        semantic_key: "adapter-schema-export.constraint-projection",
        related_module_ids: ["adapter-mapping-infrastructure"],
        questions: localized("某个 canonical 约束在指定目标格式中如何表达，是否存在不可表达部分？", "How is a canonical constraint expressed in a selected target format, and is any part inexpressible?", "canonical 制約は指定した対象形式でどう表現され、表現不能な部分がありますか。"),
        query: "Resolve the target profile and map each component of the canonical constraint to target constructs, collecting unsupported components.",
        expected_assertion: "Every constraint component has a target expression or an explicit inexpressible marker with a reason.",
      },
      {
        semantic_key: "adapter-schema-export.round-trip-loss",
        related_module_ids: ["runtime-artifacts"],
        questions: localized("导出后再导入能否恢复原概念、关系方向、基数和受控值，损失具体在哪里？", "Can export followed by import restore concepts, relation direction, cardinality, and controlled values, and exactly where does loss occur?", "出力後に再取込すると概念、関係方向、基数、統制値を復元でき、損失は具体的にどこで生じますか。"),
        query: "Compare canonical source fingerprints with the normalized round-trip graph across node identity, relation direction, cardinality, and value constraints.",
        expected_assertion: "The report lists exact preserved and lost semantics; a lossless result requires equality in all four dimensions.",
      },
      {
        semantic_key: "adapter-schema-export.artifact-lineage",
        related_module_ids: ["runtime-artifacts"],
        questions: localized("一个导出文件由哪个 canonical 版本、投影配置和导出活动生成？", "Which canonical version, projection profile, and export activity generated an exported file?", "ある出力ファイルは、どの canonical 版、投影プロファイル、出力活動から生成されましたか。"),
        query: "Trace an ExportArtifact to its ExportRun, target profile, canonical version IRI, and source fingerprint.",
        expected_assertion: "Exactly one lineage chain identifies the export run, profile, version, and fingerprint used to produce the artifact.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "adapter-mapping-infrastructure",
        decision: "owns",
        owner_module_id: "adapter-schema-export",
        rationale: localized("模式导出拥有目标格式、投影运行和导出产物；映射规则与验证拥有可跨格式复用的对应规则和测试方法。", "Schema export owns target formats, projection runs, and exported artifacts; mapping rules and validation own correspondence rules and test methods reusable across formats.", "スキーマ出力は対象形式、投影実行、出力成果物を所有し、写像規則と検証は形式横断で再利用できる対応規則とテスト方法を所有します。"),
      },
      {
        other_module_id: "runtime-artifacts",
        decision: "owns",
        owner_module_id: "adapter-schema-export",
        rationale: localized("模式导出拥有目标格式 profile、约束投影规则、导出活动和往返损失判断；运行产物拥有生成文件或图的身份、版本、摘要与存储血缘。", "Schema export owns target-format profiles, constraint-projection rules, export activities, and round-trip loss judgments; runtime artifacts own generated file or graph identity, version, digest, and storage lineage.", "schema export は対象形式 profile、制約投影規則、export activity、round-trip loss 判断を所有し、runtime artifact は生成 file/graph の identity、version、digest、storage lineage を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "adapter-mapping-infrastructure",
        semantic_area: localized("格式投影规则与通用语义映射", "Format projection rules versus generic semantic mappings", "形式投影規則と共通意味写像"),
        overlap_reason: localized("两者都可能有 source/target 和验证，但投影规则必须指定 schema 语言，通用映射不要求生成文件。", "Both may have source, target, and validation, but a projection rule must name a schema language while a generic mapping need not produce a file.", "どちらも source、target、検証を持ち得ますが、投影規則はスキーマ言語を指定し、共通写像はファイル生成を必要としません。"),
        disambiguation_test: localized("若删除目标格式后规则仍能完整描述语义对应，则归通用映射；若规则依赖格式表达能力或产生导出产物，则归模式导出。", "If removing the target format leaves a complete semantic correspondence, it is generic mapping; if the rule depends on format expressiveness or produces an export artifact, it belongs to schema export.", "対象形式を除いても意味対応が完全なら共通写像、形式の表現力に依存するか出力成果物を生成するならスキーマ出力に属します。"),
        result: "distinct",
        owner_module_id: "adapter-schema-export",
      },
      {
        other_module_id: "runtime-artifacts",
        semantic_area: localized("导出过程语义与导出产物身份", "Export-process semantics versus exported-artifact identity", "export process 意味と export artifact identity"),
        overlap_reason: localized("一次导出活动产生 Artifact，但投影规则可跨多次导出复用，产物也可在生成后独立版本化和评审。", "An export activity produces an Artifact, but projection rules are reusable across runs and the artifact can be versioned and reviewed independently after generation.", "一 export activity は Artifact を生成しますが、投影規則は複数 run で再利用でき、artifact は生成後に独立 versioning・review できます。"),
        disambiguation_test: localized("若对象由目标格式和投影 profile 识别，归模式导出；若由 artifact id、digest 或 version 识别，归运行产物。", "If identity comes from target format and projection profile, it belongs to schema export; if it comes from artifact ID, digest, or version, it belongs to runtime artifacts.", "対象形式と projection profile で識別されれば schema export、artifact id・digest・version で識別されれば runtime artifact に属します。"),
        result: "distinct",
        owner_module_id: "adapter-schema-export",
      },
    ],
  },
  "adapter-statecharts": {
    purpose: localized("把 SCXML、XState 等状态图的状态、迁移、事件、守卫与动作对应到核心控制和运行语义。", "Map states, transitions, events, guards, and actions from SCXML, XState, and similar statecharts to core control and runtime semantics.", "SCXML、XState などの状態、遷移、イベント、ガード、アクションをコアの制御・実行意味へ対応付けます。"),
    includes: [
      localized("状态图模型、复合状态、迁移、守卫和动作到核心状态与控制概念的版本化对应。", "Versioned correspondences from statechart models, compound states, transitions, guards, and actions to core state and control concepts.", "状態図モデル、複合状態、遷移、ガード、アクションからコア状態・制御概念への版付き対応。"),
      localized("事件队列、进入退出语义、并行区域和历史状态无法直接对应时的差异记录。", "Difference records for event queues, entry/exit semantics, parallel regions, and history states that do not map directly.", "イベントキュー、入退場意味、並行領域、履歴状態が直接対応しない場合の差異記録。"),
    ],
    excludes: [
      localized("实际发生的运行事件、会话状态和检查点归运行域；这里不保存某次状态机实例的轨迹。", "Actual runtime events, session states, and checkpoints belong to runtime scopes; a particular machine instance's trace is not stored here.", "実際に発生した実行イベント、セッション状態、チェックポイントは実行領域に属し、特定状態機械インスタンスの軌跡は保存しません。"),
      localized("框架无关的路由策略、门条件和重试控制归路由与门控。", "Framework-neutral routing policies, gate conditions, and retry controls belong to routing and gates.", "フレームワーク非依存の経路方針、ゲート条件、再試行制御は経路制御とゲートに属します。"),
    ],
    owns_when: localized("当语义端点由状态图标准或实现中的 State、Transition、Guard、Action 身份决定时，在此拥有。", "Own a semantic endpoint when its identity is determined by a State, Transition, Guard, or Action in a statechart standard or implementation.", "意味端点の同一性が状態図標準または実装の State、Transition、Guard、Action で決まる場合に所有します。"),
    references_when: localized("路由控制或运行会话需要说明其状态逻辑来自哪份状态图时，引用适配器与状态图构造。", "Routing control or runtime sessions reference the adapter and statechart construct when stating which statechart supplies their state logic.", "経路制御や実行セッションが状態論理の由来となる状態図を示すとき、アダプターと状態図構造を参照します。"),
    competency_questions: [
      {
        semantic_key: "adapter-statecharts.transition-guard-mapping",
        related_module_ids: ["orchestration-routing-control"],
        questions: localized("一条带事件和守卫的状态迁移分别对应哪些核心条件、决定与状态变化？", "Which core conditions, decisions, and state changes correspond to a statechart transition with an event and guard?", "イベントとガードを持つ状態遷移は、どのコア条件、判断、状態変化に対応しますか。"),
        query: "Resolve one source Transition and return separate mappings for trigger event, guard condition, control decision, and target state.",
        expected_assertion: "The transition is not flattened into is_a; trigger, guard, decision, and state change remain separately typed and directionally linked.",
      },
      {
        semantic_key: "adapter-statecharts.parallel-history-loss",
        related_module_ids: ["runtime-system"],
        questions: localized("并行区域或历史状态映射到核心会话状态时，会丢失哪些恢复语义？", "When parallel regions or history states map to core session state, which recovery semantics are lost?", "並行領域または履歴状態をコアのセッション状態へ写像するとき、どの回復意味が失われますか。"),
        query: "Compare source parallel/history semantics with mapped RuntimeSession state and checkpoint relations, then enumerate unsupported restoration behavior.",
        expected_assertion: "Every unsupported concurrency or restoration behavior is recorded as a mapping gap rather than silently collapsed.",
      },
      {
        semantic_key: "adapter-statecharts.event-instance-separation",
        related_module_ids: ["runtime-observability"],
        questions: localized("状态图中的事件类型与运行时实际发生的事件实例是否保持清晰区分？", "Are statechart event types kept distinct from event instances that actually occur at runtime?", "状態図のイベント型と実行時に実際に発生したイベントインスタンスは明確に区別されていますか。"),
        query: "Inspect mappings from statechart Event declarations and runtime TraceEvent values, including instantiation or reference relations.",
        expected_assertion: "A declaration maps to event semantics, an occurrence remains a runtime event, and no cross-kind is_a edge conflates them.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "orchestration-routing-control",
        decision: "owns",
        owner_module_id: "adapter-statecharts",
        rationale: localized("状态图映射拥有外部 State、Transition 与 Guard 的对应；路由与门控拥有实现无关的路由规约、控制门和决定。", "Statechart mappings own correspondences for external State, Transition, and Guard constructs; routing and gates own implementation-neutral routing specifications, control gates, and decisions.", "状態図写像は外部 State、Transition、Guard の対応を所有し、経路制御とゲートは実装非依存の経路仕様、制御ゲート、判断を所有します。"),
      },
      {
        other_module_id: "runtime-system",
        decision: "owns",
        owner_module_id: "adapter-statecharts",
        rationale: localized("状态图映射拥有 SCXML/XState 状态与转换构造到核心会话状态的版本化对应；运行系统拥有某次会话实际经历的状态与生命周期事件。", "Statechart mappings own versioned correspondences from SCXML or XState state and transition constructs to core session states; runtime system owns the states and lifecycle events actually experienced by a session.", "statechart 写像は SCXML/XState の state・transition 構造からコア session state への版付き対応を所有し、runtime system は session が実際に経験した状態・lifecycle event を所有します。"),
      },
      {
        other_module_id: "runtime-observability",
        decision: "owns",
        owner_module_id: "adapter-statecharts",
        rationale: localized("状态图映射拥有事件类型、guard 和 action 到核心控制语义的对应；运行可观测性拥有实际触发的 TraceEvent 及其因果顺序。", "Statechart mappings own correspondences from event types, guards, and actions to core control semantics; runtime observability owns the TraceEvents actually triggered and their causal order.", "statechart 写像は event type、guard、action からコア制御意味への対応を所有し、runtime observability は実際に発火した TraceEvent と因果順序を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "orchestration-routing-control",
        semantic_area: localized("状态图守卫与核心门条件", "Statechart guards versus core gate conditions", "状態図ガードとコアのゲート条件"),
        overlap_reason: localized("两者都可计算真假并控制后续动作，但 Guard 依赖状态图语法和迁移端点，GateCondition 可独立用于其他控制流程。", "Both evaluate truth and control subsequent action, but Guard depends on statechart syntax and transition endpoints while GateCondition can govern other control flows.", "どちらも真偽を評価して後続動作を制御しますが、Guard は状態図構文と遷移端点に依存し、GateCondition は他の制御フローにも使えます。"),
        disambiguation_test: localized("若条件必须绑定某个状态图迁移才能识别，则归状态图映射；若可脱离状态图作为核心控制条件复用，则归路由与门控。", "If the condition is identifiable only when bound to a statechart transition, it belongs to statechart mappings; if reusable as a core control condition, it belongs to routing and gates.", "条件が状態図遷移への結合なしには識別できなければ状態図写像、コア制御条件として再利用できれば経路制御とゲートに属します。"),
        result: "distinct",
        owner_module_id: "adapter-statecharts",
      },
      {
        other_module_id: "runtime-system",
        semantic_area: localized("状态图规范状态与会话状态发生", "Statechart specification states versus session-state occurrences", "statechart 仕様状態と session 状態発生"),
        overlap_reason: localized("状态图定义平行区和历史状态的规范含义，运行会话只记录在特定时间实际处于何种状态。", "A statechart defines normative meaning for parallel regions and history states; a runtime session records only which state actually held at a particular time.", "statechart は並行 region・history state の規範意味を定義し、runtime session は特定時点で実際に成立した状態を記録します。"),
        disambiguation_test: localized("无 session id 仍可重用的 state/transition 对应归状态图映射；带发生时间和会话身份的状态归运行系统。", "A state or transition correspondence reusable without a session ID belongs to statechart mappings; a state with occurrence time and session identity belongs to runtime system.", "session id なしで再利用できる state/transition 対応は statechart 写像、発生時刻と session identity を持つ状態は runtime system に属します。"),
        result: "distinct",
        owner_module_id: "adapter-statecharts",
      },
      {
        other_module_id: "runtime-observability",
        semantic_area: localized("状态图事件类型与运行事件实例", "Statechart event types versus runtime event instances", "statechart event type と runtime event instance"),
        overlap_reason: localized("同一事件类型可在多个 trace 中发生，映射的兼容性不随单次事件的时间和父 span 改变。", "The same event type can occur in many traces, and mapping compatibility does not change with an occurrence's time or parent span.", "同じ event type は複数 trace で発生でき、写像互換性は一発生の時刻や parent span では変わりません。"),
        disambiguation_test: localized("由状态图模型与版本识别的对象归状态图映射；由 trace id、时间戳和 parent link 识别的对象归运行可观测性。", "An object identified by statechart model and version belongs to statechart mappings; one identified by trace ID, timestamp, and parent link belongs to runtime observability.", "statechart model と版で識別されるものは statechart 写像、trace id・timestamp・parent link で識別されるものは runtime observability に属します。"),
        result: "distinct",
        owner_module_id: "adapter-statecharts",
      },
    ],
  },
});
