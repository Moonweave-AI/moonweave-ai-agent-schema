import { deepFreeze } from "./deep-freeze.mjs";

const localized = (zh, en, ja) => ({ zh, en, ja });

export const ORCHESTRATION_MODULE_BOUNDARIES = deepFreeze({
  "orchestration-actors-delegation": {
    purpose: localized("定义协作中需要承担的协调角色与角色绑定，使运行主体可在不同任务和团队中改变职责而不改变主体身份。", "Define coordination roles and role bindings so runtime actors can change responsibilities across tasks and teams without changing actor identity.", "協働で担う調整役割と役割バインディングを定義し、実行主体が同一性を変えずタスクやチームごとに責務を変えられるようにします。"),
    includes: [
      localized("CoordinationRole 及编排者、子智能体、工作者、审查者等角色专门类型。", "CoordinationRole and orchestrator, subagent, worker, reviewer, and related role specializations.", "CoordinationRole と orchestrator、subagent、worker、reviewer などの役割特殊型。"),
      localized("ActorRoleBinding、团队角色、协调归属、适用任务与有效时间范围。", "ActorRoleBinding, team roles, coordination membership, applicable tasks, and validity intervals.", "ActorRoleBinding、team role、coordination membership、applicable task、validity interval。"),
    ],
    excludes: [
      localized("AgentAsToolInvocation 归调用与执行；协调角色只描述调用者在协作中的职责，不拥有一次程序化工具调用的发生身份。", "AgentAsToolInvocation belongs to invocation and execution; coordination roles describe the caller's collaboration responsibility but do not own the occurrence identity of a programmatic tool call.", "AgentAsToolInvocation は呼び出しと実行に属し、協調ロールは呼び出し元の協働責務を記述しますが、プログラム的ツール呼び出しの発生同一性を所有しません。"),
      localized("HumanActor、AgentActor、SystemServiceActor、ToolServiceActor 和 Model 等运行参与者归运行参与者与绑定；Role 不等于 Actor。", "HumanActor, AgentActor, SystemServiceActor, ToolServiceActor, Model, and other runtime participants belong to runtime actors and bindings; Role is not Actor.", "HumanActor、AgentActor、SystemServiceActor、ToolServiceActor、Model 等は runtime actor/binding に属し、Role は Actor ではありません。"),
      localized("DelegationProcess、Handoff、WorkerSelection 和委派契约归委派与移交；角色不会自动产生委派事件。", "DelegationProcess, Handoff, WorkerSelection, and delegation contracts belong to delegation and handoff; a role does not automatically create a delegation event.", "DelegationProcess、Handoff、WorkerSelection、委任契約は委任と引き継ぎに属し、役割が自動的に委任イベントを生成することはありません。"),
    ],
    owns_when: localized("当对象表达一项可由不同运行主体承担的协作职责，或某主体在给定任务和时段承担该职责的绑定时，在此拥有。", "Own an object when it expresses a collaboration responsibility that different runtime actors may bear, or a binding assigning that responsibility for a task and interval.", "対象が複数の実行主体が担い得る協働責務、または特定タスク・期間で主体へ責務を割り当てるバインディングを表す場合に所有します。"),
    references_when: localized("委派、组合或运行追踪需要说明谁以何种协作职责参与时，引用 CoordinationRole 与 ActorRoleBinding，同时保留 Actor 的运行身份。", "Delegation, composition, or runtime tracing references CoordinationRole and ActorRoleBinding when stating which responsibility an actor bears while retaining the actor's runtime identity.", "delegation、composition、runtime tracing が actor の collaboration responsibility を示すとき、Actor identity を保って CoordinationRole と ActorRoleBinding を参照します。"),
    competency_questions: [
      {
        semantic_key: "orchestration-actors-delegation.active-role-bindings",
        related_module_ids: ["runtime-actors"],
        questions: localized("在指定任务与时间点，哪些运行主体承担编排者、工作者或审查者角色？", "At a specified task and time, which runtime actors bear orchestrator, worker, or reviewer roles?", "指定 task と時点で、どの runtime actor が orchestrator、worker、reviewer role を担いますか。"),
        query: "Filter ActorRoleBinding values by task scope and validity interval, then join bearer Actor and CoordinationRole specialization.",
        expected_assertion: "Every answer is an active binding between a runtime actor and a role; the actor is not inferred from a role class name.",
      },
      {
        semantic_key: "orchestration-actors-delegation.role-not-actor",
        related_module_ids: ["runtime-actors"],
        questions: localized("Orchestrator、Subagent 和 Worker 是否都建模为角色而非永久 Actor 子类？", "Are Orchestrator, Subagent, and Worker all modeled as roles rather than permanent Actor subtypes?", "Orchestrator、Subagent、Worker はすべて恒久的 Actor 下位型ではなく役割としてモデル化されていますか。"),
        query: "Inspect accepted is_a paths and bears_role bindings for coordination-role labels and runtime actor classes.",
        expected_assertion: "Coordination labels reach CoordinationRole; runtime actors bear those roles through bindings, with no cross-kind is_a edge.",
      },
      {
        semantic_key: "orchestration-actors-delegation.team-coverage",
        related_module_ids: ["orchestration-composition"],
        questions: localized("一个协作拓扑要求的角色是否都已绑定，是否有冲突绑定或无人承担的职责？", "Are all roles required by a collaboration topology bound, and are there conflicting bindings or unfilled responsibilities?", "collaboration topology が要求する role は全て binding 済みで、conflicting binding や unfilled responsibility はありませんか。"),
        query: "Compare topology required-role slots with active ActorRoleBinding values, cardinality constraints, and actor availability.",
        expected_assertion: "Each mandatory role has the allowed number of active bearers; missing and conflicting assignments are reported separately.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "orchestration-delegation-handoff",
        decision: "owns",
        owner_module_id: "orchestration-actors-delegation",
        rationale: localized("协同角色拥有职责类型与主体绑定；委派与移交拥有把工作、控制或责任从一方转给另一方的过程和约定。", "Coordination roles own responsibility types and actor bindings; delegation and handoff own processes and agreements that transfer work, control, or responsibility.", "協調ロールは責務型と主体バインディングを所有し、委任と引き継ぎは作業、制御、責任を一方から他方へ移す過程と合意を所有します。"),
      },
      {
        other_module_id: "tool-invocation-execution",
        decision: "owns",
        owner_module_id: "orchestration-actors-delegation",
        rationale: localized(
          "协同角色拥有 Orchestrator、AnswerOwnership 与角色绑定；调用与执行拥有该角色发起的 AgentAsToolInvocation 发生事实。",
          "Coordination roles own Orchestrator, AnswerOwnership, and role bindings; invocation and execution own the AgentAsToolInvocation occurrence initiated by that role.",
          "協調ロールは Orchestrator、AnswerOwnership、役割バインディングを所有し、呼び出しと実行はその役割が開始した AgentAsToolInvocation の発生事実を所有します。",
        ),
      },
      {
        other_module_id: "runtime-actors",
        decision: "owns",
        owner_module_id: "orchestration-actors-delegation",
        rationale: localized("协作角色与责任拥有 Orchestrator、WorkerRole、CoordinationRole 和在协作中的 ownership 记录；运行参与者拥有承担这些角色的 Actor 身份、模型和有效绑定。", "Collaboration roles and responsibility own Orchestrator, WorkerRole, CoordinationRole, and collaboration ownership records; runtime actors own Actor identity, model, and effective bindings that carry those roles.", "collaboration role/responsibility は Orchestrator、WorkerRole、CoordinationRole、collaboration ownership record を所有し、runtime actor はそれらを担う Actor identity、model、effective binding を所有します。"),
      },
      {
        other_module_id: "orchestration-composition",
        decision: "owns",
        owner_module_id: "orchestration-actors-delegation",
        rationale: localized("协作角色与责任拥有谁协调、谁工作、谁拥有答案或控制的责任事实；编排拓扑拥有 Chain、Parallel、Sectioning、Voting 和 Synthesis 的结构与组成。", "Collaboration roles and responsibility own who coordinates, works, and owns answer or control; orchestration topology owns the structure and composition of Chain, Parallel, Sectioning, Voting, and Synthesis.", "collaboration role/responsibility は誰が coordinate/work し answer/control を所有するかを所有し、orchestration topology は Chain、Parallel、Sectioning、Voting、Synthesis の structure/composition を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "orchestration-delegation-handoff",
        semantic_area: localized("角色绑定与委派分配", "Role bindings versus delegation assignments", "役割バインディングと委任割当"),
        overlap_reason: localized("两者都能回答谁负责，但 ActorRoleBinding 描述一段时间内的职责，WorkerAssignment 是某次委派选择过程产生的工作分配。", "Both can answer who is responsible, but ActorRoleBinding describes responsibility over an interval while WorkerAssignment is work allocation produced by one delegation-selection process.", "どちらも responsibility を答えますが、ActorRoleBinding は interval 内の responsibility、WorkerAssignment は一 delegation-selection process が生成する work allocation です。"),
        disambiguation_test: localized("若更换具体工作项后职责仍成立，则归协同角色；若身份绑定某次委派、任务和交接事件，则归委派与移交。", "If responsibility remains after changing the concrete work item, it belongs to coordination roles; if identity is tied to one delegation, task, and transfer event, it belongs to delegation and handoff.", "具体作業項目を替えても責務が成立するなら協調ロール、一回の委任、タスク、引継イベントに同一性が結び付くなら委任と引き継ぎに属します。"),
        result: "distinct",
        owner_module_id: "orchestration-actors-delegation",
      },
      {
        other_module_id: "tool-invocation-execution",
        semantic_area: localized("协调责任与智能体工具调用", "Coordination responsibility versus agent-as-tool invocation", "協調責任とエージェントツール呼び出し"),
        overlap_reason: localized("Orchestrator 可发起 AgentAsToolInvocation，但前者是可复用协作角色，后者是具有调用参数、尝试和结果的一次程序化工具调用。", "An Orchestrator may initiate AgentAsToolInvocation, but the former is a reusable collaboration role while the latter is one programmatic tool-call occurrence with arguments, attempts, and results.", "Orchestrator は AgentAsToolInvocation を開始できますが、前者は再利用可能な協働ロール、後者は引数・試行・結果を持つ一回のプログラム的ツール呼び出しです。"),
        disambiguation_test: localized("由角色、职责范围和有效期识别的对象归协同角色；由 invocation id、参数、尝试和工具结果识别的对象归调用与执行。", "An object identified by role, responsibility scope, and validity interval belongs to coordination roles; one identified by invocation ID, arguments, attempts, and tool result belongs to invocation and execution.", "ロール・責務範囲・有効期間で識別される対象は協調ロール、invocation id・引数・試行・ツール結果で識別される対象は呼び出しと実行に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-actors-delegation",
      },
      {
        other_module_id: "runtime-actors",
        semantic_area: localized("协作角色责任与运行主体身份", "Collaboration role responsibility versus runtime actor identity", "collaboration role responsibility と runtime actor identity"),
        overlap_reason: localized("同一 Actor 可在不同任务中承担不同协调角色，一个 WorkerRole 也可先后绑定不同 Actor，角色不是永久主体子类。", "The same Actor can carry different coordination roles across tasks and one WorkerRole can bind different Actors over time; a role is not a permanent actor subtype.", "同じ Actor は task ごとに異なる coordination role を持ち、一 WorkerRole は時系列で別 Actor に bind できるため、role は permanent actor subtype ではありません。"),
        disambiguation_test: localized("由 actor id、model/provider 和绑定有效期识别的对象归运行参与者；由 collaboration id、role、answer/control ownership 识别的对象归协作角色与责任。", "An object identified by actor ID, model or provider, and binding interval belongs to runtime actors; one identified by collaboration ID, role, answer ownership, or control ownership belongs to collaboration roles and responsibility.", "actor id・model/provider・binding interval で識別されれば runtime actor、collaboration id・role・answer/control ownership で識別されれば collaboration role/responsibility に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-actors-delegation",
      },
      {
        other_module_id: "orchestration-composition",
        semantic_area: localized("责任分配与工作拓扑", "Responsibility assignment versus work topology", "responsibility assignment と work topology"),
        overlap_reason: localized("拓扑节点可被不同角色执行，责任也可在不改变拓扑的情况下移交；结构关系不等同于 ownership。", "Topology nodes can be executed by different roles and responsibility can transfer without changing topology; structural relations are not ownership.", "topology node は別 role が実行でき、topology を変えず responsibility を transfer できるため structure relation は ownership ではありません。"),
        disambiguation_test: localized("由 actor/role 与责任范围识别的对象归协作角色与责任；由 stage、branch、section、vote 或 synthesis membership 识别的对象归编排拓扑。", "An object identified by actor or role and responsibility scope belongs to collaboration roles and responsibility; one identified by stage, branch, section, vote, or synthesis membership belongs to orchestration topology.", "actor/role と responsibility scope で識別されれば collaboration role/responsibility、stage・branch・section・vote・synthesis membership で識別されれば orchestration topology に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-actors-delegation",
      },
    ],
  },
  "orchestration-composition": {
    purpose: localized("定义多个智能体、任务或步骤如何按链式、并行、分区、投票和综合模式组成可复用协作拓扑。", "Define reusable collaboration topologies that compose agents, tasks, or steps through chain, parallel, partition, voting, and synthesis patterns.", "複数のエージェント、タスク、手順を chain、parallel、partition、voting、synthesis パターンで構成する再利用可能な協働トポロジーを定義します。"),
    includes: [
      localized("CompositionPattern 及 Chain、Parallel、Partition、Voting、Synthesis 等专门模式。", "CompositionPattern and Chain, Parallel, Partition, Voting, Synthesis, and related specializations.", "CompositionPattern と Chain、Parallel、Partition、Voting、Synthesis などの特殊型。"),
      localized("OrchestrationTopology、ChainStage、ParallelBranch、SectionAssignment、VoteBallot、SynthesisInput、AggregationRule、Synthesis 和 SynthesisOutput。", "OrchestrationTopology, ChainStage, ParallelBranch, SectionAssignment, VoteBallot, SynthesisInput, AggregationRule, Synthesis, and SynthesisOutput.", "OrchestrationTopology、ChainStage、ParallelBranch、SectionAssignment、VoteBallot、SynthesisInput、AggregationRule、Synthesis、SynthesisOutput。"),
    ],
    excludes: [
      localized("Topology、Activity、Input、Output 和 Plan 不是 CompositionPattern 子类；它们通过组成、使用和产出关系连接。", "Topology, Activity, Input, Output, and Plan are not CompositionPattern subtypes; they connect through composition, use, and production relations.", "Topology、Activity、Input、Output、Plan は CompositionPattern 下位型ではなく、構成、使用、生成関係で接続します。"),
      localized("运行时 RoutingDecision 和 GateOutcome 归路由与门控，实际 RunAttempt 与 TraceEvent 归运行域。", "Runtime RoutingDecision and GateOutcome belong to routing and gates, while actual RunAttempt and TraceEvent values belong to runtime scopes.", "runtime RoutingDecision/GateOutcome は routing/gate、actual RunAttempt/TraceEvent は runtime scope に属します。"),
    ],
    owns_when: localized("当对象的身份由框架无关的协作模式、拓扑位置、分支或阶段组成，或聚合综合结构决定时，在此拥有。", "Own an object when its identity is determined by a framework-neutral collaboration pattern, topology position, branch or stage composition, or aggregation and synthesis structure.", "対象の同一性がフレームワーク非依存の協働パターン、トポロジー位置、分岐・段階構成、または集約・統合構造で決まる場合に所有します。"),
    references_when: localized("路由、执行或框架适配需要说明工作所在的协作拓扑和模式时，引用 CompositionPattern 与组件身份。", "Routing, execution, or framework adapters reference CompositionPattern and component identity when locating work in a collaboration topology.", "経路制御、実行、フレームワーク適配が作業の協働トポロジーとパターンを示すとき、CompositionPattern と構成要素同一性を参照します。"),
    competency_questions: [
      {
        semantic_key: "orchestration-composition.topology-expansion",
        related_module_ids: ["orchestration-routing-control"],
        questions: localized("一个组合模式展开后有哪些有序阶段、并行分支或投票与综合组件，入口和汇合点在哪里？", "After expanding a composition pattern, which ordered stages, parallel branches, voting or synthesis components exist, and where are entry and join points?", "composition pattern 展開後にどの ordered stage、parallel branch、voting/synthesis component があり、entry/join point はどこですか。"),
        query: "Traverse OrchestrationTopology composition relations and pattern-specific order, branch, vote, and join constraints.",
        expected_assertion: "The topology has explicit entry and completion semantics; every component belongs by composition rather than being classified as the pattern.",
      },
      {
        semantic_key: "orchestration-composition.pattern-validity",
        related_module_ids: ["orchestration-task-planning"],
        questions: localized("某个拓扑是否满足所声明 Chain、Parallel、Voting 或 Synthesis 模式的结构约束？", "Does a topology satisfy the structural constraints of its declared Chain, Parallel, Voting, or Synthesis pattern?", "あるトポロジーは宣言された Chain、Parallel、Voting、Synthesis パターンの構造制約を満たしますか。"),
        query: "Select constraints for the declared CompositionPattern and validate component cardinality, ordering, branch independence, and join behavior.",
        expected_assertion: "Validity reports each failed structural invariant; having similarly named components alone does not establish the pattern.",
      },
      {
        semantic_key: "orchestration-composition.synthesis-provenance",
        related_module_ids: ["runtime-artifacts"],
        questions: localized("综合输出由哪些分支输入、投票或聚合活动产生，能否追溯每项贡献？", "Which branch inputs, votes, or aggregation activities produced a synthesis output, and can every contribution be traced?", "統合出力はどの分岐入力、投票、集約活動から生成され、各寄与を追跡できますか。"),
        query: "Trace SynthesisOutput through producing activity to ordered or weighted inputs, votes, and source branch components.",
        expected_assertion: "Every output segment or decision cites contributing inputs and aggregation rule; topology does not claim ownership of runtime artifacts.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "orchestration-routing-control",
        decision: "owns",
        owner_module_id: "orchestration-composition",
        rationale: localized("协作组合拥有可复用模式和可能路径拓扑；路由与门控拥有在特定运行状态下对这些路径的实际选择与阻断。", "Collaboration composition owns reusable patterns and possible-path topology; routing and gates own actual selection and blocking of those paths in a runtime state.", "協働構成は再利用可能なパターンと可能経路トポロジーを所有し、経路制御とゲートは特定実行状態での実際の経路選択・遮断を所有します。"),
      },
      {
        other_module_id: "orchestration-task-planning",
        decision: "owns",
        owner_module_id: "orchestration-composition",
        rationale: localized("编排拓扑拥有 Chain、ParallelBranch、Sectioning、Voting、Synthesis 及其组成顺序；目标与任务规划拥有 Goal、Task、WorkItem、约束、依赖和完成判据。", "Orchestration topology owns Chain, ParallelBranch, Sectioning, Voting, Synthesis, and composition order; goals and task planning own Goal, Task, WorkItem, constraints, dependencies, and completion criteria.", "orchestration topology は Chain、ParallelBranch、Sectioning、Voting、Synthesis と composition order を所有し、goal/task planning は Goal、Task、WorkItem、constraint、dependency、completion criterion を所有します。"),
      },
      {
        other_module_id: "runtime-artifacts",
        decision: "owns",
        owner_module_id: "orchestration-composition",
        rationale: localized("编排拓扑拥有 SynthesisPlan、Input membership、aggregation/voting rule 和合成活动结构；运行产物拥有这些活动最终生成的 Report、Patch、Graph 或 Schema 身份。", "Orchestration topology owns SynthesisPlan, input membership, aggregation or voting rule, and synthesis activity structure; runtime artifacts own the Report, Patch, Graph, or Schema identity ultimately produced.", "orchestration topology は SynthesisPlan、input membership、aggregation/voting rule、synthesis activity structure を所有し、runtime artifact は最終生成 Report、Patch、Graph、Schema identity を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "orchestration-routing-control",
        semantic_area: localized("结构分支与运行分支决定", "Structural branches versus runtime branch decisions", "構造分岐と実行時分岐判断"),
        overlap_reason: localized("两者都出现分支目标，但 ParallelBranch 是可复用拓扑组件，RoutingDecision 是一次条件评估的结果。", "Both mention branch targets, but ParallelBranch is a reusable topology component while RoutingDecision is the result of one condition evaluation.", "どちらも branch target を扱いますが、ParallelBranch は reusable topology component、RoutingDecision は一 condition evaluation の result です。"),
        disambiguation_test: localized("若无运行输入仍能画出该边，则归协作组合；若必须代入当前状态、策略和证据才确定边，则归路由与门控。", "If an edge can be drawn without runtime inputs, it belongs to collaboration composition; if current state, policy, and evidence are required to determine it, it belongs to routing and gates.", "実行入力なしでも辺を描けるなら協働構成、現在状態、方針、証拠を代入して初めて決まるなら経路制御とゲートに属します。"),
        result: "distinct",
        owner_module_id: "orchestration-composition",
      },
      {
        other_module_id: "orchestration-task-planning",
        semantic_area: localized("工作结构拓扑与任务规范", "Work-structure topology versus task specification", "work structure topology と task specification"),
        overlap_reason: localized("Task 可在不同拓扑中执行，同一拓扑模式也可编排不同 Task；组成成员关系不替代任务目标和完成条件。", "A Task can run in different topologies and one topology pattern can orchestrate different Tasks; composition membership does not replace task goals or completion conditions.", "Task は異なる topology で実行でき、一 topology pattern は異なる Task を orchestrate でき、composition membership は task goal/completion condition を置換しません。"),
        disambiguation_test: localized("由目标、task identity、constraint 和 criterion 识别的对象归任务规划；由 stage、branch、section、ballot 或 synthesis membership 识别的对象归编排拓扑。", "An object identified by goal, task identity, constraint, and criterion belongs to task planning; one identified by stage, branch, section, ballot, or synthesis membership belongs to orchestration topology.", "goal・task identity・constraint・criterion で識別されれば task planning、stage・branch・section・ballot・synthesis membership で識別されれば orchestration topology に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-composition",
      },
      {
        other_module_id: "runtime-artifacts",
        semantic_area: localized("合成编排过程与合成产物", "Synthesis orchestration versus synthesized artifact", "synthesis orchestration と synthesized artifact"),
        overlap_reason: localized("一个 Synthesis 可产生一个或多个 Artifact，产物生成后可独立版本化、存储和评审而不改变原拓扑。", "One Synthesis can produce one or more Artifacts, which can be versioned, stored, and reviewed independently without changing the topology.", "一 Synthesis は一つ以上 Artifact を生成でき、artifact は topology を変えず独立 versioning・storage・review できます。"),
        disambiguation_test: localized("由输入成员、stage、aggregation rule 和 synthesis plan 识别的对象归编排拓扑；由 artifact id、digest、version 和 producer 识别的对象归运行产物。", "An object identified by input membership, stage, aggregation rule, and synthesis plan belongs to orchestration topology; one identified by artifact ID, digest, version, and producer belongs to runtime artifacts.", "input membership・stage・aggregation rule・synthesis plan で識別されれば orchestration topology、artifact id・digest・version・producer で識別されれば runtime artifact に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-composition",
      },
    ],
  },
  "orchestration-delegation-handoff": {
    purpose: localized("管理工作者选择、委派约定及控制或责任移交的完整过程，使发起、接受、撤销和完成均可追踪。", "Manage worker selection, delegation agreements, and transfer of control or responsibility as complete processes whose initiation, acceptance, revocation, and completion are traceable.", "作業者選択、委任合意、制御・責任の引き継ぎを完全な過程として管理し、開始、受諾、取消、完了を追跡可能にします。"),
    includes: [
      localized("DelegationProcess、HandoffProcess、委派阶段、委派事件、委派结果、契约与约定。", "DelegationProcess, HandoffProcess, delegation phases, events, outcomes, contracts, and agreements.", "DelegationProcess、HandoffProcess、委任段階、イベント、結果、契約、合意。"),
      localized("WorkerSelection、WorkerPool、WorkerRole、可用性评估、能力匹配、选择决定和工作分配阶段。", "WorkerSelection, WorkerPool, WorkerRole, availability assessment, capability matching, selection-decision, and worker-assignment phases.", "WorkerSelection、WorkerPool、WorkerRole、可用性評価、能力照合、選択判断、作業割当段階。"),
    ],
    excludes: [
      localized("协作角色类型与长期 ActorRoleBinding 归协同角色；委派只引用发起者和接受者当时承担的角色。", "Coordination-role types and longer-lived ActorRoleBinding values belong to coordination roles; delegation only references roles borne by initiator and recipient at that time.", "coordination-role type と longer-lived ActorRoleBinding は coordination role に属し、delegation は initiator/recipient が当時担う role だけを参照します。"),
      localized("工具 Invocation 由能力调用语义决定；把 Agent 当作 Tool 调用不自动等于 Delegation。", "Tool Invocation is determined by capability-call semantics; invoking an agent as a Tool is not automatically Delegation.", "ツール Invocation は能力呼出し意味で決まり、Agent を Tool として呼び出しても自動的に Delegation にはなりません。"),
    ],
    owns_when: localized("当对象的身份由一次工作者选择、委派契约、发起接受撤销完成阶段，或控制和责任的转移决定时，在此拥有。", "Own an object when its identity is determined by one worker selection, delegation contract, initiation/acceptance/revocation/completion phase, or transfer of control or responsibility.", "対象の同一性が一回の作業者選択、委任契約、開始・受諾・取消・完了段階、または制御・責任移転で決まる場合に所有します。"),
    references_when: localized("计划、运行、角色或安全范围需要说明一项工作如何被交给另一主体时，引用委派过程和当前契约状态。", "Planning, runtime, role, or safety scopes reference the delegation process and current contract state when explaining how work was assigned to another actor.", "計画、実行、役割、安全範囲が作業を別主体へ渡した経緯を示すとき、委任過程と現在契約状態を参照します。"),
    competency_questions: [
      {
        semantic_key: "orchestration-delegation-handoff.process-state",
        related_module_ids: ["orchestration-actors-delegation"],
        questions: localized("某次委派当前处于发起、接受、撤销还是完成阶段，哪条事件使状态生效？", "Is a delegation currently initiated, accepted, revoked, or completed, and which event made that state effective?", "ある委任は現在、開始、受諾、取消、完了のどの段階で、どのイベントが状態を有効にしましたか。"),
        query: "Order DelegationEvent values by process sequence, validate allowed phase transitions, and return the latest effective non-superseded state.",
        expected_assertion: "Exactly one current state follows a valid transition path, with the causative event and actor recorded.",
      },
      {
        semantic_key: "orchestration-delegation-handoff.worker-selection",
        related_module_ids: ["tool-discovery-selection"],
        questions: localized("候选工作者经过哪些可用性、能力和约束判断后被选择，落选原因是什么？", "Through which availability, capability, and constraint assessments was a worker selected, and why were others rejected?", "候補作業者はどの可用性、能力、制約判断を経て選ばれ、他候補の不採用理由は何ですか。"),
        query: "Trace WorkerSelectionProcess from WorkerPool through WorkerAvailabilityAssessment, WorkerCapabilityMatching, WorkerSelectionDecisionActivity, and WorkerAssignment.",
        expected_assertion: "The selected WorkerRole satisfies every mandatory condition; every rejected role has a stage-specific reason.",
      },
      {
        semantic_key: "orchestration-delegation-handoff.responsibility-transfer",
        related_module_ids: ["runtime-actors"],
        questions: localized("移交前后谁拥有任务控制权和结果责任，未被移交的权限或预算有哪些？", "Who holds task control and outcome responsibility before and after handoff, and which permissions or budgets were not transferred?", "handoff 前後で誰が task control/outcome responsibility を持ち、transfer されない permission/budget は何ですか。"),
        query: "Compare DelegationContract scope with pre- and post-handoff responsibility bindings, explicitly listing excluded authority, budget, and context fields.",
        expected_assertion: "Transferred and retained responsibilities are disjoint and complete for the contract scope; no permission is inferred from task transfer.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "orchestration-actors-delegation",
        decision: "owns",
        owner_module_id: "orchestration-delegation-handoff",
        rationale: localized("委派与移交拥有选择、契约、阶段、事件和责任转移；协同角色拥有可复用职责类型与角色绑定。", "Delegation and handoff own selection, contracts, phases, events, and responsibility transfer; coordination roles own reusable responsibility types and role bindings.", "委任と引き継ぎは選択、契約、段階、イベント、責任移転を所有し、協調ロールは再利用可能な責務型と役割バインディングを所有します。"),
      },
      {
        other_module_id: "tool-invocation-execution",
        decision: "owns",
        owner_module_id: "orchestration-delegation-handoff",
        rationale: localized(
          "委派与移交拥有转交有范围工作、权限、上下文或控制责任的过程与契约；调用与执行拥有管理者保留控制和答案责任的 AgentAsToolInvocation。",
          "Delegation and handoff own processes and contracts transferring scoped work, authority, context, or control responsibility; invocation and execution own AgentAsToolInvocation when the manager retains control and answer responsibility.",
          "委任と引き継ぎは範囲付き作業・権限・コンテキスト・制御責任を移すプロセスと契約を所有し、呼び出しと実行は管理者が制御と回答責任を保持する AgentAsToolInvocation を所有します。",
        ),
      },
      {
        other_module_id: "tool-discovery-selection",
        decision: "owns",
        owner_module_id: "orchestration-delegation-handoff",
        rationale: localized("委派与移交拥有 WorkerPool、availability assessment、capability matching、WorkerSelectionProcess、DelegationContract 和责任转移；能力发现拥有工具候选、匹配与 ToolSelectionDecision。", "Delegation and handoff own WorkerPool, availability assessment, capability matching, WorkerSelectionProcess, DelegationContract, and responsibility transfer; capability discovery owns tool candidates, matching, and ToolSelectionDecision.", "delegation/handoff は WorkerPool、availability assessment、capability matching、WorkerSelectionProcess、DelegationContract、responsibility transfer を所有し、capability discovery は tool candidate、matching、ToolSelectionDecision を所有します。"),
      },
      {
        other_module_id: "runtime-actors",
        decision: "owns",
        owner_module_id: "orchestration-delegation-handoff",
        rationale: localized("委派与移交拥有一次协作中的候选评估、选择阶段、契约、预算、上下文界限和移交事件；运行参与者拥有被选择 Actor 的稳定身份与能力绑定。", "Delegation and handoff own candidate assessment, selection phases, contract, budget, context bounds, and handoff events in one collaboration; runtime actors own stable identity and capability bindings of selected Actors.", "delegation/handoff は一 collaboration の candidate assessment、selection phase、contract、budget、context bound、handoff event を所有し、runtime actor は selected Actor の stable identity/capability binding を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "orchestration-actors-delegation",
        semantic_area: localized("委派接受者与协作角色承担者", "Delegation recipients versus coordination-role bearers", "委任受任者と協調役割担当者"),
        overlap_reason: localized("同一 AgentActor 可承担 WorkerRole 并成为 Handoff 的控制接收方，但前者是可复用职责绑定，后者是一次移交关系中的运行主体端点。", "The same AgentActor may bear WorkerRole and receive control through Handoff, but the former is a reusable responsibility binding while the latter is a runtime-actor endpoint of one transfer relation.", "同じ AgentActor は WorkerRole を担い Handoff で制御を受領できますが、前者は再利用可能な責務バインディング、後者は一回の移転関係における実行アクター端点です。"),
        disambiguation_test: localized("若只需角色、作用域和有效期即可识别，则归协同角色；若必须有 delegation id、契约和阶段事件，则归委派与移交。", "If role, scope, and validity interval suffice for identity, it belongs to coordination roles; if delegation ID, contract, and phase event are required, it belongs to delegation and handoff.", "役割、作用域、有効期間だけで識別できれば協調ロール、delegation id、契約、段階イベントが必要なら委任と引き継ぎに属します。"),
        result: "distinct",
        owner_module_id: "orchestration-delegation-handoff",
      },
      {
        other_module_id: "tool-invocation-execution",
        semantic_area: localized(
          "责任委派与智能体工具调用",
          "Responsibility delegation versus agent-as-tool invocation",
          "責任委任とエージェントツール呼び出し",
        ),
        overlap_reason: localized(
          "两者都可能请求另一智能体执行工作，但 DelegationProcess 通过契约移交有范围的工作、权限、上下文或责任；AgentAsToolInvocation 由能力调用语义识别并保留调用者控制和答案责任。",
          "Both may request work from another agent, but DelegationProcess transfers scoped work, authority, context, or responsibility under a contract; AgentAsToolInvocation is identified by capability-call semantics and retains caller control and answer responsibility.",
          "どちらも別エージェントへ作業を求め得ますが、DelegationProcess は契約に基づき範囲付き作業・権限・コンテキスト・責任を移し、AgentAsToolInvocation は能力呼び出し意味で識別され呼び出し元の制御と回答責任を保持します。",
        ),
        disambiguation_test: localized(
          "若存在委派契约并转交工作范围、权限或责任，归委派与移交；若调用者保留最终答案和会话控制且接收 ToolResult，归调用与执行。",
          "If a delegation contract transfers work scope, authority, or responsibility, it belongs to delegation and handoff; if the caller retains final-answer and conversation control and receives a ToolResult, it belongs to invocation and execution.",
          "委任契約が作業範囲・権限・責任を移すなら委任と引き継ぎ、呼び出し元が最終回答と会話制御を保持し ToolResult を受け取るなら呼び出しと実行に属します。",
        ),
        result: "distinct",
        owner_module_id: "orchestration-delegation-handoff",
      },
      {
        other_module_id: "tool-discovery-selection",
        semantic_area: localized("工作者选择与工具选择", "Worker selection versus tool selection", "worker selection と tool selection"),
        overlap_reason: localized("两者都评估能力与可用性，但选择 Worker 会建立职责、上下文与授权边界；选择 Tool 只选择可调用能力，不转移责任。", "Both assess capability and availability, but selecting a Worker establishes responsibility, context, and authority boundaries; selecting a Tool chooses callable capability without transferring responsibility.", "どちらも capability/availability を評価しますが、Worker selection は responsibility・context・authority boundary を確立し、Tool selection は callable capability を選ぶだけで responsibility を移しません。"),
        disambiguation_test: localized("若结果需要 DelegationContract、WorkerRole 或 ResponsibilityTransfer，归委派与移交；若结果是 ToolCandidate/ToolSelectionDecision，归能力发现。", "If the result requires DelegationContract, WorkerRole, or ResponsibilityTransfer, it belongs to delegation and handoff; if it is ToolCandidate or ToolSelectionDecision, it belongs to capability discovery.", "結果に DelegationContract・WorkerRole・ResponsibilityTransfer が必要なら delegation/handoff、ToolCandidate/ToolSelectionDecision なら capability discovery に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-delegation-handoff",
      },
      {
        other_module_id: "runtime-actors",
        semantic_area: localized("委派过程事实与参与者身份", "Delegation-process facts versus participant identity", "delegation process fact と participant identity"),
        overlap_reason: localized("同一 Actor 可参与多次委派并获得不同 authority/context，Actor 的身份不由某次 DelegationProcess 创建。", "The same Actor can participate in many delegations with different authority and context; Actor identity is not created by one DelegationProcess.", "同じ Actor は異なる authority/context で複数 delegation に参加でき、Actor identity は一 DelegationProcess が作るものではありません。"),
        disambiguation_test: localized("由 actor id、provider 和长期 capability binding 识别的对象归运行参与者；由 delegation id、phase、contract、budget 或 transfer 识别的对象归委派与移交。", "An object identified by actor ID, provider, and durable capability binding belongs to runtime actors; one identified by delegation ID, phase, contract, budget, or transfer belongs to delegation and handoff.", "actor id・provider・durable capability binding で識別されれば runtime actor、delegation id・phase・contract・budget・transfer で識別されれば delegation/handoff に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-delegation-handoff",
      },
    ],
  },
  "orchestration-evaluation": {
    purpose: localized("编排评估、反馈、修订和重试之间的控制顺序，使改进循环具有明确入口、停止条件和尝试血缘。", "Orchestrate the control order among evaluation, feedback, revision, and retry so improvement loops have explicit entry, stop conditions, and attempt lineage.", "評価、フィードバック、修訂、再試行の制御順序を編成し、改善ループに明確な入口、停止条件、試行系譜を与えます。"),
    includes: [
      localized("ImprovementLoop、EvaluatorOptimizerLoop、ReviewRevisionLoop 及循环入口、阶段和停止条件。", "ImprovementLoop, EvaluatorOptimizerLoop, ReviewRevisionLoop, and loop entries, phases, and stop conditions.", "ImprovementLoop、EvaluatorOptimizerLoop、ReviewRevisionLoop、およびループ入口、段階、停止条件。"),
      localized("EvaluationAttempt、RevisionAttempt、审查协调、调度、路由、重试和阶段间控制血缘。", "EvaluationAttempt, RevisionAttempt, review coordination, dispatch, routing, retry, and inter-phase control lineage.", "EvaluationAttempt、RevisionAttempt、レビュー調整、dispatch、routing、retry、段階間制御系譜。"),
    ],
    excludes: [
      localized("Review、Finding、Correction 和 Reevaluation 的内容语义归审查与纠正。", "The content semantics of Review, Finding, Correction, and Reevaluation belong to review and correction.", "Review、Finding、Correction、Reevaluation の内容意味はレビューと修正に属します。"),
      localized("Metric、Measurement、Score 和 Criterion 归评估与度量；本范围只引用结果来控制下一阶段。", "Metric, Measurement, Score, and Criterion belong to evaluation and measurement; this scope only references results to control the next phase.", "Metric、Measurement、Score、Criterion は評価と測定に属し、この範囲は次段階制御のため結果を参照するだけです。"),
    ],
    owns_when: localized("当对象的身份由改进循环中的阶段位置、评估与修订尝试的调度、重试或停止控制决定时，在此拥有。", "Own an object when its identity is determined by phase position in an improvement loop, scheduling of evaluation and revision attempts, retry, or stop control.", "対象の同一性が改善ループ内の段階位置、評価・修訂試行の調度、再試行、停止制御で決まる場合に所有します。"),
    references_when: localized("评估、审查、优化或运行需要说明某项活动为何在循环中的此时发生时，引用循环、阶段和控制尝试。", "Evaluation, review, optimization, or runtime references loop, phase, and control attempt when explaining why an activity occurs at that point in an improvement cycle.", "評価、レビュー、最適化、実行が活動のループ内発生理由を示すとき、ループ、段階、制御試行を参照します。"),
    competency_questions: [
      {
        semantic_key: "orchestration-evaluation.phase-order",
        related_module_ids: ["feedback-metrics-evaluation"],
        questions: localized("某次改进循环按何种顺序调度评估、反馈、修订和复验，当前位于哪一阶段？", "In what order does an improvement loop schedule evaluation, feedback, revision, and reevaluation, and which phase is current?", "改善ループは評価、フィードバック、修訂、再評価をどの順序で調度し、現在どの段階ですか。"),
        query: "Traverse loop phase and precedes relations, then resolve the latest completed and currently active control attempt.",
        expected_assertion: "The phase graph is acyclic and current position follows completed evidence; content objects remain references to their owners.",
      },
      {
        semantic_key: "orchestration-evaluation.retry-stop",
        related_module_ids: ["orchestration-routing-control"],
        questions: localized("评估未达标后应修订重试还是停止，次数、预算和改善幅度阈值如何共同决定？", "After evaluation misses target, should control revise and retry or stop, and how do count, budget, and improvement-delta thresholds decide?", "評価未達後に修訂・再試行か停止か、回数、予算、改善幅閾値はどう共同判断しますか。"),
        query: "Apply loop retry and stop conditions to attempt count, remaining budget, latest criterion verdict, and improvement delta.",
        expected_assertion: "A new revision attempt is scheduled only when every continuation condition holds; otherwise the stop reason is explicit.",
      },
      {
        semantic_key: "orchestration-evaluation.content-reference",
        related_module_ids: ["feedback-review-optimization"],
        questions: localized("循环中的 EvaluationAttempt 和 RevisionAttempt 分别引用了哪些评估结果与 Finding，而没有复制其内容语义？", "Which evaluation results and Findings do EvaluationAttempt and RevisionAttempt reference without copying their content semantics?", "ループ内の EvaluationAttempt と RevisionAttempt はどの評価結果と Finding を内容意味の複製なしに参照しますか。"),
        query: "List control attempts and their references to EvaluationRun, verdict, Finding, correction request, and resulting runtime attempt.",
        expected_assertion: "Each referenced content object retains its feedback owner; orchestration owns only order, dispatch, retry, and stop links.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "feedback-review-optimization",
        decision: "owns",
        owner_module_id: "orchestration-evaluation",
        rationale: localized("改进循环编排拥有评估—审查—修订的阶段顺序与重试控制；审查与纠正拥有 Review、Finding、Correction 和复验结论。", "Improvement-loop orchestration owns phase order and retry control across evaluation, review, and revision; review and correction own Review, Finding, Correction, and reevaluation conclusions.", "改善ループ編成は評価、レビュー、修訂の段階順序と再試行制御を所有し、レビューと修正は Review、Finding、Correction、再評価結論を所有します。"),
      },
      {
        other_module_id: "feedback-metrics-evaluation",
        decision: "owns",
        owner_module_id: "orchestration-evaluation",
        rationale: localized("评估改进循环拥有 EvaluationAttempt、RevisionAttempt、FeedbackRouting、重试血缘和停止顺序；评估与度量拥有 EvaluationSpecification、Run、Metric、Measurement 和 criterion verdict。", "Evaluation improvement loops own EvaluationAttempt, RevisionAttempt, FeedbackRouting, retry lineage, and stop order; evaluation and measurement own EvaluationSpecification, Run, Metric, Measurement, and criterion verdict.", "evaluation improvement loop は EvaluationAttempt、RevisionAttempt、FeedbackRouting、retry lineage、stop order を所有し、evaluation/measurement は EvaluationSpecification、Run、Metric、Measurement、criterion verdict を所有します。"),
      },
      {
        other_module_id: "orchestration-routing-control",
        decision: "owns",
        owner_module_id: "orchestration-evaluation",
        rationale: localized("评估改进循环拥有 evaluator-optimizer 专用的评估、反馈、修订与再评顺序；路由与门拥有可用于任意流程的 Route、Gate、RetryPolicy 和 StopCondition。", "Evaluation improvement loops own evaluator-optimizer-specific order of evaluation, feedback, revision, and reevaluation; routing and gates own Route, Gate, RetryPolicy, and StopCondition reusable in any flow.", "evaluation improvement loop は evaluator-optimizer 固有の evaluation・feedback・revision・reevaluation order を所有し、routing/gate は任意 flow で再利用する Route、Gate、RetryPolicy、StopCondition を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "feedback-review-optimization",
        semantic_area: localized("审查调度与审查内容", "Review scheduling versus review content", "レビュー調度とレビュー内容"),
        overlap_reason: localized("两者都出现 review 和 revision，但编排回答何时调用与是否重试，反馈回答发现了什么以及如何纠正。", "Both mention review and revision, but orchestration answers when to invoke and whether to retry while feedback answers what was found and how it was corrected.", "どちらも review と revision を扱いますが、編成は呼出時期と再試行、フィードバックは発見内容と修正方法を答えます。"),
        disambiguation_test: localized("若改变阶段顺序会改变对象身份而 Finding 内容不变，则归改进循环编排；若身份由被审对象、证据和处置决定，则归审查与纠正。", "If changing phase order changes object identity while Finding content stays the same, it belongs to improvement-loop orchestration; if identity comes from reviewed subject, evidence, and disposition, it belongs to review and correction.", "段階順序の変更で対象同一性が変わり Finding 内容が不変なら改善ループ編成、同一性がレビュー対象、証拠、処置で決まるならレビューと修正に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-evaluation",
      },
      {
        other_module_id: "feedback-metrics-evaluation",
        semantic_area: localized("评估循环控制与评估语义", "Evaluation-loop control versus evaluation semantics", "evaluation loop control と evaluation semantics"),
        overlap_reason: localized("循环决定何时评估、修订和停止，但不重新定义 metric、measurement 值或 pass threshold；一次 EvaluationRun 也可脱离改进循环存在。", "The loop decides when to evaluate, revise, and stop but does not redefine metrics, measurement values, or pass thresholds; an EvaluationRun can also exist outside an improvement loop.", "loop は evaluate/revise/stop の時機を決めますが metric・measurement value・pass threshold を再定義せず、EvaluationRun は improvement loop 外でも存在できます。"),
        disambiguation_test: localized("由 metric、dataset slice、subject 和 measurement 识别的对象归评估与度量；由 loop id、attempt order、feedback route 和 stop lineage 识别的对象归评估改进循环。", "An object identified by metric, dataset slice, subject, and measurement belongs to evaluation and measurement; one identified by loop ID, attempt order, feedback route, and stop lineage belongs to evaluation improvement loops.", "metric・dataset slice・subject・measurement で識別されれば evaluation/measurement、loop id・attempt order・feedback route・stop lineage で識別されれば evaluation improvement loop に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-evaluation",
      },
      {
        other_module_id: "orchestration-routing-control",
        semantic_area: localized("评估专用循环与通用控制流", "Evaluation-specific loop versus generic control flow", "evaluation-specific loop と generic control flow"),
        overlap_reason: localized("改进循环可由通用 Gate 和 StopCondition 治理，但 EvaluationAttempt 与 RevisionAttempt 的角色和反馈血缘只在评估循环中成立。", "An improvement loop can be governed by generic Gate and StopCondition, but EvaluationAttempt and RevisionAttempt roles and feedback lineage exist only in the evaluation loop.", "improvement loop は generic Gate/StopCondition で govern できますが、EvaluationAttempt/RevisionAttempt role と feedback lineage は evaluation loop 固有です。"),
        disambiguation_test: localized("去掉 evaluation/revision 语义仍可复用的控制对象归路由与门；必须依赖评估尝试、反馈与修订次序的对象归评估改进循环。", "A control object reusable after removing evaluation or revision semantics belongs to routing and gates; one requiring evaluation attempts, feedback, and revision order belongs to evaluation improvement loops.", "evaluation/revision 意味を除いても再利用できる control object は routing/gate、evaluation attempt・feedback・revision order が必要なら evaluation improvement loop に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-evaluation",
      },
    ],
  },
  "orchestration-routing-control": {
    purpose: localized("根据条件、策略和运行证据选择后续路径，并用控制门、重试与停止条件约束流程推进。", "Select subsequent paths from conditions, policies, and runtime evidence, constraining progress through control gates, retry, and stop conditions.", "条件、方針、実行証拠から後続経路を選択し、制御ゲート、再試行、停止条件で進行を制約します。"),
    includes: [
      localized("RoutingSpecification、RoutingPolicy、RetryPolicy、StopCondition 和 RoutingTarget。", "RoutingSpecification, RoutingPolicy, RetryPolicy, StopCondition, and RoutingTarget.", "RoutingSpecification、RoutingPolicy、RetryPolicy、StopCondition、RoutingTarget。"),
      localized("条件评估、RoutingDecision、Route、ControlGate、GateCondition、GateOutcome 与下游治理关系。", "Condition evaluation, RoutingDecision, Route, ControlGate, GateCondition, GateOutcome, and governance of downstream operations.", "条件評価、RoutingDecision、Route、ControlGate、GateCondition、GateOutcome、下流操作の統制関係。"),
    ],
    excludes: [
      localized("Chain、Parallel、Voting 与 Synthesis 的静态组合拓扑归协作组合；路由决定只选择拓扑中的下一条路径。", "Static Chain, Parallel, Voting, and Synthesis topologies belong to collaboration composition; a routing decision selects the next path within such topology.", "静的 Chain、Parallel、Voting、Synthesis トポロジーは協働構成に属し、経路判断はその中の次経路を選ぶだけです。"),
      localized("Retry 产生的新 RunAttempt 归执行尝试与结果；Stop 必须引用终止证据而不是把停止写成伪类。", "A new RunAttempt produced by retry belongs to execution attempts and outcomes; stop must reference termination evidence rather than becoming a pseudo-class.", "retry が生成する新 RunAttempt は execution attempt/outcome に属し、stop は pseudo-class ではなく termination evidence を参照します。"),
    ],
    owns_when: localized("当对象的身份由路由规约、条件评估、路径选择、门判定、重试或停止控制决定时，在此拥有。", "Own an object when its identity is determined by a routing specification, condition evaluation, path selection, gate judgment, retry, or stop control.", "対象の同一性が経路仕様、条件評価、経路選択、ゲート判断、再試行、停止制御で決まる場合に所有します。"),
    references_when: localized("计划、组合或运行执行需要说明为何选择、阻断、重试或停止某条路径时，引用规约和决定。", "Planning, composition, or runtime execution references specifications and decisions when explaining why a path was selected, blocked, retried, or stopped.", "計画、構成、実行が経路の選択、遮断、再試行、停止理由を示すとき、仕様と判断を参照します。"),
    competency_questions: [
      {
        semantic_key: "orchestration-routing-control.route-decision",
        related_module_ids: ["orchestration-composition"],
        questions: localized("给定当前状态与候选路径，哪项条件评估产生了哪个 RoutingDecision 和 RoutingTarget？", "Given current state and candidate paths, which condition evaluation produced which RoutingDecision and RoutingTarget?", "current state と candidate path が与えられたとき、どの condition evaluation がどの RoutingDecision/RoutingTarget を生成しましたか。"),
        query: "Evaluate RoutingSpecification conditions over the cited state, record evaluation evidence, and return the selected RoutingTarget.",
        expected_assertion: "One selected target follows the policy's tie and fallback rules; rejected paths retain their failed conditions.",
      },
      {
        semantic_key: "orchestration-routing-control.gate-outcome",
        related_module_ids: ["safety-permission-policy"],
        questions: localized("某个 ControlGate 为何允许、拒绝或暂缓下游操作，所需证据是否齐全？", "Why did a ControlGate allow, deny, or defer a downstream operation, and was all required evidence present?", "ControlGate が下流操作を許可、拒否、保留した理由と、必要証拠が揃っていたかを示せますか。"),
        query: "Resolve GateCondition values, evidence inputs, policy references, and resulting GateOutcome governing the downstream operation.",
        expected_assertion: "The outcome follows all mandatory conditions; missing evidence yields defer or deny as declared, never implicit allow.",
      },
      {
        semantic_key: "orchestration-routing-control.retry-stop",
        related_module_ids: ["runtime-execution-attempts"],
        questions: localized("一次失败后应重试还是停止，剩余次数、退避、终止证据和新 RunAttempt 如何确定？", "After a failure, should control retry or stop, and how are remaining count, backoff, termination evidence, and the new RunAttempt determined?", "failure 後に retry/stop のどちらで、remaining count・backoff・termination evidence・new RunAttempt はどう決まりますか。"),
        query: "Apply RetryPolicy and StopCondition to RunAttempt history, failure classification, RuntimeBudget, and elapsed time.",
        expected_assertion: "Retry creates a referenced new RunAttempt only when limits and stop conditions allow; otherwise RoutingDecision selects stop and cites conclusive termination evidence.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "orchestration-composition",
        decision: "owns",
        owner_module_id: "orchestration-routing-control",
        rationale: localized("路由与门控拥有运行时条件评估、路径决定和门结果；协作组合拥有可选择路径所在的模式、组件和拓扑。", "Routing and gates own runtime condition evaluation, path decisions, and gate outcomes; collaboration composition owns the patterns, components, and topology containing selectable paths.", "経路制御とゲートは実行時条件評価、経路判断、ゲート結果を所有し、協働構成は選択可能経路を含むパターン、構成要素、トポロジーを所有します。"),
      },
      {
        other_module_id: "safety-permission-policy",
        decision: "owns",
        owner_module_id: "orchestration-routing-control",
        rationale: localized("路由与门拥有基于运行状态选择分支、目标、重试或停止的 RoutingDecision/GateOutcome；权限与策略拥有对 subject-resource-action 的 allow/deny/defer 授权判断。", "Routing and gates own RoutingDecision or GateOutcome choosing branch, target, retry, or stop from runtime state; permission and policy own allow, deny, or defer authorization over subject-resource-action.", "routing/gate は runtime state から branch・target・retry・stop を選ぶ RoutingDecision/GateOutcome を所有し、permission/policy は subject-resource-action の allow/deny/defer authorization を所有します。"),
      },
      {
        other_module_id: "runtime-execution-attempts",
        decision: "owns",
        owner_module_id: "orchestration-routing-control",
        rationale: localized("路由与门拥有 RetryPolicy、RoutingCondition、Route、StopCondition 和基于证据作出的控制决定；执行尝试拥有这些决定实际创建或终止的 RunAttempt 及其结果。", "Routing and gates own RetryPolicy, RoutingCondition, Route, StopCondition, and evidence-based control decisions; execution attempts own the RunAttempt actually created or terminated by those decisions and its result.", "routing/gate は RetryPolicy、RoutingCondition、Route、StopCondition と evidence-based control decision を所有し、execution attempt はその decision が実際に作成・終了した RunAttempt と result を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "orchestration-composition",
        semantic_area: localized("路由选择与组合拓扑", "Route selection versus composition topology", "経路選択と構成トポロジー"),
        overlap_reason: localized("两者都连接步骤，但拓扑定义可能路径结构，路由决定在特定状态下实际走哪一条。", "Both connect steps, but topology defines possible path structure while a routing decision chooses the actual path for a particular state.", "どちらも手順を接続しますが、トポロジーは可能経路構造、経路判断は特定状態で実際に進む経路を決めます。"),
        disambiguation_test: localized("若删除运行状态和条件值后边仍作为协作结构存在，则归协作组合；若边的成立依赖一次条件评估，则归路由与门控。", "If an edge remains collaboration structure after removing runtime state and condition values, it belongs to collaboration composition; if existence depends on one condition evaluation, it belongs to routing and gates.", "実行状態と条件値を除いても辺が協働構造として残るなら協働構成、一回の条件評価に成立が依存するなら経路制御とゲートに属します。"),
        result: "distinct",
        owner_module_id: "orchestration-routing-control",
      },
      {
        other_module_id: "safety-permission-policy",
        semantic_area: localized("流程控制决定与授权决定", "Flow-control decision versus authorization decision", "flow-control decision と authorization decision"),
        overlap_reason: localized("PolicyDecision 可作为 GateCondition 的证据，但安全允许并不决定应走哪条业务分支，路由选择也不能授予权限。", "PolicyDecision can evidence a GateCondition, but safety allowance does not choose the business branch and routing cannot grant authority.", "PolicyDecision は GateCondition の evidence になりますが、安全 allow は business branch を選ばず routing は authority を grant できません。"),
        disambiguation_test: localized("若问题是主体是否被允许执行动作，归权限与策略；若问题是在已知条件下下一步去哪里、重试还是停止，归路由与门。", "If the question is whether a subject may perform an action, it belongs to permission and policy; if it is where execution goes next, retries, or stops under known conditions, it belongs to routing and gates.", "subject が action を許可されるかは permission/policy、既知 condition 下で次へどこに進み retry/stop するかは routing/gate に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-routing-control",
      },
      {
        other_module_id: "runtime-execution-attempts",
        semantic_area: localized("重试控制策略与重试尝试发生", "Retry-control policy versus retry-attempt occurrence", "retry control policy と retry attempt occurrence"),
        overlap_reason: localized("一个 RetryPolicy 可治理多次执行且某次决定可能不创建重试；RunAttempt 则有独立 attempt number、时间和预算消费。", "One RetryPolicy can govern many executions and a decision may create no retry; RunAttempt has independent attempt number, time, and budget consumption.", "一 RetryPolicy は複数 execution を govern し decision が retry を作らない場合もあり、RunAttempt は独自 attempt number・time・budget consumption を持ちます。"),
        disambiguation_test: localized("由 condition、policy version 和 decision evidence 识别的对象归路由与门；由 execution id、attempt number、retry lineage 和 result 识别的对象归执行尝试。", "An object identified by condition, policy version, and decision evidence belongs to routing and gates; one identified by execution ID, attempt number, retry lineage, and result belongs to execution attempts.", "condition・policy version・decision evidence で識別されれば routing/gate、execution id・attempt number・retry lineage・result で識別されれば execution attempt に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-routing-control",
      },
    ],
  },
  "orchestration-task-planning": {
    purpose: localized("把意图转化为可验证目标、工作规约、计划、任务和步骤，并显式表达前置条件、依赖与完成标准。", "Turn intent into verifiable goals, work specifications, plans, tasks, and steps with explicit preconditions, dependencies, and completion criteria.", "意図を検証可能な目標、作業仕様、計画、タスク、手順へ変換し、前提条件、依存関係、完了基準を明示します。"),
    includes: [
      localized("Intent、Objective、Goal、工作规约，以及目标的成功条件、约束和优先级。", "Intent, Objective, Goal, work specifications, and goal success conditions, constraints, and priorities.", "Intent、Objective、Goal、作業仕様、および目標の成功条件、制約、優先度。"),
      localized("TaskPlan、Task、TaskStep、前置条件、完成标准、依赖和计划分解关系。", "TaskPlan, Task, TaskStep, preconditions, completion criteria, dependencies, and plan-decomposition relations.", "TaskPlan、Task、TaskStep、前提条件、完了基準、依存関係、計画分解関係。"),
    ],
    excludes: [
      localized("执行中的 Execution、RunAttempt、RunResult 和 RunOutcome 归执行尝试与结果；计划描述应做什么，不记录已发生的尝试。", "In-progress Execution, RunAttempt, RunResult, and RunOutcome belong to execution attempts and outcomes; a plan states what should be done rather than recording an occurrence.", "実行中の Execution、RunAttempt、RunResult、RunOutcome は execution attempt/outcome に属し、plan は実施すべき内容を示して発生済み attempt を記録しません。"),
      localized("Route、Gate、Retry 和 Stop 的动态控制逻辑归路由与门控；任务依赖不是运行路由决定。", "Dynamic Route, Gate, Retry, and Stop control belongs to routing and gates; a task dependency is not a runtime routing decision.", "動的 Route、Gate、Retry、Stop 制御は経路制御とゲートに属し、タスク依存関係は実行時経路判断ではありません。"),
    ],
    owns_when: localized("当对象的身份由尚待实现的目标、工作规约、计划分解、任务定义或完成条件决定时，在此拥有。", "Own an object when its identity is determined by a goal to be realized, a work specification, plan decomposition, task definition, or completion condition.", "対象の同一性が未実現の目標、作業仕様、計画分解、タスク定義、完了条件で決まる場合に所有します。"),
    references_when: localized("执行、路由、委派或评估需要说明所实现的目标和任务定义时，引用计划实体而不把运行状态写回定义。", "Execution, routing, delegation, or evaluation references planning entities when identifying the goal and task definition being realized without writing runtime state into the definition.", "実行、経路制御、委任、評価が実現対象の目標とタスク定義を示すとき、実行状態を定義へ書き戻さず計画実体を参照します。"),
    competency_questions: [
      {
        semantic_key: "orchestration-task-planning.goal-realization",
        related_module_ids: ["runtime-execution-attempts"],
        questions: localized("某个 Goal 由哪份工作规约定义，并由哪些 TaskPlan、Task 和 TaskStep 逐层实现？", "Which work specification defines a Goal, and which TaskPlan, Task, and TaskStep values realize it by decomposition?", "ある Goal はどの作業仕様で定義され、どの TaskPlan、Task、TaskStep によって段階的に実現されますか。"),
        query: "Traverse Goal specified_by WorkSpecification and inverse realizes from TaskPlan, then ordered contains relations to Task and TaskStep.",
        expected_assertion: "The plan decomposition reaches executable steps without classifying plan, task, and step as one inheritance chain.",
      },
      {
        semantic_key: "orchestration-task-planning.readiness",
        related_module_ids: ["orchestration-routing-control"],
        questions: localized("一个 Task 在什么前置条件和依赖满足后可开始，哪些完成标准决定它结束？", "After which preconditions and dependencies may a Task start, and which completion criteria determine that it is done?", "Task はどの前提条件と依存関係が満たされた後に開始でき、どの完了基準で終了が決まりますか。"),
        query: "Evaluate declared task preconditions and predecessor completion facts, and separately return completion criteria for later verification.",
        expected_assertion: "Ready requires every mandatory precondition and dependency; completion criteria are not misused as readiness conditions.",
      },
      {
        semantic_key: "orchestration-task-planning.definition-versus-attempt",
        related_module_ids: ["runtime-execution-attempts"],
        questions: localized("查询中的对象是可复用 Task 定义还是某次 RunAttempt，二者如何通过 realizes 关联？", "Is a queried object a reusable Task definition or one RunAttempt, and how are they linked through realization?", "照会対象は reusable Task definition ですか、それとも一回の RunAttempt で、両者は realization でどう結ばれますか。"),
        query: "Inspect identity keys and relation direction: Task has specification identity; RunAttempt has execution, sequence, start time, and realizes a Task.",
        expected_assertion: "Definitions persist across runs, attempts are occurrence-bound, and no RunAttempt is_a Task edge is accepted.",
      },
    ],
    boundary_decisions: [
      {
        other_module_id: "runtime-execution-attempts",
        decision: "owns",
        owner_module_id: "orchestration-task-planning",
        rationale: localized("目标与任务规划拥有规范性的目标、计划和任务定义；执行尝试与结果拥有在某次运行中实现它们的 Execution 与 Attempt。", "Goals and task planning own normative goals, plans, and task definitions; execution attempts and outcomes own the Execution and Attempt occurrences that realize them in a run.", "目標とタスク計画は規範的な目標、計画、タスク定義を所有し、実行試行と結果は実行内でそれらを実現する Execution と Attempt を所有します。"),
      },
      {
        other_module_id: "orchestration-routing-control",
        decision: "owns",
        owner_module_id: "orchestration-task-planning",
        rationale: localized("目标与任务规划拥有 Goal、Task、TaskPlan、Step、Dependency、Constraint 和完成判据；路由与门拥有执行时对下一目标、分支、重试或停止的决定。", "Goals and task planning own Goal, Task, TaskPlan, Step, Dependency, Constraint, and completion criteria; routing and gates own runtime decisions about the next target, branch, retry, or stop.", "goal/task planning は Goal、Task、TaskPlan、Step、Dependency、Constraint、completion criterion を所有し、routing/gate は実行時の next target・branch・retry・stop decision を所有します。"),
      },
    ],
    overlap_checks: [
      {
        other_module_id: "runtime-execution-attempts",
        semantic_area: localized("任务定义与执行尝试", "Task definitions versus execution attempts", "タスク定義と実行試行"),
        overlap_reason: localized("两者共享目标、输入和完成标准，但 Task 可跨运行复用，Attempt 具有一次性的开始时间、序号和结果。", "Both share goals, inputs, and completion criteria, but Task is reusable across runs while Attempt has one-time start, sequence, and outcome identity.", "どちらも目標、入力、完了基準を共有しますが、Task は実行横断で再利用でき、Attempt は一回限りの開始時刻、順番、結果同一性を持ちます。"),
        disambiguation_test: localized("若替换运行会话后对象仍保持同一工作定义，则归目标与任务规划；若身份随 run、attempt number 或时间变化，则归执行尝试与结果。", "If replacing the runtime session leaves the same work definition, it belongs to goals and task planning; if identity changes with run, attempt number, or time, it belongs to execution attempts and outcomes.", "実行セッションを替えても同じ作業定義なら目標とタスク計画、同一性が run、attempt number、時刻で変わるなら実行試行と結果に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-task-planning",
      },
      {
        other_module_id: "orchestration-routing-control",
        semantic_area: localized("规范任务计划与运行时控制决定", "Normative task plan versus runtime control decision", "normative task plan と runtime control decision"),
        overlap_reason: localized("计划可以声明依赖和可选路径，但某次运行选择哪条 Route、是否通过 Gate 是独立的发生事实。", "A plan can declare dependencies and optional paths, but which Route one run chooses and whether a Gate passes are separate occurrence facts.", "plan は dependency と optional path を宣言できますが、一 run がどの Route を選び Gate を通るかは別の occurrence fact です。"),
        disambiguation_test: localized("由 goal、task id、plan version 和完成判据识别的对象归任务规划；由当前 state、condition evaluation 和 routing decision 识别的对象归路由与门。", "An object identified by goal, task ID, plan version, and completion criterion belongs to task planning; one identified by current state, condition evaluation, and routing decision belongs to routing and gates.", "goal・task id・plan version・completion criterion で識別されれば task planning、current state・condition evaluation・routing decision で識別されれば routing/gate に属します。"),
        result: "distinct",
        owner_module_id: "orchestration-task-planning",
      },
    ],
  },
});
