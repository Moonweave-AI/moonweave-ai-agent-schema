id: node.cancellation_signal
artifact: NodeClass
label: Cancellation Signal
label_zh: 取消信号
intra_level: 3
intra_group: task-plane
intra_group_zh: 任务平面
intra_axis: message-task
intra_axis_zh: 任务平面 / 消息平面
intra_role: signal
parent_node: node.task_envelope
layer: protocol
description: >
  Protocol message requesting abort of an in-progress operation or task, propagating cancellation intent to executing endpoints.
description_zh: >
  请求中止正在进行的操作或任务的协议消息，将取消意图传播到执行端点。
attributes:
  target_task_id:
    type: string
    required: true
    description: Task or operation identifier to cancel
  reason:
    type: string
    required: false
    description: Human-readable cancellation reason
  graceful:
    type: boolean
    required: false
    description: Whether to allow graceful shutdown before hard abort
  issued_at:
    type: datetime
    required: false
    description: Timestamp when cancellation was requested
status: active
