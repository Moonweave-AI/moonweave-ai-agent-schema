id: contract.labelable
artifact: InterfaceContract
label: Labelable
label_zh: 可标注
description: 'Capability indicating a data artifact can carry confidentiality and integrity labels.

  '
operations:
- name: apply_label
  description: Attach or update a data label on the artifact.
  input:
    label: object
  output:
    labeled: boolean
- name: get_label
  description: Retrieve current labels on the artifact.
  input: {}
  output:
    labels: object
status: active
