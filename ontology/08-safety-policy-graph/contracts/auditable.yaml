id: contract.auditable
artifact: InterfaceContract
label: Auditable
label_zh: 可审计
description: 'Capability indicating an entity produces structured audit records for security-relevant events.

  '
operations:
- name: emit_audit_record
  description: Append an immutable audit log entry.
  input:
    event_type: string
    payload: object
  output:
    record_id: string
status: active
