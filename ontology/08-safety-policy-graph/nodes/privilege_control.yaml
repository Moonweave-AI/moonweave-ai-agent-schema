id: node.privilege_control
artifact: NodeClass
label: Privilege Control
label_zh: 权限控制
intra_level: 2
intra_group: permission-boundary
intra_group_zh: 权限边界
intra_axis: permission-trust
intra_axis_zh: 权限决策 / 权限边界
intra_role: control
parent_node: node.permission_policy
layer: safety
plane: SafetyPlane
description: 'Mechanism enforcing least privilege by dynamically scoping permissions to the minimum necessary for the current
  operation.

  '
description_zh: >
  通过动态地将权限范围限制为当前操作所需的最小权限来强制执行最小权限的机制。
attributes:
  elevation_requires_approval:
    type: boolean
    required: false
    description: Whether privilege elevation triggers approval flow.
  scope_reduction_on_completion:
    type: boolean
    required: false
    description: Whether elevated permissions auto-revoke after use.
status: active
