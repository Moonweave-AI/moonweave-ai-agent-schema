id: node.deny_decision
artifact: NodeClass
label: Deny Decision
label_zh: 拒绝决策
intra_level: 3
intra_group: permission-decision
intra_group_zh: 权限决策
intra_axis: permission-trust
intra_axis_zh: 权限决策 / 权限边界
intra_role: decision
parent_node: node.permission_policy
layer: safety
plane: SafetyPlane
description: 'Policy outcome blocking a requested action, returning rationale and optional retry guidance.

  '
description_zh: >
  政策结果阻止请求的操作，返回理由和可选的重试指导。
attributes:
  reason_code:
    type: string
    required: true
    description: Machine-readable denial reason.
  rationale:
    type: string
    required: true
    description: Human-readable explanation of the denial.
  retry_allowed:
    type: boolean
    required: false
    description: Whether the agent may retry with modified parameters.
status: active
state_machine:
  ref: state.policy_enforcement_lifecycle
required_edges:
  incoming:
  - predicate: DENIES
    source: node.policy_contract
