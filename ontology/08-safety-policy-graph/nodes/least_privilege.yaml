id: node.least_privilege
artifact: NodeClass
label: Least Privilege
label_zh: 最小权限
layer: safety
plane: SafetyPlane
description: 'Principle requiring that every agent, tool, and process receive only the minimal permissions necessary to accomplish
  its current task.

  '
attributes:
  baseline_permissions:
    type: object
    required: false
    description: Default minimal permission set for new sessions.
  monotonic_reduction:
    type: boolean
    required: false
    description: Whether permissions may only decrease without explicit elevation.
status: active
