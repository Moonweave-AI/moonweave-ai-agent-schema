id: node.plan
artifact: NodeClass
label: Plan
label_zh: 计划
description: Structured sequence of steps toward achieving a goal, including dependencies, preconditions, expected outcomes,
  and rollback points.
attributes:
  step_count:
    type: integer
    required: false
    description: Number of steps in the plan
  goal_description:
    type: string
    required: true
    description: Natural language description of the plan goal
  version:
    type: integer
    required: false
    description: Plan revision number
evidence:
- ref: evidence.paper.cognitive_architecture
layer: cognitive
plane: orchestration
state_machine:
  ref: state.plan_lifecycle
