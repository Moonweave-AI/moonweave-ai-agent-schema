id: edge.VERSIONED_AS
artifact: EdgeClass
predicate: VERSIONED_AS
source_domain: node.ontology_graph
target_range: node.version
cardinality: "1:N"
description: >
  Records version history of the ontology graph. Each release is a Version
  node linked from the root OntologyGraph.
evidence:
  - ref: evidence.ontology_self_description
status: active
