id: edge.CONTAINS
artifact: EdgeClass
predicate: CONTAINS
source_domain: [node.ontology_graph, node.subgraph]
target_range: [node.subgraph, node.node_class, node.edge_class, node.graph_constraint, node.graph_view, node.evidence_ref, node.source_claim, node.benchmark_ref]
cardinality: "1:N"
description: >
  Structural containment: an ontology graph contains subgraphs; a subgraph
  contains its node classes, edge classes, constraints, and views.
attributes:
  ordering:
    type: integer
    required: false
    description: Optional display or load order among siblings
evidence:
  - ref: evidence.ontology_self_description
status: active
