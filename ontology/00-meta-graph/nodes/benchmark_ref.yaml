id: node.benchmark_ref
artifact: NodeClass
label: BenchmarkRef
label_zh: 基准引用
intra_level: 4
intra_group: provenance
intra_group_zh: 证据溯源
intra_axis: governance-provenance
intra_axis_zh: 证据溯源 / 治理元数据
intra_role: benchmark
parent_node: node.evidence_ref
layer: meta
description: >
  Structured reference to an agent benchmark, task environment, metric suite,
  or evaluation dataset used to validate ontology coverage and runtime behavior.
description_zh: >
  指向智能体基准、任务环境、指标套件或评测数据集的结构化引用，用于验证本体覆盖和运行时行为。
attributes:
  benchmark_id:
    type: string
    required: true
    description: Globally unique id with benchmark. prefix.
  task_domain:
    type: string
    required: true
    description: Domain or environment family evaluated by the benchmark.
  metrics:
    type: array
    required: true
    description: Metrics used to judge agent performance, robustness, safety, or completion.
interfaces:
  - ref: contract.identifiable
  - ref: contract.describable
evidence:
  - ref: evidence.ontology_self_description
status: active

