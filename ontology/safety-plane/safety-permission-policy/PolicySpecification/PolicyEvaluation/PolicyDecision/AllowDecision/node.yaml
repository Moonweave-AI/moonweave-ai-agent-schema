schema: moonweave.ai/ontology-node/v1
id: AllowDecision
kind: concept
status: accepted
labels:
  zh: 允许决定
  en: allow decision
  ja: 許可判断
semantics:
  short_definition:
    zh: 结果为 allow、允许当前请求在记录范围和义务内继续的策略决定。
    en: A PolicyDecision with outcome allow, permitting the current request to continue within recorded scope and obligations.
    ja: 結果が allow で、現在要求を記録範囲と義務内で継続可能にする方針判断です。
  definition:
    zh: 允许决定是 PolicyDecision 的结果子类，只有 outcome=allow 且请求快照、决定规则、有效期和义务均明确时成立。它仅说明授权判断通过；若需要可复用权限，还必须另行签发 AuthorizationGrant，若要执行工具调用，还必须由 ToolApprovalGate 验证和执行。
    en: An AllowDecision is the outcome subtype of PolicyDecision present only when outcome=allow and the request snapshot, determining rules, validity, and obligations are explicit. It states that authorization evaluation passed; reusable authority still requires a separately issued AuthorizationGrant, and a tool call still requires ToolApprovalGate validation and enforcement.
    ja: AllowDecision は PolicyDecision の結果下位型で、outcome=allow かつ要求スナップショット、決定規則、有効期間、義務が明示された場合だけ成立します。認可評価通過を示しますが、再利用権限には別 AuthorizationGrant、ツール呼出には ToolApprovalGate の検証・執行が必要です。
  why_needed:
    zh: 明确允许结果可让执行方采用封闭式检查，同时避免把“允许”误读为副作用已发生或长期授权已建立。
    en: An explicit allow subtype supports closed-world enforcement checks without implying that a side effect occurred or standing authority was established.
    ja: 明示 allow 下位型は閉世界執行検査を支え、副作用発生や恒常権限成立を含意しません。
  includes:
    - zh: 包含继承的 decision_id、固定 allow 结果、适用义务和允许范围摘要。
      en: Includes inherited decision_id, fixed allow outcome, applicable obligations, and allowed-scope summary.
      ja: 継承 decision_id、固定 allow 結果、適用義務、許可範囲要約を含みます。
  excludes:
    - zh: 不包含 DenyDecision、DeferDecision、AuthorizationGrant、执行成功或副作用回执。
      en: Excludes DenyDecision, DeferDecision, AuthorizationGrant, execution success, or side-effect receipt.
      ja: DenyDecision、DeferDecision、AuthorizationGrant、実行成功、副作用受領を含みません。
  semantic_kind: information
  aliases: []
structure:
  identity_keys:
    - decision_id
  fields:
    - id: decision_id
      labels:
        zh: 决定标识
        en: decision ID
        ja: 判断ID
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 与上位 PolicyDecision 相同的稳定标识。
        en: Stable identity shared with the parent PolicyDecision record.
        ja: 上位 PolicyDecision 記録と共有する安定識別子です。
      allowed_values: []
      pattern: null
      example_value: decision-42
      source_claims:
        - claim-allow-decision-001
        - claim-allow-decision-002
    - id: allowed_scope_ref
      labels:
        zh: 允许范围引用
        en: allowed scope reference
        ja: 許可範囲参照
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 该 allow 结果实际覆盖的 PermissionScope。
        en: PermissionScope actually covered by the allow outcome.
        ja: allow 結果が実際に覆う PermissionScope です。
      allowed_values: []
      pattern: null
      example_value: scope-search-read
      source_claims:
        - claim-allow-decision-001
        - claim-allow-decision-002
    - id: obligations
      labels:
        zh: 允许义务
        en: allow obligations
        ja: 許可義務
      datatype: string
      required: false
      cardinality:
        min: 0
        max: null
      definitions:
        zh: 请求继续时执行方必须履行的要求。
        en: Requirements enforcement must fulfill when allowing the request to proceed.
        ja: 要求継続時に執行側が履行する要件です。
      allowed_values: []
      pattern: null
      example_value:
        - audit tool result
      source_claims:
        - claim-allow-decision-001
        - claim-allow-decision-002
  constraints: []
  required_relation_constraints: []
examples:
  - id: allow-decision-cedar
    labels:
      zh: 允许决定 - Cedar 授权评估
      en: allow decision - Cedar authorization evaluation
      ja: 許可判断 - Cedar 認可評価
    descriptions: {zh: "允许决定(AllowDecision) [AllowDecision]: 结果为 allow、允许当前请求在记录范围和义务内继续的策略决定。. Amazon Cedar 策略：permit(principal == User::\"alice\", action == Action::\"read\", resource == Document::\"report\") when { principal.department == \"engineering\" }；permit/forbid + when/unless 语法。 Fields: allowed_scope_ref, decision_id.", en: "allow decision (AllowDecision) [AllowDecision]: A PolicyDecision with outcome allow, permitting the current request to continue within recorded scope and obligations.. Amazon Cedar policy: permit(principal == User::\"alice\", action == Action::\"read\", resource == Document::\"report\") when { principal.department == \"engineering\" }; forbid/permit + when/unless syntax (docs.cedarpolicy.com). Fields include allowed_scope_ref, decision_id.", ja: "許可判断(AllowDecision) [AllowDecision]: 結果が allow で、現在要求を記録範囲と義務内で継続可能にする方針判断です。. Amazon Cedar：permit(principal == User::\"alice\", action == Action::\"read\", resource == Document::\"report\") when { principal.department == \"engineering\" }；permit/forbid + when/unless 構文。 Fields: allowed_scope_ref, decision_id."}
    field_values:
      allowed_scope_ref: scope-search-read
      decision_id: decision-42
    related_node_ids:
      - AllowDecision
      - AuthorizationGrant
      - DeferDecision
      - PolicyDecision
    related_relation_ids:
      - AllowDecision
      - AllowDecision-is_a-PolicyDecision
      - AuthorizationGrant
      - AuthorizationGrant-authorized_by-AllowDecision
      - claim-allow-decision-001
      - claim-allow-decision-002
      - DeferDecision
      - DeferDecision-is_a-PolicyDecision
      - PolicyDecision
    example_source: Amazon Cedar
    source_claims:
      - claim-allow-decision-001
  - id: allow-decision-opa
    labels:
      zh: 允许决定 - OPA Rego 策略评估
      en: allow decision - OPA Rego policy evaluation
      ja: 許可判断 - OPA Rego ポリシー評価
    descriptions: {zh: "允许决定(AllowDecision) [AllowDecision]: 结果为 allow、允许当前请求在记录范围和义务内继续的策略决定。. OPA Rego：package authz; default allow := false; allow if { input.user.role == \"admin\" }; allow if { input.user.role == \"editor\"; input.action == \"read\" }。 Fields: allowed_scope_ref, decision_id.", en: "allow decision (AllowDecision) [AllowDecision]: A PolicyDecision with outcome allow, permitting the current request to continue within recorded scope and obligations.. OPA Rego: package authz; default allow := false; allow if { input.user.role == \"admin\" }; allow if { input.user.role == \"editor\"; input.action == \"read\" } (openpolicyagent.org/docs/policy-language). Fields include allowed_scope_ref, decision_id.", ja: "許可判断(AllowDecision) [AllowDecision]: 結果が allow で、現在要求を記録範囲と義務内で継続可能にする方針判断です。. OPA Rego：package authz; default allow := false; allow if { input.user.role == \"admin\" }；editor+read も allow。 Fields: allowed_scope_ref, decision_id."}
    field_values:
      allowed_scope_ref: scope-search-read
      decision_id: decision-42
    related_node_ids:
      - AllowDecision
      - AuthorizationGrant
      - DeferDecision
      - PolicyDecision
    related_relation_ids:
      - AllowDecision
      - AllowDecision-is_a-PolicyDecision
      - AuthorizationGrant
      - AuthorizationGrant-authorized_by-AllowDecision
      - claim-allow-decision-001
      - claim-allow-decision-002
      - DeferDecision
      - DeferDecision-is_a-PolicyDecision
      - PolicyDecision
    example_source: Open Policy Agent
    source_claims:
      - claim-allow-decision-001
  - id: allow-decision-trustllm
    labels:
      zh: 允许决定 - TrustLLM 六维可信度
      en: allow decision - TrustLLM six-dimension trust
      ja: 許可判断 - TrustLLM 6次元信頼性
    descriptions: {zh: "允许决定(AllowDecision) [AllowDecision]: 结果为 allow、允许当前请求在记录范围和义务内继续的策略决定。. Sun 等（2024）TrustLLM 评估 LLM 在 truthfulness、safety、fairness、robustness、privacy、machine ethics 六维度的可信度（arxiv.org/abs/2401.05561）。 Fields: allowed_scope_ref, decision_id.", en: "allow decision (AllowDecision) [AllowDecision]: A PolicyDecision with outcome allow, permitting the current request to continue within recorded scope and obligations.. Sun et al. (2024) TrustLLM evaluates LLM trustworthiness across truthfulness, safety, fairness, robustness, privacy, and machine ethics (arxiv.org/abs/2401.05561). Fields include allowed_scope_ref, decision_id.", ja: "許可判断(AllowDecision) [AllowDecision]: 結果が allow で、現在要求を記録範囲と義務内で継続可能にする方針判断です。. Sun ほか（2024）TrustLLM：truthfulness/safety/fairness/robustness/privacy/machine ethics の6次元で LLM 信頼性を評価（arxiv.org/abs/2401.05561）。 Fields: allowed_scope_ref, decision_id."}
    field_values:
      allowed_scope_ref: scope-search-read
      decision_id: decision-42
    related_node_ids:
      - AllowDecision
      - AuthorizationGrant
      - DeferDecision
      - PolicyDecision
    related_relation_ids:
      - AllowDecision
      - AllowDecision-is_a-PolicyDecision
      - AuthorizationGrant
      - AuthorizationGrant-authorized_by-AllowDecision
      - claim-allow-decision-001
      - claim-allow-decision-002
      - DeferDecision
      - DeferDecision-is_a-PolicyDecision
      - PolicyDecision
    example_source: Sun TrustLLM 2024
    source_claims:
      - claim-allow-decision-001
  - id: allow-decision-decodingtrust
    labels:
      zh: 允许决定 - DecodingTrust GPT 评估
      en: allow decision - DecodingTrust GPT assessment
      ja: 許可判断 - DecodingTrust GPT 評価
    descriptions: {zh: "允许决定(AllowDecision) [AllowDecision]: 结果为 allow、允许当前请求在记录范围和义务内继续的策略决定。. Wang 等（2023）DecodingTrust 评估 GPT 在 toxicity、stereotype bias、adversarial robustness、OOD robustness、privacy、machine ethics、fairness 八维度的可信度（arxiv.org/abs/2306.11698）。 Fields: allowed_scope_ref, decision_id.", en: "allow decision (AllowDecision) [AllowDecision]: A PolicyDecision with outcome allow, permitting the current request to continue within recorded scope and obligations.. Wang et al. (2023) DecodingTrust assesses GPT trustworthiness across toxicity, stereotype bias, adversarial robustness, OOD robustness, privacy, machine ethics, and fairness (arxiv.org/abs/2306.11698). Fields include allowed_scope_ref, decision_id.", ja: "許可判断(AllowDecision) [AllowDecision]: 結果が allow で、現在要求を記録範囲と義務内で継続可能にする方針判断です。. Wang ほか（2023）DecodingTrust：toxicity/bias/adversarial/OOD/privacy/ethics/fairness 等8次元で GPT 信頼性を評価（arxiv.org/abs/2306.11698）。 Fields: allowed_scope_ref, decision_id."}
    field_values:
      allowed_scope_ref: scope-search-read
      decision_id: decision-42
    related_node_ids:
      - AllowDecision
      - AuthorizationGrant
      - DeferDecision
      - PolicyDecision
    related_relation_ids:
      - AllowDecision
      - AllowDecision-is_a-PolicyDecision
      - AuthorizationGrant
      - AuthorizationGrant-authorized_by-AllowDecision
      - claim-allow-decision-001
      - claim-allow-decision-002
      - DeferDecision
      - DeferDecision-is_a-PolicyDecision
      - PolicyDecision
    example_source: Wang DecodingTrust 2023
    source_claims:
      - claim-allow-decision-001
external_mappings: []
applicability:
  - core
sibling_differentiation:
  - sibling_concept_id: DenyDecision
    shared_parent_concept_id: PolicyDecision
    differentia:
      zh: AllowDecision 当前授权请求在记录范围内继续；DenyDecision 当前禁止请求继续。二者共享 PolicyDecision 字段，但 outcome 和执行后果互斥。
      en: AllowDecision permits the current request within recorded scope; DenyDecision prohibits it. They share PolicyDecision fields but have mutually exclusive outcomes and enforcement consequences.
      ja: AllowDecision は現在要求を記録範囲内で許可し、DenyDecision は禁止します。PolicyDecision 項目を共有しますが結果と執行効果は排他的です。
    source_claims:
      - claim-allow-decision-001
parent_relation:
  id: AllowDecision-is_a-PolicyDecision
  predicate: is_a
  direction: target-to-source
  relation_kind: hierarchy
  labels:
    zh: 是一种策略决定
    en: is a policy decision
    ja: ポリシー判断の一種
  definitions:
    zh: 每个 AllowDecision 都是 outcome=allow 的 PolicyDecision，并继承请求、评估、规则、算法、理由和有效期要求。
    en: Every AllowDecision is a PolicyDecision with outcome=allow and inherits request, evaluation, rule, algorithm, rationale, and validity requirements.
    ja: 各 AllowDecision は outcome=allow の PolicyDecision で、要求・評価・規則・アルゴリズム・理由・有効期間要件を継承します。
  cardinality: null
  cardinality_not_applicable_reason:
    zh: 本关系描述本体事实；实例基数由所引用规范与生命周期约束决定。
    en: This relation states an ontology fact; instance cardinality is governed by the referenced specification and lifecycle constraints.
    ja: この関係は本体事実を表し、実体基数は参照仕様とライフサイクル制約で決まります。
  inverse_reading:
    predicate: part_of
    labels:
      zh: 具有允许子类
      en: has allow subtype
      ja: 許可下位型を持つ
  conditions: []
  temporal_scope: timeless
  boundary_context: null
  constraints: []
  examples:
    - id: AllowDecision-is_a-PolicyDecision-example-positive-manual-001
      labels:
        zh: 是一种策略决定正例
        en: Positive is a policy decision case
        ja: ポリシー判断の一種の正例
      descriptions:
        zh: decision-42 的 outcome=allow 且完整满足 PolicyDecision 身份字段，因此分类为 AllowDecision。
        en: decision-42 has outcome=allow and complete PolicyDecision identity fields, so it is classified as AllowDecision.
        ja: decision-42 は outcome=allow で完全 PolicyDecision 識別項目を持ち AllowDecision に分類されます。
      field_values: {}
      related_node_ids:
        - AllowDecision
        - PolicyDecision
      related_relation_ids:
        - AllowDecision-is_a-PolicyDecision

        en: Querying AllowDecision also returns its PolicyDecision membership.
        ja: AllowDecision 照会は上位 PolicyDecision 所属も返します。

        en: The outcome differentia, decision identity, and required fields are all satisfied.
        ja: 結果種差、判断同一性、必須項目をすべて満たします。
      source_claims:
        - claim-allow-decision-001
        - claim-allow-decision-002
    - id: AllowDecision-is_a-PolicyDecision-example-boundary-manual-001
      labels:
        zh: 是一种策略决定边界反例
        en: Boundary counterexample for is a policy decision
        ja: ポリシー判断の一種の境界反例
      descriptions:
        zh: grant-42 源自 allow 决定但自身是 AuthorizationGrant，不是 AllowDecision。
        en: grant-42 derives from an allow decision but is an AuthorizationGrant, not an AllowDecision.
        ja: grant-42 は allow 判断に由来しますが AuthorizationGrant で AllowDecision ではありません。
      field_values: {}
      related_node_ids:
        - AllowDecision
        - PolicyDecision
      related_relation_ids:
        - AllowDecision-is_a-PolicyDecision

        en: authorized_by does not establish is_a.
        ja: authorized_by は is_a を成立させません。

        en: Serving as grant basis does not establish membership in a decision-outcome subtype.
        ja: 付与根拠になることは判断結果下位型への所属を意味しません。
      source_claims:
        - claim-allow-decision-001
        - claim-allow-decision-002
  source_claims:
    - claim-allow-decision-001
    - claim-allow-decision-002
  distinct_fact_rationale: null
  layout_role: primary-backbone
  review_status: accepted
  endpoint_order: child-to-parent
relations: []
review:
  status: accepted
  method: manual-semantic
  reviewer: manual-safety-verification
  reviewed_on: 2026-07-17
  note:
    zh: 已对照 XACML 3.0、NIST SP 800-162、OAuth/MCP 授权与 Cursor 工具批准模式逐项人工语义核验；三值策略决定与授权闭环成立。
    en: Manually verified against XACML 3.0, NIST SP 800-162, OAuth/MCP authorization, and Cursor-style tool approval; three-valued policy decisions and authorization loop hold.
    ja: XACML 3.0、NIST SP 800-162、OAuth/MCP authorization、Cursor-style tool approval に照らし手動意味審査を完了しました。
  sections:
    semantics: accepted
    hierarchy: accepted
    relations: accepted
    structure: accepted
    examples: accepted
    source_claims: accepted
source_claims:
  - id: claim-allow-decision-001
    source: eng-policy-aws-verified-permissions
    supports: Verified Permissions defines an authorization request over principal, action, resource, and context, and a response containing allow or deny plus determining-policy identifiers.
    locator: Amazon Verified Permissions User Guide > Terms and concepts > authorization request, authorization response, determining policies, and enforcement
    evidence_kind: official
    confidence: high
    review_status: accepted
  - id: claim-allow-decision-002
    source: eng-policy-xacml-3
    supports: XACML normatively separates a policy and its rules, targets or conditions, Permit or Deny effects, obligations, decision values, and rule or policy combining algorithms.
    locator: "OASIS XACML 3.0 Core, Sections 3, 5 and 7.11-7.18: policy model, rule evaluation, decisions, obligations, and combining algorithms"
    evidence_kind: normative
    confidence: high
    review_status: accepted
