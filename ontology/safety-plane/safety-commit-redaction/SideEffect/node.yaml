schema: moonweave.ai/ontology-node/v1
id: SideEffect
kind: concept
status: accepted
labels:
  en: side effect
  zh: 副作用
  ja: 副作用
semantics:
  short_definition:
    zh: 由智能体动作引起、改变外部可观察状态的事件。
    en: An event in which an agent action changes externally observable state.
    ja: エージェント動作により外部から観測可能な状態が変化するイベント。
  definition:
    zh: 副作用是一种独立事件，其种差是工具调用、命令或网络操作使智能体内部推理之外的状态发生可观察变化，例如写文件、提交代码、发送消息、退款或修改远端资源；它不是变更请求、执行许可、返回文本或效果回执。
    en: SideEffect is an independent event distinguished by a tool call, command, or network operation producing an observable state change outside the agent's internal reasoning, such as writing a file, committing code, sending a message, issuing a refund, or changing a remote resource; it is not the request, permission, returned text, or receipt.
    ja: SideEffect は独立イベントであり、ツール呼出し、コマンド、ネットワーク操作がエージェント内部推論の外で、ファイル書込み、コード確定、送信、返金、遠隔資源変更などの可観測状態変化を生じる点で区別されます。要求、許可、返却テキスト、受領証ではありません。
  why_needed:
    zh: 将真实状态变化单独建模，系统才能判断审批保护的是哪一种风险、哪些结果需要对账或补偿，并避免把工具输出或请求意图误报为已经发生的外部效果。
    en: Modeling the actual state change separately identifies what risk approval protects, which outcomes need reconciliation or compensation, and prevents tool output or request intent from being mistaken for an effect that occurred.
    ja: 実際の状態変化を分離して表すことで、承認対象リスク、照合・補償対象を特定し、ツール出力や要求意図を発生済み外部効果と誤認しません。
  includes:
    - zh: 文件系统、仓库、数据库、外部 API、消息通道、支付系统或远端资源中由一次智能体操作实际造成的可观察创建、更新、删除或发送事件。
      en: Observable creation, update, deletion, or sending events actually caused by an agent operation in a filesystem, repository, database, external API, messaging channel, payment system, or remote resource.
      ja: ファイルシステム、repository、database、外部 API、message channel、決済、遠隔資源でエージェント操作が実際に生じさせた作成、更新、削除、送信イベント。
  excludes:
    - zh: 只读查询、候选 CommitRequest、CommitApproval 或 CommitDenial、没有落地的模型计划、以及描述结果的 EffectReceipt。
      en: Read-only queries, candidate CommitRequest records, CommitApproval or CommitDenial, unrealized model plans, and EffectReceipt records describing results.
      ja: 読取専用照会、候補 CommitRequest、CommitApproval/CommitDenial、未実現モデル計画、結果を記述する EffectReceipt。
  semantic_kind: event
  aliases: []
structure:
  identity_keys:
    - side_effect_id
  fields:
    - id: side_effect_id
      labels:
        zh: 副作用事件标识
        en: side-effect event ID
        ja: side-effect event ID
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 稳定标识一次已经实现且可外部观察的状态变化事件；重试造成的新变化必须有新标识。
        en: Stably identifies one realized, externally observable state-change event; a retry causing another change receives another ID.
        ja: 実現済みで externally observable な state-change event を安定識別し、retry が別 change を生じた場合は別 ID を持ちます。
      allowed_values: []
      pattern: null
      example_value: side-effect:refund-re_77
      source_claims:
        - claim-side-effect-002
    - id: operation_ref
      labels:
        zh: 致因操作引用
        en: causal operation reference
        ja: 原因 operation 参照
      datatype: reference
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 引用直接造成该变化的确切工具调用、命令或网络操作发生，不引用可复用工具定义或审批记录。
        en: References the exact tool-call, command, or network-operation occurrence that directly caused the change, not a reusable tool definition or approval.
        ja: change を直接生じさせた exact tool-call、command、network-operation occurrence を参照し reusable tool definition や approval は参照しません。
      allowed_values: []
      pattern: null
      example_value: tool-call:refund_order#order-913
      source_claims:
        - claim-side-effect-003
    - id: effect_kind
      labels:
        zh: 外部变化种类
        en: external change kind
        ja: external change kind
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 按外部系统最终可观察的变化把事件分类为创建、更新、删除、发送或事务；分类不依据工具名称猜测。
        en: Classifies the event by its finally observable external change as create, update, delete, send, or transaction; it is never guessed from the tool name.
        ja: 最終的に observable な external change により create、update、delete、send、transaction に分類し tool name から推測しません。
      allowed_values:
        - id: SideEffectKindCreate
          value: create
          labels:
            zh: 创建资源
            en: create
            ja: create
          definitions:
            zh: 创建资源；该分类描述已实现的外部变化，而非工具结果文本。
            en: The operation creates a previously absent external resource with a stable resulting reference.
            ja: 操作が以前存在しなかった external resource を作成し stable resulting reference を持ちます。
          status: accepted
          source_claims:
            - claim-side-effect-004
        - id: SideEffectKindUpdate
          value: update
          labels:
            zh: 更新资源
            en: update
            ja: update
          definitions:
            zh: 更新资源；该分类描述已实现的外部变化，而非工具结果文本。
            en: The operation changes observable state on an existing external resource without replacing its identity.
            ja: 操作が既存 external resource の identity を保ったまま observable state を変更します。
          status: accepted
          source_claims:
            - claim-side-effect-004
        - id: SideEffectKindDelete
          value: delete
          labels:
            zh: 删除资源
            en: delete
            ja: delete
          definitions:
            zh: 删除资源；该分类描述已实现的外部变化，而非工具结果文本。
            en: The operation makes an existing external resource unavailable or invalid under the external system's semantics.
            ja: 操作が既存 external resource を外部 system の semantics に従って unavailable または invalid にします。
          status: accepted
          source_claims:
            - claim-side-effect-004
        - id: SideEffectKindSend
          value: send
          labels:
            zh: 发送或发布
            en: send
            ja: send
          definitions:
            zh: 发送或发布；该分类描述已实现的外部变化，而非工具结果文本。
            en: The operation delivers or publishes information to an external recipient or channel.
            ja: 操作が information を external recipient または channel へ送信・公開します。
          status: accepted
          source_claims:
            - claim-side-effect-004
        - id: SideEffectKindTransaction
          value: transaction
          labels:
            zh: 外部事务
            en: transaction
            ja: transaction
          definitions:
            zh: 外部事务；该分类描述已实现的外部变化，而非工具结果文本。
            en: One externally committed operation causes a jointly auditable set of resource creations or updates, such as creating a refund and changing a balance.
            ja: 一つの externally committed operation が refund 作成と balance 変更のような jointly auditable resource changes を生じます。
          status: accepted
          source_claims:
            - claim-side-effect-004
      pattern: null
      example_value: transaction
      source_claims:
        - claim-side-effect-005
    - id: affected_resource_refs
      labels:
        zh: 受影响资源引用
        en: affected resource references
        ja: affected resource 参照
      datatype: array[reference]
      required: true
      cardinality:
        min: 1
        max: null
      definitions:
        zh: 列出变化完成后可在外部系统解析的全部受影响资源；复合事务不得只记录其中一个资源。
        en: Lists every affected resource resolvable in the external system after the change; a compound transaction cannot omit one of its changed resources.
        ja: change 後に external system で解決できる全 affected resources を列挙し compound transaction の一部を省略しません。
      allowed_values: []
      pattern: null
      example_value:
        - payment-refund:re_77
        - order:913#refundable-balance
      source_claims:
        - claim-side-effect-006
    - id: observed_change
      labels:
        zh: 已观测状态变化
        en: observed state change
        ja: observed state change
      datatype: object
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 保存证明外部状态确已改变的最小结构化事实，例如新资源标识、字段前后值或数值增量；只含 success=true 的返回文本不足。
        en: Preserves minimal structured facts proving external state changed, such as a new resource ID, before/after field value, or numeric delta. Returned text containing only success=true is insufficient.
        ja: new resource ID、before/after value、numeric delta など external state change を証明する最小 structured facts を保持し success=true だけの result text は不十分です。
      allowed_values: []
      pattern: null
      example_value:
        refund_created: re_77
        refundable_balance_delta: -100
        currency: USD
      source_claims:
        - claim-side-effect-007
    - id: occurred_at
      labels:
        zh: 副作用发生时间
        en: side-effect occurrence time
        ja: side-effect occurrence time
      datatype: datetime
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 记录外部系统确认变化生效的有效时间，不使用请求发送时间或模型生成时间替代。
        en: Records the valid time when the external system confirmed the change took effect, not request-send or model-generation time.
        ja: external system が change effective を確認した valid time を記録し request-send time や model-generation time で代用しません。
      allowed_values: []
      pattern: null
      example_value: 2026-07-16T10:15:32Z
      source_claims:
        - claim-side-effect-008
  constraints:
    - id: side-effect-requires-observable-resource-change
      severity: error
      expression_language: plain
      expression: affected_resource_refs IS NON-EMPTY AND observed_change contains at least one created identifier, before/after value, or non-zero delta attributable to operation_ref
      explanations:
        zh: 只有当至少一个外部资源存在可归因于该操作的实际变化证据时，记录才能分类为 SideEffect。
        en: A record is a SideEffect only when at least one external resource has actual change evidence attributable to the operation.
        ja: 少なくとも一つの external resource に operation attributable な actual change evidence がある場合のみ SideEffect とします。
      source_claims:
        - claim-side-effect-009
  required_relation_constraints: []
examples:
  - id: side-effect-openai_hitl
    labels:
      zh: 副作用 - OpenAI Agents SDK 人工审批
      en: side effect - OpenAI Agents SDK human approval
      ja: 副作用 - OpenAI Agents SDK 人手承認
    descriptions: {zh: "副作用(SideEffect) [SideEffect]: 由智能体动作引起、改变外部可观察状态的事件。. OpenAI Agents SDK：cancel_order 设置 needsApproval: true 时运行暂停，result.interruptions 出现 RunToolApprovalItem，直至 state.approve(interruption)。 Fields: operation_ref, side_effect_id, occurred_at.", en: "side effect (SideEffect) [SideEffect]: An event in which an agent action changes externally observable state.. OpenAI Agents SDK: cancel_order with needsApproval: true pauses the run and surfaces RunToolApprovalItem in result.interruptions until state.approve(interruption) (openai.github.io/openai-agents-js/guides/human-in-the-loop/). Fields include operation_ref, side_effect_id, occurred_at.", ja: "副作用(SideEffect) [SideEffect]: エージェント動作により外部から観測可能な状態が変化するイベント。. OpenAI Agents SDK：needsApproval: true の cancel_order は RunToolApprovalItem を返し state.approve まで pause する。 Fields: operation_ref, side_effect_id, occurred_at."}
    field_values:
      effect_kind: transaction
      occurred_at: "2026-07-16T10:15:32Z"
      operation_ref: "tool-call:refund_order#order-913"
      side_effect_id: "side-effect:refund-re_77"
    related_node_ids:
      - CommitGate
      - CommitRequest
      - EffectReceipt
      - SideEffect
    related_relation_ids:
      - claim-side-effect-001
      - claim-side-effect-010
      - claim-side-effect-011
      - claim-side-effect-012
      - CommitGate
      - CommitRequest
      - EffectReceipt
      - EffectReceipt-evidences-SideEffect
      - SideEffect
    example_source: OpenAI Agents SDK HITL
    source_claims:
      - claim-side-effect-001
  - id: side-effect-langgraph_interrupt
    labels:
      zh: 副作用 - LangGraph 人工中断
      en: side effect - LangGraph human interrupt
      ja: 副作用 - LangGraph 人手 interrupt
    descriptions: {zh: "副作用(SideEffect) [SideEffect]: 由智能体动作引起、改变外部可观察状态的事件。. LangGraph interrupt() 暂停图执行供人工审查；Command(resume=...) 在 approve/edit/reject 后继续。 Fields: operation_ref, side_effect_id, occurred_at.", en: "side effect (SideEffect) [SideEffect]: An event in which an agent action changes externally observable state.. LangGraph interrupt() pauses graph execution for human review; Command(resume=...) continues after approve/edit/reject (docs.langchain.com/langgraph/interrupts). Fields include operation_ref, side_effect_id, occurred_at.", ja: "副作用(SideEffect) [SideEffect]: エージェント動作により外部から観測可能な状態が変化するイベント。. LangGraph interrupt() は human review のため graph を pause し Command(resume=...) で再開する。 Fields: operation_ref, side_effect_id, occurred_at."}
    field_values:
      effect_kind: transaction
      occurred_at: "2026-07-16T10:15:32Z"
      operation_ref: "tool-call:refund_order#order-913"
      side_effect_id: "side-effect:refund-re_77"
    related_node_ids:
      - CommitGate
      - CommitRequest
      - EffectReceipt
      - SideEffect
    related_relation_ids:
      - claim-side-effect-001
      - claim-side-effect-010
      - claim-side-effect-011
      - claim-side-effect-012
      - CommitGate
      - CommitRequest
      - EffectReceipt
      - EffectReceipt-evidences-SideEffect
      - SideEffect
    example_source: LangGraph interrupt
    source_claims:
      - claim-side-effect-001
  - id: side-effect-echoleak
    labels:
      zh: 副作用 - EchoLeak Copilot 邮件注入
      en: side effect - EchoLeak Copilot email injection
      ja: 副作用 - EchoLeak Copilot メール注入
    descriptions: {zh: "副作用(SideEffect) [SideEffect]: 由智能体动作引起、改变外部可观察状态的事件。. EchoLeak（CVE-2025-32711，CVSS 9.3）：精心构造的 Microsoft 365 Copilot 邮件隐藏指令，受害者在要求 Copilot 汇总收件箱时触发内部 SharePoint 数据外泄（NVD，2025）。 Fields: operation_ref, side_effect_id, occurred_at.", en: "side effect (SideEffect) [SideEffect]: An event in which an agent action changes externally observable state.. EchoLeak (CVE-2025-32711, CVSS 9.3): a crafted Microsoft 365 Copilot email hid instructions that exfiltrated internal SharePoint data when the victim asked Copilot to summarize the inbox (NVD, 2025). Fields include operation_ref, side_effect_id, occurred_at.", ja: "副作用(SideEffect) [SideEffect]: エージェント動作により外部から観測可能な状態が変化するイベント。. EchoLeak（CVE-2025-32711、CVSS 9.3）：細工された Microsoft 365 Copilot メールが内部 SharePoint データの exfiltration を誘発した。 Fields: operation_ref, side_effect_id, occurred_at."}
    field_values:
      effect_kind: transaction
      occurred_at: "2026-07-16T10:15:32Z"
      operation_ref: "tool-call:refund_order#order-913"
      side_effect_id: "side-effect:refund-re_77"
    related_node_ids:
      - CommitGate
      - CommitRequest
      - EffectReceipt
      - SideEffect
    related_relation_ids:
      - claim-side-effect-001
      - claim-side-effect-010
      - claim-side-effect-011
      - claim-side-effect-012
      - CommitGate
      - CommitRequest
      - EffectReceipt
      - EffectReceipt-evidences-SideEffect
      - SideEffect
    example_source: EchoLeak CVE-2025-32711
    source_claims:
      - claim-side-effect-001
  - id: side-effect-greshake_bing
    labels:
      zh: 副作用 - Greshake Bing Chat 间接注入
      en: side effect - Greshake Bing Chat indirect injection
      ja: 副作用 - Greshake Bing Chat 間接注入
    descriptions: {zh: "副作用(SideEffect) [SideEffect]: 由智能体动作引起、改变外部可观察状态的事件。. Greshake 等（ACM CCS 2023，arXiv:2302.12173）演示 Bing Chat 间接注入：Edge 侧栏读取网页时，HTML 注释改变聊天行为。 Fields: operation_ref, side_effect_id, occurred_at.", en: "side effect (SideEffect) [SideEffect]: An event in which an agent action changes externally observable state.. Greshake et al. (ACM CCS 2023, arXiv:2302.12173) demonstrated Bing Chat indirect injection: HTML comments on a webpage changed chat behavior when Edge sidebar read the page. Fields include operation_ref, side_effect_id, occurred_at.", ja: "副作用(SideEffect) [SideEffect]: エージェント動作により外部から観測可能な状態が変化するイベント。. Greshake ほか（ACM CCS 2023, arXiv:2302.12173) は Bing Chat 間接注入を実証：Edge サイドバーが HTML コメント付きページを読むと chat 挙動が変わる。 Fields: operation_ref, side_effect_id, occurred_at."}
    field_values:
      effect_kind: transaction
      occurred_at: "2026-07-16T10:15:32Z"
      operation_ref: "tool-call:refund_order#order-913"
      side_effect_id: "side-effect:refund-re_77"
    related_node_ids:
      - CommitGate
      - CommitRequest
      - EffectReceipt
      - SideEffect
    related_relation_ids:
      - claim-side-effect-001
      - claim-side-effect-010
      - claim-side-effect-011
      - claim-side-effect-012
      - CommitGate
      - CommitRequest
      - EffectReceipt
      - EffectReceipt-evidences-SideEffect
      - SideEffect
    example_source: Greshake ACM CCS 2023
    source_claims:
      - claim-side-effect-001
external_mappings: []
applicability:
  - core
sibling_differentiation: []
parent_relation: null
relations:
  - id: SideEffect-realizes-CommitRequest
    predicate: realizes
    direction: source-to-target
    relation_kind: causal
    labels:
      zh: 实现提交请求
      en: realizes commit request
      ja: 確定要求を実現
    definitions:
      zh: 一个 SideEffect realizes 一个 CommitRequest，当且仅当该已发生外部状态变化由该请求的同一调用、目标、冻结参数与幂等键实际执行产生；批准只表示允许，验证失败或未开始执行的请求不具有该关系。
      en: A SideEffect realizes a CommitRequest only when the occurred external state change was produced by executing that request's same call, target, frozen arguments, and idempotency key. Approval merely permits execution; a validation failure or request whose execution never began has no such fact.
      ja: SideEffect が CommitRequest を realizes するのは、発生した外部状態変更が同じ call・対象・固定引数・冪等キーの実行で生じた場合だけです。承認は許可にすぎず、検証失敗や未実行要求には成立しません。
    cardinality: null
    cardinality_not_applicable_reason:
      zh: 幂等性与失败模型可能使请求没有效果或多条证据记录，实例约束由请求键和回执校验。
      en: Idempotency and failure models may leave a request without an effect or with multiple evidence records; instance integrity is checked through request keys and receipts.
      ja: 冪等性と失敗 model により要求に効果がない場合や複数証拠があり、instance 整合性は要求キーと受領証で検証します。
    inverse_reading:
      predicate: realized_by_side_effect
      labels:
        zh: 由副作用实现
        en: realized by side effect
        ja: 副作用により実現
    conditions: []
    temporal_scope: timeless
    boundary_context: null
    constraints: []
    examples:
      - id: SideEffect-realizes-CommitRequest-example-positive-001
        labels:
          zh: 退款请求实际实现
          en: Refund request realized
          ja: 返金要求の実現
        descriptions:
          zh: cr_82 以 idempotency_key=refund:913:1 请求退款；提供方创建 refund re_77，余额变化事件 se_77 记录相同请求键与参数摘要。
          en: cr_82 requests a refund with idempotency_key=refund:913:1; the provider creates refund re_77 and balance-change event se_77 records the same request key and argument digest.
          ja: cr_82 は idempotency_key=refund:913:1 で返金を要求し、provider が refund re_77 を作成、残高変更 event se_77 が同じ要求キーと引数 digest を記録します。
        field_values: {}
        related_node_ids:
          - SideEffect
          - CommitRequest
          - EffectReceipt
        related_relation_ids:
          - SideEffect-realizes-CommitRequest
          - EffectReceipt-evidences-SideEffect

          en: The query traces uniquely from cr_82 to se_77 and its receipt.
          ja: 照会は cr_82 から se_77 と受領証へ一意に追跡します。

          en: Request key, target, arguments, and external result agree.
          ja: 要求キー・対象・引数・外部結果が一致します。
        source_claims:
          - claim-side-effect-013
      - id: SideEffect-realizes-CommitRequest-example-boundary-001
        labels:
          zh: 验证失败无效果边界
          en: Validation-failure boundary
          ja: 検証失敗境界
        descriptions:
          zh: cr_90 因参数校验失败而未进入端点执行；虽然请求存在，也没有 SideEffect realizes cr_90。
          en: cr_90 fails argument validation before endpoint execution begins; although the request exists, no SideEffect realizes cr_90.
          ja: cr_90 は endpoint 実行前の引数検証に失敗し、要求は存在しても realizes する SideEffect はありません。
        field_values: {}
        related_node_ids:
          - SideEffect
          - CommitRequest
        related_relation_ids:
          - SideEffect-realizes-CommitRequest

          en: Record the failure without inventing an external state change.
          ja: 失敗を記録し外部状態変更を捏造しません。

          en: Execution never began, so the occurrence condition is false.
          ja: 実行未開始のため成立条件を満たしません。
        source_claims:
          - claim-side-effect-014
    source_claims:
      - claim-side-effect-015
    distinct_fact_rationale:
      zh: 批准、工具返回文本和实际外部状态变化是不同事实，必须用请求键与执行证据显式绑定。
      en: Approval, tool-result text, and an actual external state change are different facts and require explicit binding through request key and execution evidence.
      ja: 承認・tool result text・実際の外部状態変更は別事実で、要求キーと実行証拠による明示結合が必要です。
    layout_role: cross-link
    review_status: accepted
    target: concept:CommitRequest
  - id: side_effect_produced_by_network_call
    predicate: side_effect_produced_by_network_call
    direction: source-to-target
    relation_kind: causal
    labels:
      zh: 由网络调用产生
      en: produced by network call
      ja: network call により発生
    definitions:
      zh: 一个 SideEffect 在其外部状态变化由特定 NetworkCall 的成功处理造成时指向该调用；普通读取、被拒请求或仅接收响应不自动建立此因果边。
      en: A SideEffect points to a specific NetworkCall when successful handling of that call caused the external state change; ordinary reads, denied requests, or merely receiving a response do not establish the causal edge.
      ja: SideEffect は特定 NetworkCall の成功処理が外部状態変化を生じた時その call を指します。通常読取、拒否要求、response 受信だけでは因果辺を作りません。
    cardinality: null
    cardinality_not_applicable_reason:
      zh: 该本体关系记录可查询的语义事实，但不对所有运行实例施加统一基数。
      en: This ontology relation records a queryable semantic fact without imposing one universal cardinality on all runtime instances.
      ja: この本体関係は検索可能な意味事実を記録しますが、すべての実行インスタンスに一律の基数を課しません。
    inverse_reading:
      predicate: is_target_of_side_effect_produced_by_network_call
      labels:
        zh: 反向：side effect produced by network call
        en: is target of side effect produced by network call
        ja: 逆方向：side effect produced by network call
    conditions: []
    temporal_scope: timeless
    boundary_context: null
    constraints: []
    examples:
      - id: side_effect_produced_by_network_call-example-positive-001
        labels:
          zh: 退款 API 因果
          en: Refund API causation
          ja: 返金 API 因果
        descriptions:
          zh: POST /refunds 请求 nc_77 被支付服务处理并创建 refund re_77；SideEffect se_77 指向 nc_77。
          en: POST /refunds call nc_77 is processed by the payment service and creates refund re_77; SideEffect se_77 points to nc_77.
          ja: POST /refunds call nc_77 が決済 service に処理され refund re_77 を作成し、SideEffect se_77 は nc_77 を指します。
        field_values: {}
        related_node_ids:
          - SideEffect
          - NetworkCall
        related_relation_ids:
          - side_effect_produced_by_network_call

          en: Audit traces from se_77 to request, endpoint, and response without treating the response record as the event.
          ja: 監査は se_77 から request、endpoint、response を追跡し、response 記録を event 自体にしません。

          en: The network call and external creation share a direct processing chain and identifiers.
          ja: network call と外部作成の間に直接処理 chain と共通 ID があります。
        source_claims:
          - claim-side-effect-016
      - id: side_effect_produced_by_network_call-example-boundary-001
        labels:
          zh: 只读 GET 边界
          en: Read-only GET boundary
          ja: 読取専用 GET の境界
        descriptions:
          zh: GET /orders/913 返回订单状态但不改变远端资源；NetworkCall 存在，SideEffect 不存在。
          en: GET /orders/913 returns order status without changing a remote resource; NetworkCall exists but SideEffect does not.
          ja: GET /orders/913 は注文状態を返し遠隔資源を変更せず、NetworkCall は存在しても SideEffect はありません。
        field_values: {}
        related_node_ids:
          - SideEffect
          - NetworkCall
        related_relation_ids:
          - side_effect_produced_by_network_call

          en: The system records call and response only and creates no produced_by edge.
          ja: call と response だけを記録し produced_by 辺を作りません。

          en: The causal edge requires an actual observable state change; network communication alone is insufficient.
          ja: 因果辺には実際の可観測状態変化が必要で、network 通信だけでは不足です。
        source_claims:
          - claim-side-effect-016
    source_claims:
      - claim-side-effect-016
    distinct_fact_rationale:
      zh: NetworkCall 类型不说明它是否改变状态，需在已发生 SideEffect 时单独记录因果。
      en: NetworkCall type does not state whether it changed state; causation is recorded only for a realized SideEffect.
      ja: NetworkCall 型は状態変更を示さず、実現 SideEffect にだけ因果を記録します。
    layout_role: cross-link
    review_status: accepted
    target: concept:NetworkCall
  - id: SideEffect-produced_by-InvocationAttempt
    predicate: produced_by
    direction: source-to-target
    relation_kind: causal
    labels:
      zh: 由调用尝试产生
      en: produced by invocation attempt
      ja: invocation attempt により生成
    definitions:
      zh: SideEffect 指向实际产生外部状态变化的同一个 InvocationAttempt，要求尝试标识、执行区间、目标资源和变更证据相互一致；调用计划、返回文本或失败的其他尝试不能承担该因果角色。
      en: SideEffect points to the InvocationAttempt that actually produced the external state change, requiring attempt identity, execution interval, target resource, and change evidence to agree; an invocation plan, returned text, or another failed attempt cannot carry this causal role.
      ja: SideEffect は external state change を実際に生成した InvocationAttempt を指し、attempt identity・execution interval・target resource・change evidence の一致を要求します。invocation plan・returned text・別 failed attempt は causal role を担えません。
    cardinality: null
    cardinality_not_applicable_reason:
      zh: 因果边数量取决于实际观察到的效应实例，不强加统一基数。
      en: The number of causal edges depends on observed effect instances, so no universal cardinality is imposed.
      ja: causal edge 数は observed effect instance に依存し共通 cardinality は課しません。
    inverse_reading:
      predicate: produces_side_effect
      labels:
        zh: 反向：产生副作用
        en: "inverse: produces side effect"
        ja: 逆方向：副作用を生成
    conditions:
      - id: SideEffect-produced_by-InvocationAttempt-condition-manual-20260717
        severity: error
        expression_language: plain
        expression: Assert only when causal_attempt_ref matches the same InvocationAttempt, the change time lies within its execution interval, and before/after evidence exists for filesystem, process, or workspace state; a successful result alone is insufficient.
        explanations:
          zh: 仅当 causal_attempt_ref 匹配同一 InvocationAttempt，变更时间位于执行区间内，并有文件、进程或工作区状态的前后证据时成立；成功结果本身不足。
          en: Assert only when causal_attempt_ref matches the same InvocationAttempt, the change time lies within its execution interval, and before/after evidence exists for filesystem, process, or workspace state; a successful result alone is insufficient.
          ja: causal_attempt_ref が同一 InvocationAttempt と一致し change time が interval 内で before/after evidence がある場合のみ成立し successful result だけでは不十分です。
        source_claims:
          - claim-side-effect-017
    temporal_scope: valid-time
    boundary_context: null
    constraints: []
    examples:
      - id: SideEffect-produced_by-InvocationAttempt-example-positive-manual-001
        labels:
          zh: 由命令执行产生正例
          en: produced by command execution positive case
          ja: コマンド実行により生成の正例
        descriptions:
          zh: invocation-attempt-17 在执行区间写入 config.yaml，se-17 保存尝试引用和文件前后摘要。
          en: invocation-attempt-17 writes config.yaml during its execution interval, and se-17 stores the attempt reference and before/after file digests.
          ja: invocation-attempt-17 が execution interval 内に config.yaml を書き se-17 が attempt reference と file before/after digest を保持します。
        field_values: {}
        related_node_ids:
          - SideEffect
          - InvocationAttempt
        related_relation_ids:
          - SideEffect-produced_by-InvocationAttempt

          en: Assert se-17 produced_by invocation-attempt-17.
          ja: se-17 produced_by invocation-attempt-17 を成立させます。

          en: Execution identity, time, and change evidence jointly close the causal chain.
          ja: execution identity・time・change evidence が causal chain を閉じます。
        source_claims:
          - claim-side-effect-017
      - id: SideEffect-produced_by-InvocationAttempt-example-boundary-manual-001
        labels:
          zh: 由命令执行产生边界例
          en: produced by command execution boundary case
          ja: コマンド実行により生成の境界例
        descriptions:
          zh: invocation-attempt-18 失败且未写文件，之后有人手动修改 config.yaml。
          en: invocation-attempt-18 fails without writing a file; a person modifies config.yaml afterward.
          ja: invocation-attempt-18 は file を書かず失敗し後で人が config.yaml を変更します。
        field_values: {}
        related_node_ids:
          - SideEffect
          - InvocationAttempt
        related_relation_ids:
          - SideEffect-produced_by-InvocationAttempt

          en: Do not attribute the manual change to invocation-attempt-18.
          ja: manual change を invocation-attempt-18 に帰属させません。

          en: Temporal proximity or shared command identity cannot replace execution-change evidence.
          ja: temporal proximity や shared command identity は execution-change evidence を代替できません。
        source_claims:
          - claim-side-effect-017
    source_claims:
      - claim-side-effect-017
    distinct_fact_rationale: null
    layout_role: cross-link
    review_status: accepted
    target: concept:InvocationAttempt
  - id: SideEffect-produced_by-ToolCallAttempt
    predicate: produced_by
    direction: source-to-target
    relation_kind: causal
    labels:
      zh: 由工具调用尝试产生
      en: produced by tool-call attempt
      ja: ツール呼出し試行により生成
    definitions:
      zh: SideEffect 指向实际产生可观察外部变化的同一个 ToolCallAttempt，并要求 attempt 标识、执行时间、目标资源与效应收据一致；ToolCall 级计划、返回文本或未执行的重试不能证明状态改变。
      en: SideEffect points to the same ToolCallAttempt that actually produced the observable external change and requires attempt identity, execution time, target resource, and effect receipt to agree; a ToolCall-level plan, returned text, or unexecuted retry does not prove state change.
      ja: SideEffect は observable external change を実際に生成した同一 ToolCallAttempt を指し attempt identity・execution time・target resource・effect receipt の一致を要求し ToolCall-level plan・returned text・unexecuted retry は state change を証明しません。
    cardinality: null
    cardinality_not_applicable_reason:
      zh: 因果边数量取决于实际观察到的效应实例，不强加统一基数。
      en: The number of causal edges depends on observed effect instances, so no universal cardinality is imposed.
      ja: causal edge 数は observed effect instance に依存し共通 cardinality は課しません。
    inverse_reading:
      predicate: produces_side_effect
      labels:
        zh: 反向：产生副作用
        en: "inverse: produces side effect"
        ja: 逆方向：副作用を生成
    conditions:
      - id: SideEffect-produced_by-ToolCallAttempt-condition-manual-20260717
        severity: error
        expression_language: plain
        expression: Assert only when causal_attempt_ref matches the same ToolCallAttempt, the effect receipt matches the target resource and idempotency key, and the change time lies between attempt start and confirmed completion.
        explanations:
          zh: 仅当 causal_attempt_ref 匹配同一 ToolCallAttempt，效应收据匹配目标资源与幂等键，且变更时间位于尝试开始和确认完成之间时成立。
          en: Assert only when causal_attempt_ref matches the same ToolCallAttempt, the effect receipt matches the target resource and idempotency key, and the change time lies between attempt start and confirmed completion.
          ja: causal_attempt_ref が同一 ToolCallAttempt と一致し effect receipt が target resource と idempotency key に一致し change time が attempt start と confirmed completion の間の場合のみ成立します。
        source_claims:
          - claim-side-effect-018
          - claim-side-effect-019
    temporal_scope: valid-time
    boundary_context: null
    constraints: []
    examples:
      - id: SideEffect-produced_by-ToolCallAttempt-example-positive-manual-001
        labels:
          zh: 由工具调用尝试产生正例
          en: produced by tool-call attempt positive case
          ja: ツール呼出し試行により生成の正例
        descriptions:
          zh: attempt-31 创建工单 INC-778，se-31 的 attempt 引用、资源、幂等键和服务端收据均匹配。
          en: attempt-31 creates ticket INC-778, and se-31 has matching attempt reference, resource, idempotency key, and server receipt.
          ja: attempt-31 が ticket INC-778 を作成し se-31 の attempt reference・resource・idempotency key・server receipt が一致します。
        field_values: {}
        related_node_ids:
          - SideEffect
          - ToolCallAttempt
        related_relation_ids:
          - SideEffect-produced_by-ToolCallAttempt

          en: Assert se-31 produced_by attempt-31.
          ja: se-31 produced_by attempt-31 を成立させます。

          en: The same attempt identity and server-side effect evidence close the causal chain.
          ja: 同一 attempt identity と server-side effect evidence が causal chain を閉じます。
        source_claims:
          - claim-side-effect-018
          - claim-side-effect-019
      - id: SideEffect-produced_by-ToolCallAttempt-example-boundary-manual-001
        labels:
          zh: 由工具调用尝试产生边界例
          en: produced by tool-call attempt boundary case
          ja: ツール呼出し試行により生成の境界例
        descriptions:
          zh: 同一 ToolCall 的 attempt-30 超时，attempt-31 才成功创建工单。
          en: For the same ToolCall, attempt-30 times out and only attempt-31 creates the ticket.
          ja: 同じ ToolCall の attempt-30 は timeout し attempt-31 のみが ticket を作成します。
        field_values: {}
        related_node_ids:
          - SideEffect
          - ToolCallAttempt
        related_relation_ids:
          - SideEffect-produced_by-ToolCallAttempt

          en: Do not attribute the effect to attempt-30 or only to the ToolCall.
          ja: effect を attempt-30 または ToolCall のみに帰属させません。

          en: Call-level membership cannot distinguish the failed attempt from the effect-producing attempt.
          ja: call-level membership では failed attempt と effect-producing attempt を区別できません。
        source_claims:
          - claim-side-effect-018
          - claim-side-effect-019
    source_claims:
      - claim-side-effect-018
      - claim-side-effect-019
    distinct_fact_rationale: null
    layout_role: cross-link
    review_status: accepted
    target: concept:ToolCallAttempt
review:
  status: accepted
  method: manual-semantic
  reviewer: manual-safety-verification
  reviewed_on: 2026-07-17
  note:
    zh: Verified against OpenAI Agents SDK HITL (needs_approval, ToolApprovalItem, RunState resume) and Stripe idempotency; commit gate, request, approval, denial, side effect, and receipt separation holds.
    en: Verified against OpenAI Agents SDK HITL (needs_approval, ToolApprovalItem, RunState resume) and Stripe idempotency; commit gate, request, approval, denial, side effect, and receipt separation holds.
    ja: Verified against OpenAI Agents SDK HITL (needs_approval, ToolApprovalItem, RunState resume) and Stripe idempotency; commit gate, request, approval, denial, side effect, and receipt separation holds.
  sections:
    semantics: accepted
    hierarchy: accepted
    relations: accepted
    structure: accepted
    examples: accepted
    source_claims: accepted
source_claims:
  - id: claim-side-effect-001
    source: eng-security-mcp-nsa-2026
    supports: NSA treats MCP-enabled tool execution as capable of consequential system and data changes and recommends validation, isolation, and logging. Moonweave models the realized external change, separate from request and result records, as SideEffect.
    locator: MCP Security Design Considerations > Security controls for tool execution, validation, logging, and isolation
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-002
    source: eng-proto-mcp-2025-spec
    supports: MCP exposes individual tools/call requests and separate results. Moonweave gives each externally realized mutation occurrence a stable identity.
    locator: MCP 2025-06-18 > Tools > Calling Tools
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-003
    source: eng-proto-mcp-2025-spec
    supports: The MCP protocol represents a concrete tools/call request independently from tool metadata and its result. Moonweave links an external effect to that concrete operation occurrence.
    locator: MCP 2025-06-18 > Tools > tools/call request
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-004
    source: eng-proto-mcp-2025-spec
    supports: The MCP Tools specification separates a tools/call invocation from its structured or unstructured Tool Result. Moonweave infers an external mutation classification from the realized system change, never from result text alone.
    locator: MCP 2025-06-18 > Server features > Tools > Calling Tools and Tool Result
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-005
    source: eng-proto-mcp-2025-spec
    supports: MCP tools can perform actions while Tool Results report content. Moonweave classifies the realized action by external resource change, which is a domain design inference.
    locator: MCP 2025-06-18 > Server features > Tools and Tool Result
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-006
    source: eng-proto-mcp-2025-spec
    supports: A Tool Result may contain structured resource identifiers, but the effect is the external change itself. Moonweave records the external resources independently from returned content.
    locator: MCP 2025-06-18 > Tool Result structuredContent
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-007
    source: eng-proto-mcp-2025-spec
    supports: MCP Tool Results can be structured or unstructured and may report errors. Moonweave requires external change evidence instead of equating a successful result string with a SideEffect.
    locator: MCP 2025-06-18 > Tool Result and isError
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-008
    source: eng-proto-mcp-2025-spec
    supports: MCP separates request and result messages. Moonweave timestamps the realized external effect independently from request transmission.
    locator: MCP 2025-06-18 > Tools message flow
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-009
    source: eng-proto-mcp-2025-spec
    supports: MCP distinguishes invoking a tool from the Tool Result it returns. Moonweave requires proof of realized resource change before asserting SideEffect.
    locator: MCP 2025-06-18 > Calling Tools and Tool Result
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-010
    source: eng-proto-mcp-2025-spec
    supports: The MCP Tools specification defines tool invocation and structured or unstructured results; Moonweave distinguishes an externally realized mutation from the result content and models it as SideEffect.
    locator: MCP 2025-06-18 specification > Server features > Tools > Tool and Tool Result
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-011
    source: eng-fw-openai-hitl
    supports: OpenAI's approval flow pauses a sensitive tool call before execution, grounding the distinction between a pending request and an effect that has occurred.
    locator: Human-in-the-loop > How the approval flow works, steps 1–5
    evidence_kind: official
    confidence: high
    review_status: accepted
  - id: claim-side-effect-012
    source: eng-security-mcp-nsa-2026
    supports: NSA guidance recommends validating tool actions, containing execution, and retaining security logs; the concrete before-and-after event record is Moonweave's auditable representation of an external tool effect.
    locator: MCP Security Design Considerations > Security controls for tool execution, validation, logging, and isolation
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-013
    source: eng-api-stripe-idempotency
    supports: Stripe saves the first executed mutation result for an idempotency key and replays it for matching retries. Moonweave uses the matching key and arguments to bind the realized effect to its request.
    locator: API Reference > Idempotent requests > saved result, parameter comparison, and execution boundary
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-014
    source: eng-api-stripe-idempotency
    supports: Stripe does not save an idempotent result when parameter validation fails or an execution conflict prevents the endpoint from beginning; Moonweave therefore records no realized effect in this boundary case.
    locator: API Reference > Idempotent requests > results saved only after endpoint execution begins
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-015
    source: eng-api-stripe-idempotency
    supports: Stripe binds an idempotency key to the first executed mutation result, compares retry parameters, and saves no result before endpoint execution begins; Moonweave makes the request-to-realized-effect fact explicit.
    locator: API Reference > Idempotent requests > saved result, parameter comparison, and execution boundary
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-016
    source: eng-security-mcp-nsa-2026
    supports: NSA treats network-connected MCP tool actions as consequential and recommends validating and monitoring them. Moonweave records a network call as the cause of a realized SideEffect only when external state changes.
    locator: MCP Security Design Considerations > Recommendations for validation, least privilege, isolation, logging, and recovery
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-017
    source: eng-std-posix-shell-2024
    supports: POSIX.1-2024 defines shell command execution, redirection, environment effects, and exit status. Attribution of a separately observed SideEffect to one InvocationAttempt by identity, time, and before/after evidence is Moonweave's provenance contract.
    locator: POSIX.1-2024 Shell Command Language > command execution environment, redirection, and exit status
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-018
    source: eng-proto-mcp-tools-2025-06-18
    supports: The MCP Tools specification distinguishes a tools/call request from its Tool Result and error path. Moonweave adds attempt identity and effect-receipt evidence so an external SideEffect is attributed to a concrete execution attempt.
    locator: MCP 2025-06-18 > Server Features > Tools > Calling Tools, Tool Result, and Error Handling
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
  - id: claim-side-effect-019
    source: eng-security-mcp-nsa-2026
    supports: NSA MCP security guidance calls for constrained and auditable consequential tool actions. Moonweave applies that guidance by retaining attempt-level cause and receipt evidence for each realized side effect.
    locator: MCP Security Design Considerations > least privilege, isolation, logging, and consequential tool actions
    evidence_kind: design-inference
    confidence: high
    review_status: accepted
