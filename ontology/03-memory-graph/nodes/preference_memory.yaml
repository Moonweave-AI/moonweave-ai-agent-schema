id: node.preference_memory
artifact: NodeClass
label: Preference Memory
label_zh: 偏好记忆
intra_level: 1
intra_group: memory-kind
intra_group_zh: 记忆类型
intra_axis: memory-taxonomy
intra_axis_zh: 记忆类型
intra_role: memory
parent_node: node.memory_store
description: Learned user and agent preferences, stylistic biases, and default choices accumulated over interactions. Influences
  response tone, tool selection, and personalization.
attributes:
  scope:
    type: string
    required: false
    values:
    - user
    - agent
    - session
    - global
    description: Scope to which preferences apply
  decay_factor:
    type: number
    required: false
    description: Rate at which unused preferences lose weight over time
  explicit_only:
    type: boolean
    required: false
    description: Whether only explicitly stated preferences are stored
evidence:
- ref: evidence.paper.cognitive_architecture
layer: memory
plane: memory
