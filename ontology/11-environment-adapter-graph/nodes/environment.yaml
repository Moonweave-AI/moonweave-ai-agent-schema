id: node.environment
artifact: NodeClass
label: Environment
label_zh: 环境
intra_level: 0
intra_group: environment-root
intra_group_zh: 环境根
intra_axis: environment-foundation
intra_axis_zh: 环境根
intra_role: anchor
layer: environment
description: >
  Abstract execution environment representing any external context in which an agent observes state and performs actions.
attributes:
  environment_id:
    type: string
    required: true
    description: Unique environment instance identifier
  display_name:
    type: string
    required: false
    description: Human-readable environment label
  category:
    type: string
    required: true
    description: High-level environment category
interfaces:
  - ref: contract.observable
  - ref: contract.actionable
status: active
