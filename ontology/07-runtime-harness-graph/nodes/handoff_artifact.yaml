id: node.handoff_artifact
artifact: NodeClass
label: Handoff Artifact
label_zh: 交接制品
intra_level: 3
intra_group: observability
intra_group_zh: 可观测性
intra_axis: continuity-observability
intra_axis_zh: 状态连续性 / 可观测性
intra_role: artifact
parent_node: node.transcript
layer: runtime
plane: RuntimePlane
description: 'Serialized context and state package prepared for inter-agent transfer across execution boundaries.

  '
description_zh: >
  为跨执行边界的智能体间传输准备的序列化上下文和状态包。
attributes:
  artifact_id:
    type: string
    required: true
    description: Unique identifier for this handoff package.
  payload_schema_version:
    type: string
    required: true
    description: Version of the serialization schema.
  source_run_id:
    type: string
    required: true
    description: Run that produced this handoff artifact.
status: active
