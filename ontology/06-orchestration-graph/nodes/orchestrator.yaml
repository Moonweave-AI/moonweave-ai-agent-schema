id: node.orchestrator
artifact: NodeClass
label: Orchestrator
label_zh: 编排器
intra_level: 0
intra_group: orchestration-root
intra_group_zh: 编排根
intra_axis: orchestration-foundation
intra_axis_zh: 编排根
intra_role: anchor
layer: orchestration
plane: OrchestrationPlane
description: 'A dynamic task decomposer and coordinator that breaks high-level goals into subtasks, assigns them to workers
  or tools, monitors progress, and aggregates results.

  '
description_zh: >
  动态任务分解器和协调器，将高级目标分解为子任务，将它们分配给工作人员或工具，监控进度并汇总结果。
attributes:
  decomposition_strategy:
    type: enum
    required: true
    values:
    - sequential
    - hierarchical
    - opportunistic
    description: Approach for breaking goals into subtasks.
  aggregation_strategy:
    type: enum
    required: true
    values:
    - concatenate
    - summarize
    - synthesize
    - select_best
    description: How subtask results are combined.
  max_delegation_depth:
    type: integer
    required: false
    description: Maximum nesting depth for subagent delegation chains.
status: active
interfaces:
- ref: contract.orchestrable
- ref: contract.delegable
required_edges:
  outgoing:
  - predicate: DELEGATES_TO
    target_any_of:
    - node.worker
    - node.subagent
  - predicate: COORDINATES
    target: node.group_conversation
