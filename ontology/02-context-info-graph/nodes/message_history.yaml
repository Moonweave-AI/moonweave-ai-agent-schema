id: node.message_history
artifact: NodeClass
label: MessageHistory
label_zh: 消息历史
layer: context-info
plane: InfoPlane
description: >
  Ordered sequence of prior messages retained in context per retention and
  truncation policy, forming the conversational transcript backbone.
attributes:
  max_messages:
    type: integer
    required: false
    description: Maximum messages retained
  retention_policy:
    type: enum
    required: false
    values: [full, sliding_window, summarized]
    description: Strategy for history management under pressure
evidence:
  - ref: evidence.message_assembly_diagram
status: active
