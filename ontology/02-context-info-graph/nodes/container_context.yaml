id: node.container_context
artifact: NodeClass
label: ContainerContext
label_zh: 容器上下文
intra_level: 1
intra_group: context-container
intra_group_zh: 上下文容器
intra_axis: workspace-transcript
intra_axis_zh: 消息转录层 / 上下文容器
intra_role: container
parent_node: node.context_graph
layer: context-info
plane: InfoPlane
description: >
  Bounded envelope grouping command area, storage area, and attached context
  sources. Mirrors the diagram Container concept for scoped agent workspaces.
description_zh: >
  有界信封分组命令区域、存储区域和附加的上下文源。镜像范围智能体工作区的图表容器概念。
attributes:
  container_id:
    type: string
    required: true
    description: Unique container identifier
  isolation_level:
    type: enum
    required: false
    values: [shared, session, agent, user]
    description: Scope of context isolation
evidence:
  - ref: evidence.container_context_diagram
status: active
