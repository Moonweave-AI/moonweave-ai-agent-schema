id: node.agent_message
artifact: NodeClass
label: AgentMessage
label_zh: 智能体消息
intra_level: 3
intra_group: transcript-layer
intra_group_zh: 消息转录层
intra_axis: workspace-transcript
intra_axis_zh: 消息转录层 / 上下文容器
intra_role: output
parent_node: node.message
layer: context-info
plane: InfoPlane
description: >
  Message produced by the agent including natural language responses, tool call
  requests, and structured reasoning outputs.
attributes:
  finish_reason:
    type: enum
    required: false
    values: [stop, tool_calls, length, error]
    description: Why generation ended
  includes_tool_calls:
    type: boolean
    required: false
    description: Whether message contains tool invocation requests
evidence:
  - ref: evidence.message_assembly_diagram
status: active
