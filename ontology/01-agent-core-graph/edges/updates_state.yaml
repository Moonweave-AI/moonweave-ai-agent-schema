id: edge.UPDATES_STATE
artifact: EdgeClass
predicate: UPDATES_STATE
source_domain: node.decision_loop
target_range: node.agent_state
cardinality: "1:1"
description: >
  Each decision loop iteration mutates agent state with new observations,
  plan progress, and intermediate results.
evidence:
  - ref: evidence.coala_decision_loop
status: active
