id: node.agent_state
artifact: NodeClass
label: AgentState
label_zh: 智能体状态
intra_level: 2
intra_group: decision-control
intra_group_zh: 决策控制
intra_role: state
parent_node: node.decision_loop
layer: agent-core
description: >
  Mutable runtime state carried across decision loop iterations: working memory,
  plan progress, tool results cache, and scratch variables.
attributes:
  state_schema:
    type: object
    required: false
    description: Typed schema of state fields
  last_updated:
    type: datetime
    required: false
    description: Timestamp of last state mutation
  checkpoint_id:
    type: string
    required: false
    description: Reference to persisted checkpoint if durable
interfaces:
  - ref: contract.state_aware
evidence:
  - ref: evidence.coala_decision_loop
  - ref: evidence.harness_formalization
status: active
