id: node.agent_identity
artifact: NodeClass
label: AgentIdentity
label_zh: 智能体身份
intra_level: 1
intra_group: identity-boundary
intra_group_zh: 身份与边界
intra_axis: identity-governance
intra_axis_zh: 身份与边界
intra_role: identity
parent_node: node.agent
layer: agent-core
description: >
  Stable identifier and metadata distinguishing one agent instance from another
  across sessions, delegations, and protocol interop.
attributes:
  identity_id:
    type: string
    required: true
    description: Unique agent instance identifier
  display_name:
    type: string
    required: false
    description: User-facing agent name
  version:
    type: string
    required: false
    description: Agent configuration or prompt version
  owner:
    type: string
    required: false
    description: Principal or organization owning the agent
evidence:
  - ref: evidence.a2a_delegation
status: active
