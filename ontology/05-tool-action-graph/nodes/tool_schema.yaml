id: node.tool_schema
artifact: NodeClass
label: Tool Schema
label_zh: 工具模式
description: JSON Schema or equivalent structured definition of a tool's input parameters and output format, enabling validation
  and code generation.
attributes:
  input_schema:
    type: object
    required: true
    description: Schema for tool input arguments
  output_schema:
    type: object
    required: false
    description: Schema for tool output structure
  schema_version:
    type: string
    required: false
    description: Version of the schema format
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
