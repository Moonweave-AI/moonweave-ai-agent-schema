id: node.tool_discovery
artifact: NodeClass
label: Tool Discovery
label_zh: 工具发现
intra_level: 2
intra_group: tool-discovery-plane
intra_group_zh: 工具发现平面
intra_role: process
parent_node: node.tool_registry
description: Process of finding relevant tools from a registry or catalog given a task description, context, or explicit query.
attributes:
  discovery_mode:
    type: string
    required: false
    values:
    - proactive
    - on_demand
    - context_triggered
    description: When discovery is initiated
  max_candidates:
    type: integer
    required: false
    description: Maximum tools to surface per discovery run
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
state_machine:
  ref: state.tool_discovery_lifecycle
