id: node.side_effect
artifact: NodeClass
label: Side Effect
label_zh: 副作用
intra_level: 3
intra_group: action-taxonomy
intra_group_zh: 动作分类
intra_axis: action-effect
intra_axis_zh: 动作分类
intra_role: state
parent_node: node.external_action
description: Observable state change in the environment caused by an external action, requiring policy authorization and audit
  logging.
description_zh: >
  由外部操作引起的环境中可观察的状态变化，需要策略授权和审核日志记录。
attributes:
  effect_type:
    type: string
    required: true
    values:
    - state_mutation
    - data_persistence
    - external_communication
    - resource_allocation
    description: Category of side effect
  reversible:
    type: boolean
    required: false
    description: Whether the effect can be rolled back
  audit_required:
    type: boolean
    required: false
    description: Whether the effect must be audit-logged
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
