id: node.programmatic_tool_calling
artifact: NodeClass
label: Programmatic Tool Calling
label_zh: 程序化工具调用
intra_level: 2
intra_group: invocation-plane
intra_group_zh: 调用平面
intra_axis: invocation-result
intra_axis_zh: 调用平面
intra_role: pattern
parent_node: node.tool_invocation
description: Pattern where the agent generates orchestration code that invokes multiple tools in sequence or parallel within
  a controlled execution environment.
attributes:
  language:
    type: string
    required: false
    values:
    - python
    - javascript
    - shell
    description: Target language for generated code
  max_tools_per_script:
    type: integer
    required: false
    description: Maximum tools invokable in one script
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
