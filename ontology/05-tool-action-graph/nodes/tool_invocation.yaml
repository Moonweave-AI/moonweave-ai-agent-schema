id: node.tool_invocation
artifact: NodeClass
label: Tool Invocation
label_zh: 工具执行
intra_level: 1
intra_group: invocation-plane
intra_group_zh: 调用平面
intra_axis: invocation-result
intra_axis_zh: 调用平面
intra_role: process
parent_node: node.action_space
description: Actual execution instance of a tool call, including runtime context, executor binding, and streaming state.
description_zh: >
  工具调用的实际执行实例，包括运行时上下文、执行器绑定和流状态。
attributes:
  invocation_id:
    type: string
    required: true
    description: Unique invocation identifier
  started_at:
    type: string
    required: false
    description: ISO timestamp when execution began
  streaming:
    type: boolean
    required: false
    description: Whether results are streamed incrementally
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
interfaces:
- ref: contract.invocable
- ref: contract.traceable
