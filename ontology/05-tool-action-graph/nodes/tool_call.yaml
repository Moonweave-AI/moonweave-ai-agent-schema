id: node.tool_call
artifact: NodeClass
label: Tool Call
label_zh: 工具调用
intra_level: 2
intra_group: invocation-plane
intra_group_zh: 调用平面
intra_role: artifact
parent_node: node.tool_invocation
description: Request to invoke a tool with specific arguments, issued by the agent before authorization and execution.
attributes:
  call_id:
    type: string
    required: true
    description: Unique call identifier
  arguments:
    type: object
    required: true
    description: Input arguments for the tool
  requested_at:
    type: string
    required: false
    description: ISO timestamp of call creation
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
state_machine:
  ref: state.tool_call_lifecycle
interfaces:
- ref: contract.traceable
