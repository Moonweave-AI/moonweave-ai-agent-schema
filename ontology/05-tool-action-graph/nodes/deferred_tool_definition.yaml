id: node.deferred_tool_definition
artifact: NodeClass
label: Deferred Tool Definition
label_zh: 延迟加载工具定义
intra_level: 2
intra_group: tool-definition-plane
intra_group_zh: 工具定义平面
intra_axis: definition-discovery
intra_axis_zh: 工具定义平面 / 工具发现平面
intra_role: schema
parent_node: node.tool_definition
description: Tool definition loaded lazily at selection time rather than upfront in context. Reduces initial context size
  while preserving discoverability.
description_zh: >
  工具定义在选择时延迟加载，而不是在上下文中预先加载。减少初始上下文大小，同时保持可发现性。
attributes:
  defer_loading:
    type: boolean
    required: true
    description: Must be true for deferred definitions
  discovery_hint:
    type: string
    required: false
    description: Minimal hint text for search-based loading
  load_on_select:
    type: boolean
    required: false
    description: Whether full schema loads upon selection
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
