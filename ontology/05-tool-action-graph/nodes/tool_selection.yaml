id: node.tool_selection
artifact: NodeClass
label: Tool Selection
label_zh: 工具选择
description: Decision process choosing which matched tool or tool combination to invoke for the current task, considering
  scores, policies, and context.
attributes:
  selection_strategy:
    type: string
    required: false
    values:
    - top_score
    - ensemble
    - user_choice
    - policy_filtered
    description: How the final tool is chosen
  selected_tool_refs:
    type: array
    required: false
    description: References to chosen tools
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
