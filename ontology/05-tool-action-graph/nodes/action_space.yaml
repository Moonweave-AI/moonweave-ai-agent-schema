id: node.action_space
artifact: NodeClass
label: Action Space
label_zh: 动作空间
intra_level: 0
intra_group: action-root
intra_group_zh: 动作根
intra_axis: action-foundation
intra_axis_zh: 动作根
intra_role: anchor
description: Universe of all actions available to an agent, partitioned into internal (cognitive) and external (environment-affecting)
  actions.
description_zh: >
  智能体可用的所有操作的范围，分为内部（认知）和外部（影响环境）操作。
attributes:
  action_count:
    type: integer
    required: false
    description: Total registered actions
  internal_count:
    type: integer
    required: false
    description: Number of internal actions
  external_count:
    type: integer
    required: false
    description: Number of external actions
evidence:
- ref: evidence.paper.cognitive_architecture
layer: tool
plane: tool
