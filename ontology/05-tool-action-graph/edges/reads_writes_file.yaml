id: edge.READS_WRITES_FILE
artifact: EdgeClass
predicate: READS_WRITES_FILE
source_domain: node.file_action
target_range: node.file_action
description: File actions perform read or write operations on the file system. READS_FILE and WRITES_FILE are distinguished
  by the operation attribute on the file action node.
cardinality: N:M
