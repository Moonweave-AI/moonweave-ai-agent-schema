id: node.provider_adapter
artifact: NodeClass
label: Provider Adapter
label_zh: 提供者适配器
intra_level: 2
intra_group: adapter-plane
intra_group_zh: 适配器平面
intra_axis: adapters-bindings
intra_axis_zh: 适配器平面
intra_role: adapter
parent_node: node.model_adapter
layer: sdk
description: >
  Generic adapter abstracting differences between model, tool, or storage provider implementations behind a uniform SDK interface.
description_zh: >
  通用适配器抽象了统一 SDK 接口背后的模型、工具或存储提供程序实现之间的差异。
attributes:
  provider_type:
    type: enum
    required: true
    values: ['model', 'tool', 'storage', 'transport']
    description: Category of provider being adapted
  provider_id:
    type: string
    required: true
    description: Logical provider identifier without vendor naming
  capability_profile_ref:
    type: string
    required: false
    description: Reference to provider capability profile
status: active
