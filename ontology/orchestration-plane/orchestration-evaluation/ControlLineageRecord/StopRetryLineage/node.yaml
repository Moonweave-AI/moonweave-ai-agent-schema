schema: moonweave.ai/ontology-node/v1
id: StopRetryLineage
kind: concept
labels:
  zh: 停止—重试谱系
  en: stop-retry lineage
  ja: 停止—再試行系譜
semantics:
  short_definition:
    zh: 把一个来源尝试与导致继续、重试或停止的明确 StopCondition 共同记录的控制谱系记录。
    en: A control-lineage record jointly recording a source attempt and the explicit StopCondition that caused continue, retry, or stop.
    ja: source attempt と continue/retry/stop を生じた explicit StopCondition を共同記録する control-lineage record です。
  definition:
    zh: 停止—重试谱系是 ControlLineageRecord 的子类，以 lineage_record_id、source_attempt_ref 和 stop_condition_ref 为身份；它通过 StopRetryLineage-records_source_attempt-ImprovementAttempt 明确记录来源尝试，通过 StopRetryLineage-records_stop_condition-StopCondition 明确记录实际求值的停止条件，并保存选定动作以及后继尝试或循环终止结果，使“从哪次尝试为何重试或停止”可查询。它不是 RevisionArtifact，也不会用一条含混边压缩整条控制链。
    en: StopRetryLineage is a ControlLineageRecord identified by lineage-record ID, source-attempt reference, and stop-condition reference. StopRetryLineage-records_source_attempt-ImprovementAttempt explicitly records the source attempt, and StopRetryLineage-records_stop_condition-StopCondition explicitly records the evaluated stop condition; the record also preserves the selected action and successor-attempt or loop-termination outcome so that why a particular attempt retried or stopped is queryable. It is not a RevisionArtifact and does not compress the control chain into one ambiguous edge.
    ja: StopRetryLineage は lineage_record_id、source_attempt_ref、stop_condition_ref で識別する ControlLineageRecord である。StopRetryLineage-records_source_attempt-ImprovementAttempt が出所となる試行を明示し、StopRetryLineage-records_stop_condition-StopCondition が実際に評価した停止条件を明示する。さらに選択した action と後続試行または loop termination の結果を保持し、どの試行がなぜ再試行または停止したかを query 可能にする。RevisionArtifact ではなく、制御連鎖を一つの曖昧な edge に圧縮しない。
  why_needed:
    zh: 专门下位概念补齐重试和停止的共同证据结构，并把旧的无端点谓词拆成可核验的两条引用。
    en: This subtype completes the shared evidence structure for retry and stop and replaces old endpoint-poor predicates with two verifiable references.
    ja: retry/stop の shared evidence structure を完成し old endpoint-poor predicates を two verifiable references へ置換します。
  includes:
    - zh: 同时记录来源 ImprovementAttempt、被求值 StopCondition、求值结果、动作及后继或终止目标的谱系记录。
      en: Lineage records jointly storing source ImprovementAttempt, evaluated StopCondition, evaluation result, action, and successor or termination target.
      ja: source ImprovementAttempt・evaluated StopCondition・result・action・successor/termination target を共同保存する lineage record。
  excludes:
    - zh: 普通 ControlLineageRecord、RevisionArtifact、StopCondition 本身、循环当前状态和没有来源尝试的静态停止规则。
      en: Generic ControlLineageRecord, RevisionArtifact, StopCondition itself, current loop state, and static stop rules without a source attempt.
      ja: generic ControlLineageRecord・RevisionArtifact・StopCondition 自体・current loop state・source attempt なし static rule。
  semantic_kind: information
  aliases: []
engineering:
  explanation:
    zh: >-
      改进域节点把 OpenAI Agent Evals 的 trace、grader 与 eval run 流程投影为 PROV 风格活动、规范、
      状态与谱系记录。实现用 Responses API 或 Agents SDK 返回结构化评估结果，并在此记录 ImprovementActivity
      发生，不把 API wire object 重命名成本体类型。
    en: >-
      Improvement-domain nodes project OpenAI Agent Evals trace, grader, and eval-run flows into PROV-style
      activities, specifications, state, and lineage records. Implementations use the Responses API or Agents SDK
      for structured evaluation output and record ImprovementActivity occurrences here without renaming API wire
      objects as ontology types.
    ja: >-
      improvement domain ノードは OpenAI Agent Evals の trace/grader/eval run flow を PROV 風 activity/spec/state/
      lineage record に投影する。Responses API または Agents SDK の structured evaluation output を用い API wire object
      を ontology 型に改名しない。
  typical_input:
    - description:
        zh: OpenAI Agent Evals 对候选工件启动评估或改进循环的输入。
        en: OpenAI Agent Evals input starting evaluation or an improvement loop on a candidate artifact.
        ja: 候補 artifact に対する evaluation/improvement loop 開始入力。
      format: |-
        {"target_ref":"artifact-report@7","improvement_control_specification_ref":"review-assignment-security@3","dataset_item_ref":"eval-case-12"}
  typical_output:
    - description:
        zh: 带时间与状态字段的本地改进活动或控制记录。
        en: Local improvement activity or control record with time and status fields.
        ja: time/status field を持つ local improvement/control record。
      format: |-
        {"improvement_activity_id":"improvement-run-42","target_ref":"artifact-report@7","status":"completed","started_at":"2026-07-18T10:00:00Z","ended_at":"2026-07-18T10:06:00Z"}
  reference_implementations:
    - name: OpenAI Agent evals
      url: https://platform.openai.com/docs/guides/agent-evals
      description: Documents trace-based evaluation, graders, datasets, and eval runs.
    - name: OpenAI Responses API
      url: https://platform.openai.com/docs/api-reference/responses
      description: Documents structured model responses used by evaluator and optimizer agents.
    - name: W3C PROV-O
      url: https://www.w3.org/TR/prov-o/
      description: Separates activities, entities, agents, and lineage records.
  source_claims:
    - claim-improvement-activity-001
    - claim-improvement-activity-003
structure:
  identity_keys:
    - lineage_record_id
  fields:
    - id: source_attempt_ref
      labels:
        zh: 来源尝试引用
        en: source-attempt reference
        ja: source attempt ref
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 引用触发该控制判定的 ImprovementAttempt。
        en: References the ImprovementAttempt that triggered this control decision.
        ja: control decision を trigger した ImprovementAttempt を参照します。
      allowed_values: []
      pattern: null
      example_value: eval-attempt-42-2
      source_claims:
        - claim-stop-retry-lineage-001
        - claim-stop-retry-lineage-003
    - id: stop_condition_ref
      labels:
        zh: 停止条件引用
        en: stop-condition reference
        ja: stop condition ref
      datatype: reference
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 引用被实际求值并决定重试或停止的 StopCondition。
        en: References the StopCondition actually evaluated to decide retry or stop.
        ja: retry/stop を決めるため実際に evaluated された StopCondition を参照します。
      allowed_values: []
      pattern: null
      example_value: quality-threshold@4
      source_claims:
        - claim-stop-retry-lineage-001
        - claim-stop-retry-lineage-002
    - id: observed_evidence
      labels:
        zh: 条件求值观测证据
        en: condition-evaluation evidence
        ja: condition evaluation evidence
      datatype: object
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 保存 stop_condition_ref 在 source_attempt_ref 之后实际读取的最小观测键值，例如 quality_score=0.92；它必须足以复核判定，但不复制完整 EvaluationRun 输出。
        en: Preserves the minimal observed key-value evidence actually read by stop_condition_ref after source_attempt_ref, such as quality_score=0.92. It must support replay of the decision without copying the full EvaluationRun output.
        ja: source_attempt_ref 後に stop_condition_ref が実際に読んだ最小 observed key-value evidence、たとえば quality_score=0.92 を保持します。decision を再検証できる必要がありますが full EvaluationRun output は複製しません。
      allowed_values: []
      pattern: null
      example_value:
        quality_score: 0.92
      source_claims:
        - claim-stop-retry-lineage-004
    - id: condition_result
      labels:
        zh: 停止条件求值结果
        en: stop-condition evaluation result
        ja: stop condition evaluation result
      datatype: boolean
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 记录 stop_condition_ref 对 observed_evidence 的实际布尔求值结果；不得从 selected_action 反向臆测该结果。
        en: Records the actual Boolean result of evaluating stop_condition_ref against observed_evidence. It must not be guessed backward from selected_action.
        ja: stop_condition_ref を observed_evidence に対して評価した実際の Boolean result を記録します。selected_action から逆推測してはなりません。
      allowed_values: []
      pattern: null
      example_value: true
      source_claims:
        - claim-stop-retry-lineage-005
    - id: selected_action
      labels:
        zh: 停止—重试决定
        en: stop-or-retry decision
        ja: stop-or-retry decision
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 记录本次条件求值后选定的互斥控制动作：创建一个明确后继尝试，或结束当前循环。它描述已经发生的决定，不是 StopCondition 规则文本。
        en: "Records the mutually exclusive control action selected after this condition evaluation: create a named successor attempt or terminate the current loop. It describes the executed decision, not the StopCondition rule text."
        ja: この condition evaluation 後に選択された排他的 control action、すなわち明示された successor attempt を作るか current loop を終了するかを記録します。StopCondition rule text ではなく実行済み decision です。
      allowed_values:
        - id: StopRetryActionRetry
          value: retry
          labels:
            zh: 创建后继尝试
            en: retry
            ja: retry
          definitions:
            zh: 条件求值未要求结束循环，控制链创建并引用一个新的后继 ImprovementAttempt。
            en: The condition evaluation does not require loop termination, so the control chain creates and references a new successor ImprovementAttempt.
            ja: condition evaluation が loop termination を要求せず、control chain が新しい successor ImprovementAttempt を作成し参照します。
          status: accepted
          source_claims:
            - claim-stop-retry-lineage-006
        - id: StopRetryActionStop
          value: stop
          labels:
            zh: 结束循环
            en: stop
            ja: stop
          definitions:
            zh: 条件求值满足终止依据，控制链记录循环终止事实且不创建后继尝试。
            en: The condition evaluation satisfies the termination basis, so the control chain records loop termination and creates no successor attempt.
            ja: condition evaluation が termination basis を満たし、control chain が loop termination を記録して successor attempt を作成しません。
          status: accepted
          source_claims:
            - claim-stop-retry-lineage-007
      pattern: null
      example_value: stop
      source_claims:
        - claim-stop-retry-lineage-008
    - id: successor_attempt_ref
      labels:
        zh: 后继尝试引用
        en: successor-attempt reference
        ja: successor attempt 参照
      datatype: reference
      required: false
      cardinality:
        min: 0
        max: 1
      definitions:
        zh: selected_action=retry 时引用由该决定创建的唯一后继 ImprovementAttempt；selected_action=stop 时必须为空或显式为 null。
        en: When selected_action is retry, references the single successor ImprovementAttempt created by the decision. It must be absent or explicitly null when selected_action is stop.
        ja: selected_action=retry の場合、decision が作成した唯一の successor ImprovementAttempt を参照します。selected_action=stop では空または明示的 null でなければなりません。
      allowed_values: []
      pattern: null
      example_value: revision-attempt-42-3
      source_claims:
        - claim-stop-retry-lineage-009
    - id: loop_termination_ref
      labels:
        zh: 循环终止事实引用
        en: loop-termination fact reference
        ja: loop termination fact 参照
      datatype: reference
      required: false
      cardinality:
        min: 0
        max: 1
      definitions:
        zh: selected_action=stop 时引用由本次条件求值形成的循环终止事实，并保留循环身份与条件版本；selected_action=retry 时必须为空或显式为 null。
        en: When selected_action is stop, references the loop-termination fact established by this condition evaluation while preserving loop identity and condition revision. It must be absent or explicitly null for retry.
        ja: selected_action=stop の場合、この condition evaluation が成立させた loop-termination fact を参照し loop identity と condition revision を保持します。retry では空または明示的 null でなければなりません。
      allowed_values: []
      pattern: null
      example_value: loop-42#completed:quality-threshold@4
      source_claims:
        - claim-stop-retry-lineage-010
  constraints:
    - id: stop-retry-lineage-has-exclusive-outcome
      severity: error
      expression_language: plain
      expression: selected_action = retry IFF (successor_attempt_ref IS NON-NULL AND (loop_termination_ref IS ABSENT OR NULL)); selected_action = stop IFF ((successor_attempt_ref IS ABSENT OR NULL) AND loop_termination_ref IS NON-NULL)
      explanations:
        zh: 每条谱系记录必须在后继尝试与循环终止之间二选一，不能同时连接两个结果，也不能两者皆空。
        en: Each lineage record chooses exactly one of successor attempt and loop termination; it cannot point to both outcomes or to neither.
        ja: 各 lineage record は successor attempt と loop termination のちょうど一方を選び、両方またはどちらもない状態を許しません。
      source_claims:
        - claim-stop-retry-lineage-011
  required_relation_constraints: []
examples:
  - id: StopRetryLineage-example-positive-manual-20260716
    labels:
      zh: StopRetryLineage positive
      en: StopRetryLineage positive
      ja: StopRetryLineage positive
    descriptions:
      zh: Reflexion (Shinn et al., 2023) 对 StopRetryLineage 实现执行→失败→verbal self-reflection→重试：reflexion_agent.run() → if failed: reflect(trajectory) → retry with reflection；AlfWorld 上 verbal feedback 提升成功率。
      en: Reflexion (Shinn et al., 2023) for StopRetryLineage: run() → if failed: reflect(trajectory) → retry with reflection; verbal feedback improves AlfWorld success rate.
      ja: Reflexion (Shinn et al., 2023) StopRetryLineage: run→failed→reflect(trajectory)→retry；AlfWorld 成功率向上。
    field_values: {lineage_record_id: lineage-42-3, recorded_at: 2026-07-16T10:04:05Z, source_attempt_ref: eval-attempt-42-2, stop_condition_ref: "quality-threshold@4"}

    related_node_ids:
      - StopRetryLineage
      - ImprovementAttempt
      - StopCondition
      - ImprovementLoop
    related_relation_ids:
      - StopRetryLineage-records_source_attempt-ImprovementAttempt
      - StopRetryLineage-records_stop_condition-StopCondition
    source_claims:
      - claim-stop-retry-lineage-001
      - claim-stop-retry-lineage-002
      - claim-stop-retry-lineage-003

  - id: StopRetryLineage-example-boundary-manual-20260716
    labels:
      zh: StopRetryLineage boundary
      en: StopRetryLineage boundary
      ja: StopRetryLineage boundary
    descriptions:
      zh: Temporal workflow history 是运行时事件溯源，不是 StopRetryLineage WorkflowStep 规范；DurableExecution 实例与规约应区分。
      en: Temporal workflow history is runtime event sourcing, not StopRetryLineage WorkflowStep spec; separate DurableExecution instances from specification.
      ja: Temporal workflow history は runtime で StopRetryLineage WorkflowStep spec ではない。
    field_values: {}

    related_node_ids:
      - StopRetryLineage
      - StopCondition
    related_relation_ids: []
    source_claims:
      - claim-stop-retry-lineage-001
      - claim-stop-retry-lineage-002

  - id: StopRetryLineage-example-counterexample-confusable
    labels:
      zh: StopRetryLineage counterexample
      en: StopRetryLineage counterexample
      ja: StopRetryLineage counterexample
    descriptions:
      zh: 普通 Python 函数调用链无 @flow/@task 装饰不能作为 Prefect 式 StopRetryLineage PipelineComposition。
      en: Plain Python function chains without @flow/@task decorators cannot serve as Prefect-style StopRetryLineage PipelineComposition.
      ja: @flow/@task なし Python 链は Prefect 式 StopRetryLineage ではない。
    field_values: {lineage_record_id: absent, stop_basis_ref: absent, log_message: "max retries exceeded"}

    related_node_ids:
      - StopRetryLineage
      - ControlLineageRecord
      - ImprovementLoop
      - ImprovementAttempt
      - StopCondition
    related_relation_ids: []
    source_claims:
      - claim-stop-retry-lineage-001

  - id: StopRetryLineage-example-instance-concrete
    labels:
      zh: StopRetryLineage instance
      en: StopRetryLineage instance
      ja: StopRetryLineage instance
    descriptions:
      zh: Reflexion 实例：attempt_1 failed → reflect("I should have checked API docs first") → attempt_2 with reflection → 映射 StopRetryLineage。
      en: Reflexion instance: attempt_1 failed → reflect("I should have checked API docs first") → attempt_2 with reflection → maps to StopRetryLineage.
      ja: Reflexion 实例 failed→reflect→retry が StopRetryLineage に写像。
    field_values: {lineage_record_id: lineage-42-3, recorded_at: 2026-07-16T10:04:05Z, source_attempt_ref: eval-attempt-42-2, stop_condition_ref: "quality-threshold@4"}

    related_node_ids:
      - StopRetryLineage
      - ImprovementAttempt
      - StopCondition
      - ImprovementLoop
    related_relation_ids: []
    source_claims:
      - claim-stop-retry-lineage-001

sources:
  - id: openai-agent-evals
    title: OpenAI Agent evals
    url: https://platform.openai.com/docs/guides/agent-evals
    source_type: official-doc
    relevance: Documents trace-based evaluation, graders, datasets, and eval runs for improvement loops.
  - id: openai-responses-api
    title: OpenAI Responses API
    url: https://platform.openai.com/docs/api-reference/responses
    source_type: official-doc
    relevance: Documents structured evaluator and optimizer model responses.
  - id: w3c-prov-o
    title: W3C PROV-O
    url: https://www.w3.org/TR/prov-o/
    source_type: standard
    relevance: Separates activities, entities, agents, and lineage records in improvement workflows.
source_claims:
  - id: claim-improvement-activity-001
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-improvement-activity-001 to anchor improvement activity 001 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-improvement-activity-003
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-improvement-activity-003 to anchor improvement activity 003 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-001
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-001 to anchor stop retry lineage 001 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-002
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-002 to anchor stop retry lineage 002 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-003
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-003 to anchor stop retry lineage 003 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-004
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-004 to anchor stop retry lineage 004 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-005
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-005 to anchor stop retry lineage 005 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-006
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-006 to anchor stop retry lineage 006 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-007
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-007 to anchor stop retry lineage 007 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-008
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-008 to anchor stop retry lineage 008 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-009
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-009 to anchor stop retry lineage 009 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-010
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-010 to anchor stop retry lineage 010 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
  - id: claim-stop-retry-lineage-011
    source: openai-agent-evals
    supports: Moonweave records StopRetryLineage using claim claim-stop-retry-lineage-011 to anchor stop retry lineage 011 semantics against the cited official source rather than renaming API wire objects as ontology types.
    locator: Agent evals guide, trace-based evaluation and eval runs
    evidence_kind: design-inference
    confidence: high
