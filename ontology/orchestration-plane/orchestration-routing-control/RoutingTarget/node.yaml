schema: moonweave.ai/ontology-node/v1
id: RoutingTarget
kind: concept
status: review
labels:
  zh: 路由目标
  en: routing target
  ja: routing target
semantics:
  short_definition:
    zh: 可被路由决定选择、且能解析为具体控制流入口的类型化目标引用。
    en: A typed target reference selectable by a routing decision and resolvable to a concrete control-flow entry point.
    ja: routing decision が select でき concrete control-flow entry point に resolve する typed target reference。
  definition:
    zh: 路由目标是一种可在运行选择前独立存在的信息对象，以目标标识、目标类型、解析引用、能力约束和可用状态为身份，表示 worker、tool、branch、adapter、human queue、operation entry 或 stop endpoint 等可选落点。它只回答“控制可以去哪里”，不是执行主体的完整定义，也不证明“已经选择”或“已经开始执行”。当引用解析到某个操作入口时，后续实际活动仍以独立的 DownstreamOperation 标识、开始时间和状态记录。
    en: A RoutingTarget is an information object that may exist independently before runtime selection, identified by target ID, target type, resolution reference, capability constraints, and availability state. It represents an eligible worker, tool, branch, adapter, human queue, operation entry, or stop endpoint and answers only where control may go. It is neither the full executor definition nor evidence that selection or execution occurred. When it resolves to an operation entry point, the subsequent activity is still recorded separately with a DownstreamOperation ID, start time, and state.
    ja: RoutingTarget は runtime selection 前から独立して存在できる information object で、target ID・type・resolution ref・capability constraint・availability で識別します。worker/tool/branch/adapter/human queue/operation entry/stop endpoint など「control が行ける場所」だけを表し、executor 完全定義や selection/execution occurrence の証明ではありません。operation entry に resolve しても、後続 activity は DownstreamOperation ID・start time・state で別に記録します。
  why_needed:
    zh: 统一类型化目标引用，可以让 Route 与 RoutingDecision 指向同一可解析对象，同时避免把 worker、工具或操作复制进本模块。
    en: A common typed target reference lets Route and RoutingDecision point to the same resolvable object without copying workers, tools, or operations into this module.
    ja: common typed target reference により Route/Decision が同じ resolvable object を指し worker/tool/operation を本 module に複製しません。
  includes:
    - zh: 指向 worker、tool、workflow node、protocol adapter、人工队列、下游操作或停止端点的解析引用。
      en: Resolvable references to workers, tools, workflow nodes, protocol adapters, human queues, downstream operations, or stop endpoints.
      ja: worker・tool・workflow node・adapter・human queue・downstream operation・stop endpoint への resolvable reference。
  excludes:
    - zh: 实际 worker 实例、工具定义、一次选择记录、执行尝试和无类型字符串地址。
      en: An actual worker instance, tool definition, selection record, execution attempt, and untyped string address.
      ja: actual worker instance・tool definition・selection record・execution attempt・untyped string address。
  semantic_kind: information
  aliases: []
structure:
  identity_keys:
    - routing_target_id
  fields:
    - id: routing_target_id
      labels:
        zh: 路由目标标识
        en: routing-target ID
        ja: routing-target ID
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 稳定标识一个可选控制流落点。
        en: Stably identifies one eligible control-flow destination.
        ja: eligible control-flow destination を stable に識別します。
      allowed_values: []
      pattern: null
      example_value: target-billing-worker
      source_claims:
        - claim-routing-target-001
        - claim-routing-target-002
    - id: target_type
      labels:
        zh: 目标类型
        en: target type
        ja: target type
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 限定解析结果为 worker、tool、branch、adapter、human、operation 或 stop。
        en: Constrains resolution to worker, tool, branch, adapter, human, operation, or stop.
        ja: resolution result を worker/tool/branch/adapter/human/operation/stop に制約します。
      allowed_values: []
      pattern: null
      example_value: worker
      source_claims:
        - claim-routing-target-001
    - id: resolves_to_ref
      labels:
        zh: 解析引用
        en: resolves-to reference
        ja: resolves-to reference
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 引用由其规范所有模块维护的具体对象或入口。
        en: References the concrete object or entry point maintained by its owning module.
        ja: owner module が管理する concrete object/entry point を参照します。
      allowed_values: []
      pattern: null
      example_value: worker:billing-worker-2@5
      source_claims:
        - claim-routing-target-001
    - id: required_capabilities
      labels:
        zh: 所需能力
        en: required capabilities
        ja: required capabilities
      datatype: object
      required: false
      cardinality:
        min: 0
        max: 1
      definitions:
        zh: 声明选择该目标前必须满足的能力约束。
        en: Declares capability constraints that must hold before selecting the target.
        ja: target selection 前に満たす capability constraint を宣言します。
      allowed_values: []
      pattern: null
      example_value:
        - billing.read
        - billing.explain
      source_claims:
        - claim-routing-target-002
    - id: availability_state
      labels:
        zh: 可用状态
        en: availability state
        ja: availability state
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 记录候选生成时目标是可用、降级、暂停或不可用。
        en: Records whether the target is available, degraded, paused, or unavailable when candidates are formed.
        ja: candidate formation 時の available/degraded/paused/unavailable を記録します。
      allowed_values: []
      pattern: null
      example_value: available
      source_claims:
        - claim-routing-target-002
  constraints: []
  required_relation_constraints: []
examples:
  - id: RoutingTarget-example-positive-manual-20260716
    kind: positive
    labels:
      zh: 账单专员目标引用
      en: Billing-worker target reference
      ja: billing-worker target reference
    scenario_id: null
    descriptions:
      zh: target-billing-worker@5 类型为 worker，解析到 worker:billing-worker-2@5，要求 billing.read 与 billing.explain，并在候选生成时可用。
      en: target-billing-worker@5 is a worker target resolving to worker:billing-worker-2@5, requires billing.read and billing.explain, and is available when candidates are formed.
      ja: target-billing-worker@5 は worker:billing-worker-2@5 に resolve し billing.read/explain を要求し candidate formation 時 available です。
    field_values:
      routing_target_id: target-billing-worker
      target_type: worker
      resolves_to_ref: worker:billing-worker-2@5
      required_capabilities:
        - billing.read
        - billing.explain
      availability_state: available
    related_node_ids:
      - RoutingTarget
      - Route
      - RoutingDecision
    related_relation_ids:
      - Route-points_to-RoutingTarget
      - RoutingDecision-selects-RoutingTarget
    expected_result:
      zh: 创建轻量目标引用，具体 worker 仍由角色与委派域维护。
      en: Create a lightweight target reference while the concrete worker remains owned by the role/delegation domain.
      ja: lightweight target ref を作り concrete worker は role/delegation domain が所有します。
    why_valid_or_invalid:
      zh: 目标身份、类型、解析和约束完整，但未复制 worker 定义。
      en: Target identity, type, resolution, and constraints are complete without duplicating the worker definition.
      ja: target identity/type/resolution/constraint が完全で worker definition を複製しません。
    synthetic: true
    verified_version: https://moonweave.ai/ontology/agent-system/2.0.0/
    source_claims:
      - claim-routing-target-001
      - claim-routing-target-002
  - id: RoutingTarget-example-boundary-manual-20260716
    kind: boundary
    labels:
      zh: 账单 worker 正在执行
      en: Billing worker is executing
      ja: billing worker executing
    scenario_id: routing-review-routingtarget-boundary
    descriptions:
      zh: worker-run-44 在 04:22 处理账单请求并调用工具；这是执行发生，不是候选目标引用。
      en: worker-run-44 processes a billing request and invokes a tool at 04:22; this is execution, not a candidate-target reference.
      ja: worker-run-44 が 04:22 billing request を処理し tool invoke するのは execution occurrence で target reference ではありません。
    field_values: {}
    related_node_ids:
      - RunAttempt
      - RoutingTarget
    related_relation_ids: []
    expected_result:
      zh: 归运行时尝试，并引用选择它的 RoutingDecision。
      en: Keep it as a runtime attempt and reference the RoutingDecision that selected it.
      ja: runtime attempt とし selecting RoutingDecision を参照します。
    why_valid_or_invalid:
      zh: 执行具有时间、输入和结果；目标引用只提供可解析候选身份。
      en: Execution has time, input, and outcome; a target reference only provides resolvable candidate identity.
      ja: execution は time/input/outcome、target ref は resolvable candidate identity のみです。
    synthetic: true
    verified_version: https://moonweave.ai/ontology/agent-system/2.0.0/
    source_claims:
      - claim-routing-target-003
      - claim-routing-target-002
external_mappings: []
applicability:
  - core
sibling_differentiation: []
parent_relation: null
relations: []
review:
  status: pending
  method: manual-semantic
  reviewer: null
  reviewed_on: null
  note:
    zh: 该节点仅完成了从现行候选数据到唯一 YAML 树的无损转录；尚未逐项完成人工语义核验，不得据此宣称已审定。
    en: This node has only been transcribed without loss from the current candidate data into the sole YAML tree; item-by-item manual semantic review is still pending.
    ja: このノードは現行候補データから唯一の YAML ツリーへ欠落なく転記した段階であり、項目別の手動意味審査は未完了です。
  sections:
    semantics: pending
    hierarchy: pending
    relations: pending
    structure: pending
    examples: pending
    source_claims: []
source_claims:
  - id: claim-routing-target-001
    source: eng-fw-langgraph-graph-api
    supports: LangGraph's Graph API distinguishes workflow state, executable nodes, static edges, conditional routing functions, termination targets, and Command values that combine state updates with a selected destination. Moonweave uses this to separate reusable routing specifications and targets from one RoutingDecision occurrence.
    locator: LangGraph Graph API documentation, Graphs, Edges, Conditional edges, and Command sections
    evidence_kind: design-inference
    confidence: high
    review_status: pending
  - id: claim-routing-target-002
    source: eng-fw-autogen-agentchat
    supports: AutoGen SelectorGroupChat selects a next participant from described candidates using current conversation context and stops when an explicit termination condition is satisfied. Moonweave uses this as evidence for typed routing targets, selection decisions, and separately evaluated stop conditions.
    locator: AutoGen AgentChat documentation, Selector Group Chat workflow and Termination Conditions
    evidence_kind: design-inference
    confidence: high
    review_status: pending
  - id: claim-routing-target-003
    source: eng-ont-prov-o
    supports: W3C PROV-O distinguishes plans and other Entities from time-bounded Activities that use or generate Entities. Moonweave therefore treats routing policy and conditions as reusable specifications, RoutingDecision as an occurrence, and GateOutcome as information produced by gate evaluation.
    locator: W3C PROV-O Recommendation Sections 3.1 and 3.2, Entity, Activity, generation, and Plan
    evidence_kind: design-inference
    confidence: high
    review_status: pending
