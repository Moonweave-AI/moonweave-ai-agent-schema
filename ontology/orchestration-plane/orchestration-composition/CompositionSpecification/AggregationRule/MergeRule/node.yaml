schema: moonweave.ai/ontology-node/v1
id: MergeRule
kind: concept
labels:
  zh: 内容合并规则
  en: content merge rule
  ja: 内容マージ規則
semantics:
  short_definition:
    zh: 用合并键、重复项策略、字段冲突策略和输出顺序组合结构化内容的聚合规则。
    en: An aggregation rule combining structured content through a merge key, duplicate policy, field-conflict policy, and output-order policy.
    ja: merge key・duplicate policy・field-conflict policy・output-order policy で structured content を結合する aggregation rule です。
  definition:
    zh: 内容合并规则是 AggregationRule 的下位类型；它继承输入资格、聚合方法、完成与一般冲突语义，并以记录身份键、重复记录处置、同字段不兼容值处置和结果排序为种差。它是可复用的组合规范，不是某次合并活动、合并后的工件，也不通过选票阈值裁决。
    en: A MergeRule is an AggregationRule subtype inheriting input eligibility, method, completion, and general conflict semantics and distinguished by record identity key, duplicate-record treatment, incompatible field-value treatment, and result ordering. It is a reusable composition specification, not a merge activity, merged artifact, or ballot-threshold adjudication.
    ja: MergeRule は input eligibility・method・completion・general conflict semantics を継承し、record identity key・duplicate record treatment・incompatible field-value treatment・result ordering を種差とする AggregationRule 下位型です。merge activity・merged artifact・ballot threshold adjudication ではありません。
  why_needed:
    zh: 显式区分重复记录、字段冲突与结果顺序，才能避免实现把 JSON 对象覆盖、列表拼接或“最后写入胜出”误当成同一种聚合语义。
    en: Separating duplicate records, field conflicts, and result order prevents implementations from treating object overwrite, list concatenation, and last-write-wins as the same aggregation semantics.
    ja: duplicate record・field conflict・result order を分けることで object overwrite・list concatenation・last-write-wins を同一 semantics と誤認しません。
  includes:
    - zh: 按稳定键汇集结构化记录、拒绝或归并重复项、对字段冲突作显式决定并确定输出顺序的可版本化规范。
      en: Versioned specifications that collect structured records by stable key, reject or consolidate duplicates, decide field conflicts explicitly, and determine output order.
      ja: stable key で structured record を集め、duplicate を reject/consolidate、field conflict を明示処理し output order を定める versioned specification。
  excludes:
    - zh: 法定人数与胜出阈值、实际输入记录集合、一次合并运行、合并输出，以及未声明重复和顺序语义的偶然覆盖行为。
      en: Quorum and winning thresholds, concrete input-record sets, one merge run, merged output, and accidental overwrite behavior without declared duplicate and ordering semantics.
      ja: quorum/winning threshold・concrete input set・one merge run・merged output、および duplicate/order semantics を宣言しない偶発的 overwrite。
  semantic_kind: specification
  aliases: []
engineering:
  explanation:
    zh: LangGraph reducer、LangChain StuffDocumentsChain 与 conflict-preserving merge 策略映射为 MergeRule；须声明字段冲突与缺失处理。
    en: LangGraph reducers, LangChain StuffDocumentsChain, and conflict-preserving merge strategies map to MergeRule with field conflict and absence handling.
    ja: LangGraph reducer・LangChain StuffDocumentsChain・conflict-preserving merge を MergeRule へ写像し field conflict/absence handling を宣言します。
  typical_input:
    - description: Conflict-preserving merge spec.
      format: '{"merge_rule_id": "conflict-preserving", "version": "2", "on_conflict": "preserve_both"}'
      source_claims:
        - claim-merge-rule-design
  typical_output:
    - description: Moonweave MergeRule.
      format: '{"merge_rule_id": "conflict-preserving", "version": "2"}'
      source_claims:
        - claim-merge-rule-design
  reference_implementations:
    - name: LangGraph
      url: https://langchain-ai.github.io/langgraph/
      description: Send API fan-out, sequential edges, fan-in reducers.
    - name: CrewAI
      url: https://docs.crewai.com
      description: Process.sequential/parallel crew composition.
    - name: Anthropic workflow patterns
      url: https://www.anthropic.com/engineering/building-effective-agents
      description: Prompt chaining, parallelization, orchestrator-worker.
    - name: OpenAI Cookbook
      url: https://cookbook.openai.com/
      description: Multi-step pipeline and best-of-N patterns.
structure:
  identity_keys: []
  fields:
    - id: merge_key
      labels:
        zh: 合并键
        en: merge key
        ja: merge key
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 指定判断两项是否代表同一逻辑记录的稳定字段或复合键。
        en: Names the stable field or compound key used to decide whether two items represent the same logical record.
        ja: 二 item が同じ logical record を表すか判断する stable field または compound key を指定します。
      allowed_values: []
      pattern: null
      example_value: section_id
      source_claims:
        - claim-merge-rule-design
    - id: duplicate_policy
      labels:
        zh: 重复项策略
        en: duplicate policy
        ja: duplicate policy
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 规定同一合并键出现多个输入项时拒绝、去重、择版或保留多值，并给出判定前提。
        en: Specifies reject, deduplicate, select-version, or retain-multiple behavior when several inputs share a merge key, including its preconditions.
        ja: 同一 merge key の複数 input を reject・deduplicate・select-version・retain-multiple のどれで扱うかと前提を規定します。
      allowed_values: []
      pattern: null
      example_value: reject duplicates unless one is an explicitly superseding revision
      source_claims:
        - claim-merge-rule-design
    - id: field_conflict_policy
      labels:
        zh: 字段冲突策略
        en: field conflict policy
        ja: field conflict policy
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 规定同一逻辑记录的同一字段出现不兼容非空值时如何拒绝、择版、保留多值或升级人工处理。
        en: Specifies rejection, version selection, multivalue retention, or escalation when one field of the same logical record has incompatible non-null values.
        ja: 同一 logical record の一 field に incompatible non-null value がある場合の reject・version selection・multivalue retention・escalation を規定します。
      allowed_values: []
      pattern: null
      example_value: accept a newer revision only when its provenance names the superseded revision; otherwise escalate
      source_claims:
        - claim-merge-rule-design
    - id: order_policy
      labels:
        zh: 结果顺序策略
        en: result order policy
        ja: result order policy
      datatype: string
      required: true
      cardinality:
        min: 1
        max: 1
      definitions:
        zh: 规定合并结果是无序映射还是按明确字段稳定排序，禁止把输入或序列化器的偶然顺序当作语义。
        en: Declares whether the result is an unordered mapping or stably sorted by an explicit field, rather than treating incidental input or serializer order as semantic.
        ja: result を unordered mapping とするか明示 field で stable sort するかを定め、input/serializer の偶然の順序を semantics にしません。
      allowed_values: []
      pattern: null
      example_value: ascending section_ordinal, then section_id
      source_claims:
        - claim-merge-rule-design
  constraints: []
  required_relation_constraints: []
examples:
  - id: MergeRule-conflict-preserve
    labels:
      zh: 冲突保留合并
      en: Conflict-preserving merge
      ja: conflict 保留 merge
    descriptions:
      zh: conflict-preserving@2 合并三章节时保留矛盾标注而非静默覆盖。
      en: conflict-preserving@2 merges three sections preserving conflict annotations instead of silent overwrite.
      ja: conflict-preserving@2 は三 section merge 時 conflict 注釈を保持し silent overwrite しません。
    example_source:
      product: OpenAI Cookbook
      url: https://cookbook.openai.com/
      version: map-reduce merge
    field_values:
      merge_rule_id: conflict-preserving
      version: "2"
    related_node_ids:
      - MergeRule
      - Synthesis
      - AggregationRule
    related_relation_ids: []
    source_claims:
      - claim-merge-rule-cookbook
      - claim-merge-rule-design
  - id: MergeRule-langgraph-reducer
    labels:
      zh: LangGraph 合并 reducer
      en: LangGraph merge reducer
      ja: LangGraph merge reducer
    descriptions:
      zh: combine(state["worker_outputs"]) 实现 evidence-priority@2 合并语义。
      en: combine(state["worker_outputs"]) implements evidence-priority@2 merge semantics.
      ja: combine(state["worker_outputs"]) が evidence-priority@2 merge 意味論を実装。
    example_source:
      product: LangGraph
      url: https://langchain-ai.github.io/langgraph/
      version: fan-in reducer
    field_values:
      merge_rule_id: evidence-priority
      version: "2"
    related_node_ids:
      - MergeRule
      - AggregationActivity
    related_relation_ids:
      - AggregationActivity-applies-AggregationRule
    source_claims:
      - claim-merge-rule-langgraph
      - claim-merge-rule-design
  - id: MergeRule-boundary-voting
    labels:
      zh: 计票规则不是合并规则
      en: Tally rule is not merge rule
      ja: 集計 rule は merge rule ではない
    descriptions:
      zh: borda-count@4 是 VotingRule 而非 MergeRule。
      en: borda-count@4 is VotingRule not MergeRule.
      ja: borda-count@4 は VotingRule で MergeRule ではありません。
    example_source:
      product: Moonweave
      url: https://moonweave.ai/ontology/agent-system/2.0.0/
      version: subtype boundary
    field_values: {}
    related_node_ids:
      - MergeRule
      - VotingRule
    related_relation_ids: []
    source_claims:
      - claim-merge-rule-design
parent_relation:
  id: MergeRule-is_a-AggregationRule
  predicate: is_a
  direction: target-to-source
  relation_kind: hierarchy
  labels:
    zh: 是一种
    en: is a
    ja: の一種である
  definitions:
    zh: 每个内容合并规则都继承聚合规则的输入资格、方法、完成与冲突语义，并增加合并键、重复项、字段冲突和结果顺序条件。
    en: Every MergeRule inherits AggregationRule input-eligibility, method, completion, and conflict semantics and adds merge-key, duplicate, field-conflict, and result-order conditions.
    ja: すべての MergeRule は AggregationRule の input eligibility・method・completion・conflict semantics を継承し merge key・duplicate・field conflict・result order condition を追加します。
  cardinality: null
  cardinality_not_applicable_reason:
    zh: is_a 表达类型包含而非实例间数量约束。
    en: is_a states type inclusion rather than an instance-to-instance quantity constraint.
    ja: is_a は instance 間の数量制約ではなく type inclusion を表します。
  inverse_reading:
    predicate: part_of
    labels:
      zh: 具有下位类型
      en: has subtype
      ja: 下位型を持つ
  conditions: []
  temporal_scope: timeless
  boundary_context: null
  constraints: []
  source_claims:
    - claim-merge-rule-design
  distinct_fact_rationale: null
  layout_role: primary-backbone
  endpoint_order: child-to-parent
relations: []
sources:
  - id: langgraph
    title: LangGraph
    url: https://langchain-ai.github.io/langgraph/
    source_type: official-doc
    relevance: StateGraph, Send API, sequential edges, parallel supersteps, reducers.
  - id: crewai
    title: CrewAI
    url: https://docs.crewai.com
    source_type: official-doc
    relevance: Process.sequential, Process.parallel, output consolidation.
  - id: anthropic-workflows
    title: Building effective agents (Anthropic)
    url: https://www.anthropic.com/engineering/building-effective-agents
    source_type: official-doc
    relevance: Prompt chaining, parallelization, orchestrator-worker, voting patterns.
  - id: openai-cookbook
    title: OpenAI Cookbook
    url: https://cookbook.openai.com/
    source_type: official-doc
    relevance: Multi-step agent workflows and evaluation patterns.
  - id: composition-design
    title: Moonweave composition boundary design
    url: https://moonweave.ai/ontology/agent-system/2.0.0/
    source_type: design-inference
    relevance: CompositionSpecification versus CompositionActivity separation.
source_claims:
  - id: claim-merge-rule-langgraph
    source: langgraph
    supports: Fan-in reducers implement merge semantics over parallel outputs.
    locator: Parallel supersteps
    evidence_kind: design-inference
    confidence: high
  - id: claim-merge-rule-cookbook
    source: openai-cookbook
    supports: Map-reduce merge strategies define how partial outputs combine.
    locator: Multi-step pipelines
    evidence_kind: design-inference
    confidence: high
  - id: claim-merge-rule-design
    source: composition-design
    supports: MergeRule specializes AggregationRule for synthesis merge.
    locator: MergeRule boundary
    evidence_kind: design-inference
    confidence: high
