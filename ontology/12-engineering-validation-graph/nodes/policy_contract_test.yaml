id: node.policy_contract_test
artifact: NodeClass
label: Policy Contract Test
label_zh: 策略契约测试
intra_level: 2
intra_group: contract-validation
intra_group_zh: 契约验证
intra_role: test
parent_node: node.graph_validator
layer: validation
description: >
  Validates policy enforcement paths ensuring all guarded actions are intercepted by policy evaluation.
attributes:
  checker_id:
    type: string
    required: true
    description: Unique checker identifier
  severity_default:
    type: enum
    required: false
    values: ['error', 'warning', 'info']
    description: Default severity for findings
  automated:
    type: boolean
    required: false
    description: Whether checker runs without manual intervention
interfaces:
  - ref: contract.validatable
state_machine:
  ref: state.validation_lifecycle
status: active
